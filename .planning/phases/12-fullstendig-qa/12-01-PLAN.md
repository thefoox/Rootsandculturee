---
phase: 12-fullstendig-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/products/CategoryTabs.tsx
  - src/app/(public)/opplevelser/[slug]/page.tsx
autonomous: true
requirements: [QA-01, QA-02, QA-03]

must_haves:
  truths:
    - "rust-colored text (prices, earlybird labels, admin links) renders in the correct reddish-brown color"
    - "CategoryTabs on /produkter stick below the fixed header, not behind it"
    - "Experience detail booking sidebar does not scroll under the fixed header"
  artifacts:
    - path: "src/app/globals.css"
      provides: "--color-rust CSS custom property"
      contains: "--color-rust"
    - path: "src/components/products/CategoryTabs.tsx"
      provides: "sticky positioning below header"
      contains: "top-20"
    - path: "src/app/(public)/opplevelser/[slug]/page.tsx"
      provides: "sticky sidebar with correct top offset"
      contains: "top-24"
  key_links:
    - from: "globals.css --color-rust"
      to: "text-rust Tailwind utility"
      via: "Tailwind v4 @theme block"
      pattern: "--color-rust"
    - from: "CategoryTabs sticky"
      to: "fixed header h-20"
      via: "top-20 = 80px = header height"
      pattern: "top-20"
---

<objective>
Fix three positioning/color bugs introduced during Phase 11 UI rewrites:
1. `--color-rust` CSS variable is missing from globals.css — 16 usages of `text-rust` across 11 files render without color
2. `CategoryTabs` uses `sticky top-0` — tabs collide with the 80px fixed header on /produkter
3. Experience detail booking sidebar uses `lg:top-8` (32px) — widget scrolls under the fixed header

Purpose: These are visible rendering bugs that degrade UX. The rust color is used for prices and earlybird labels everywhere — missing it means all monetary values display without their intended warm highlight.
Output: globals.css with --color-rust token, corrected sticky offsets
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key facts extracted from codebase for the executor -->

From src/app/globals.css @theme block — existing color tokens:
```
--color-forest: #1B4332
--color-cream: #FEFCF3
--color-card: #F5F0E8
--color-bark: #5C3D1E
--color-body: #374740
--color-destructive: #6B3A2A
--color-cart-badge: #B84D00    ← rust/orange family, existing
```
NOTE: --color-rust is referenced in 16 places but NOT defined. Needed value: #B84D00 is already used as cart-badge (orange). Rust in Norwegian design context = warm reddish-brown. Appropriate value: #A0440B (darker orange-red, distinct from cart-badge). The rust color appears in: earlybird pricing labels, admin price totals, admin "Se alle" links, admin section label, DateCard unselected state.

From src/components/products/CategoryTabs.tsx line 60:
```tsx
className="sticky top-0 z-10 border-b border-forest/12 bg-cream py-2"
```
Fix: change to `top-20` (= 80px = h-20 fixed header height). Also update `z-10` — the header uses `z-50`, so `z-10` is fine (tabs stay below header).

From src/app/(public)/opplevelser/[slug]/page.tsx line 206:
```tsx
<div className="lg:sticky lg:top-8">
```
Fix: change to `lg:top-24` (= 96px, gives 16px breathing room above header's 80px).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add --color-rust to globals.css</name>
  <files>src/app/globals.css</files>
  <action>
    In the `@theme` block in src/app/globals.css, add `--color-rust: #A0440B;` after the existing `--color-bark` entry (line ~9). This is a warm reddish-brown distinct from `--color-cart-badge` (#B84D00 which is brighter orange).

    Place it in the brand palette section, specifically after `--color-bark: #5C3D1E;`:
    ```css
    --color-bark: #5C3D1E;
    --color-rust: #A0440B;
    ```

    This single addition makes all 16 existing `text-rust` / `bg-rust` usages across the codebase resolve correctly — no changes needed in the files using these utilities.
  </action>
  <verify>
    <automated>grep "color-rust" src/app/globals.css</automated>
  </verify>
  <done>--color-rust: #A0440B appears in @theme block of globals.css. `npx tsc --noEmit` still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix CategoryTabs sticky position + experience detail sidebar offset</name>
  <files>
    src/components/products/CategoryTabs.tsx
    src/app/(public)/opplevelser/[slug]/page.tsx
  </files>
  <action>
    Fix 1 — CategoryTabs (src/components/products/CategoryTabs.tsx line 60):
    Change `sticky top-0` to `sticky top-20`. The fixed header is `h-20` (80px). `top-20` in Tailwind = 5rem = 80px exactly.
    Before: `className="sticky top-0 z-10 border-b border-forest/12 bg-cream py-2"`
    After:  `className="sticky top-20 z-10 border-b border-forest/12 bg-cream py-2"`

    Fix 2 — Experience detail booking sidebar (src/app/(public)/opplevelser/[slug]/page.tsx line 206):
    Change `lg:top-8` to `lg:top-24`. The fixed header is 80px; `top-24` = 6rem = 96px gives 16px clearance.
    Before: `<div className="lg:sticky lg:top-8">`
    After:  `<div className="lg:sticky lg:top-24">`
  </action>
  <verify>
    <automated>grep "top-20" src/components/products/CategoryTabs.tsx && grep "top-24" "src/app/(public)/opplevelser/[slug]/page.tsx"</automated>
  </verify>
  <done>CategoryTabs has `sticky top-20`. Experience detail sidebar has `lg:sticky lg:top-24`. TypeScript still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan modifies only CSS and static class names — no trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | CSS variables | accept | Color tokens are public design values, no PII |
</threat_model>

<verification>
After both tasks complete:
1. `grep "color-rust" src/app/globals.css` — must return a line with the variable
2. `grep "top-20" src/components/products/CategoryTabs.tsx` — must return a match
3. `grep "top-24" src/app/\(public\)/opplevelser/\[slug\]/page.tsx` — must return a match in the sticky div
4. `npx tsc --noEmit` — must return no errors
</verification>

<success_criteria>
- `--color-rust: #A0440B` is defined in @theme block of globals.css
- `CategoryTabs` sticky offset changed from `top-0` to `top-20`
- Experience detail sidebar sticky changed from `top-8` to `top-24`
- TypeScript compilation passes
- `npx next build` completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-fullstendig-qa/12-01-SUMMARY.md`
</output>
