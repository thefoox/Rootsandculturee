---
phase: 12-fullstendig-qa
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified: []
autonomous: false
requirements: [QA-07]

must_haves:
  truths:
    - "All pages render correctly in a real browser with no visible header overlap"
    - "Rust color is visible on earlybird labels and price displays"
    - "CategoryTabs on /produkter stick below the header, not behind it"
    - "Blog article cover images are fully visible"
    - "Filtering to an empty category shows the Norwegian empty state"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 12 fixes in a real browser. Plans 01 and 02 fixed: color token, sticky offsets, HeroImage padding, empty state, and dead code. This checkpoint confirms the fixes render correctly.

Purpose: QA fixes require eyes-on verification. TypeScript and build passing is necessary but not sufficient — visual rendering must be confirmed.
Output: Human confirmation that all fixes are correct
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-fullstendig-qa/12-01-SUMMARY.md
@.planning/phases/12-fullstendig-qa/12-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 12 bug fixes:
    1. --color-rust CSS token added (was missing — all earlybird labels and price highlights were colorless)
    2. CategoryTabs on /produkter now sticky at top-20 (80px) instead of top-0
    3. Experience detail booking sidebar now sticky at top-24 (96px) instead of top-8 (32px)
    4. HeroImage on blog article pages now has pt-20 wrapper (cover images were hidden behind header)
    5. FilterableExperienceGrid shows "Ingen opplevelser i denne kategorien ennå." for empty filter results
    6. DynamicPage.tsx dead code removed
  </what-built>
  <how-to-verify>
    Run `npm run dev` and test these specific scenarios:

    1. RUST COLOR — Visit /opplevelser, find any experience with earlybird pricing → earlybird label should appear in a warm reddish-brown (#A0440B). Visit /admin → "Se alle" links and price values should be rust-colored.

    2. CATEGORY TABS — Visit /produkter → scroll down until CategoryTabs would stick. The tab bar should stick just below the header, NOT behind it. The gap between the header bottom edge and the tab bar top edge should be 0.

    3. EXPERIENCE SIDEBAR — Visit any /opplevelser/[slug] page (e.g. /opplevelser/tidlig-var-retreat) → scroll down. The "Bestill opplevelse" sidebar should stick just below the header as you scroll, NOT disappear behind it.

    4. BLOG COVER IMAGE — Visit /blogg and click any article that has a cover image → the cover image should start below the header, fully visible. No header overlap on the image.

    5. EMPTY FILTER STATE — Visit homepage → in the "Kurs og Retreater" section, click a category filter (e.g. "Matopplevelse" if there are no food experiences) → should show "Ingen opplevelser i denne kategorien ennå." with a reset link. (If all categories have experiences, test by temporarily using browser devtools to set activeFilter to a non-existent category, or skip this test.)

    6. GENERAL — Visit /om-oss, /kontakt — confirm no visible layout issues with the overlapping card layout and fixed header.
  </how-to-verify>
  <resume-signal>Type "godkjent" if all checks pass, or describe any issues found so they can be fixed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification checkpoint only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | N/A | Verification | accept | No code changes in this plan |
</threat_model>

<verification>
Human confirmation that all six visual checks pass.
</verification>

<success_criteria>
- Human has verified rust color renders correctly
- Human has verified CategoryTabs sticky positioning
- Human has verified experience sidebar sticky positioning
- Human has verified blog cover image visibility
- Human has verified FilterableExperienceGrid empty state (or confirmed not testable with current data)
- No new issues found during manual testing
</success_criteria>

<output>
After completion, create `.planning/phases/12-fullstendig-qa/12-03-SUMMARY.md`
</output>
