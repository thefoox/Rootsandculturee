---
phase: 03-betaling-og-booking
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/experiences/DateCardPicker.tsx
  - src/components/experiences/DateCard.tsx
  - src/components/experiences/BookingInfoPanel.tsx
  - src/app/(public)/opplevelser/[slug]/page.tsx
  - src/actions/bookings.ts
  - src/lib/data/orders.ts
  - src/lib/data/bookings.ts
  - src/components/admin/AdminSidebar.tsx
  - src/components/admin/OrderStatusBadge.tsx
  - src/components/admin/BookingStatusBadge.tsx
  - src/components/admin/BookingFilterRow.tsx
  - src/app/admin/ordrer/page.tsx
  - src/app/admin/ordrer/[id]/page.tsx
  - src/app/admin/bookinger/page.tsx
autonomous: true
requirements:
  - BOOK-03
  - BOOK-04
  - BOOK-05
  - BOOK-06
  - BOOK-08
  - ADMN-05
  - ADMN-06

must_haves:
  truths:
    - "En kunde kan velge dato for en opplevelse via klikkbare datokort og se gjenstaende plasser i sanntid"
    - "En kunde kan legge en booking i handlekurven og betale via den felles Stripe-checkout-flyten"
    - "Nar alle plasser er fylt blokkeres booking og kunden ser en tydelig melding"
    - "Etter betaling mottar kunden bekreftelseskode pa side og via e-post, inkludert hva-du-ma-ta-med sjekkliste"
    - "Admin kan se alle ordrer og oppdatere status (bekreftet, sendt, levert)"
    - "Admin kan se alle bookinger per opplevelse/dato og kansellere bookinger"
  artifacts:
    - path: "src/components/experiences/DateCardPicker.tsx"
      provides: "Date card grid with selection state for booking"
      contains: "role=\"group\""
    - path: "src/components/experiences/DateCard.tsx"
      provides: "Individual date button with available/selected/sold-out states"
      contains: "aria-pressed"
    - path: "src/components/experiences/BookingInfoPanel.tsx"
      provides: "Booking info panel with spots count, price, and add-to-cart CTA"
      contains: "Legg booking i handlekurv"
    - path: "src/actions/bookings.ts"
      provides: "Booking server actions — list, get, cancel"
      exports: ["getBookings", "getBookingById", "cancelBooking"]
    - path: "src/lib/data/orders.ts"
      provides: "Order data functions with unstable_cache"
      contains: "unstable_cache"
    - path: "src/lib/data/bookings.ts"
      provides: "Booking data functions with unstable_cache"
      contains: "unstable_cache"
    - path: "src/app/admin/ordrer/page.tsx"
      provides: "Admin orders list page"
      contains: "Ordrer"
    - path: "src/app/admin/ordrer/[id]/page.tsx"
      provides: "Admin order detail page with status update"
      contains: "Oppdater status"
    - path: "src/app/admin/bookinger/page.tsx"
      provides: "Admin bookings list page with filters"
      contains: "Bookinger"
  key_links:
    - from: "src/components/experiences/DateCardPicker.tsx"
      to: "src/components/cart/CartProvider.tsx"
      via: "useCart hook to add booking to cart"
      pattern: "useCart"
    - from: "src/components/experiences/BookingInfoPanel.tsx"
      to: "src/components/cart/CartProvider.tsx"
      via: "addItem with experience CartItem on CTA click"
      pattern: "addItem"
    - from: "src/app/admin/ordrer/[id]/page.tsx"
      to: "src/actions/orders.ts"
      via: "updateOrderStatus server action"
      pattern: "updateOrderStatus"
    - from: "src/app/admin/bookinger/page.tsx"
      to: "src/actions/bookings.ts"
      via: "cancelBooking server action"
      pattern: "cancelBooking"
    - from: "src/app/api/webhooks/stripe/route.ts"
      to: "src/actions/bookings.ts"
      via: "Webhook creates booking with confirmation code on payment success"
      pattern: "confirmationCode"
---

<objective>
Implement the booking date picker on experience pages (date cards, spots display, add-to-cart), and build the admin order and booking management pages. The booking flow integrates with the cart and Stripe checkout from Plan 01 — bookings are added to the mixed cart and go through the same payment pipeline.

Purpose: This completes the full booking experience (the core differentiator of Roots & Culture — selling nature experiences, not just products) and gives admin the tools to manage all orders and bookings.

Output: Working booking flow from date selection through payment to confirmation code, plus admin dashboards for order and booking management.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-betaling-og-booking/03-CONTEXT.md
@.planning/phases/03-betaling-og-booking/03-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/03-betaling-og-booking/03-01-SUMMARY.md

@src/types/index.ts
@src/components/cart/CartProvider.tsx
@src/lib/cart.ts
@src/lib/stripe/server.ts
@src/lib/email/resend.ts
@src/lib/email/templates.ts
@src/actions/orders.ts
@src/app/api/webhooks/stripe/route.ts
@src/components/experiences/SpotsRemaining.tsx
@src/components/experiences/ExperienceCard.tsx
@src/components/admin/AdminSidebar.tsx
@src/components/admin/DataTable.tsx
@src/components/admin/StatusBadge.tsx
@src/components/admin/DeleteConfirmDialog.tsx
@src/components/admin/AdminShell.tsx
@src/components/admin/AdminBreadcrumb.tsx
@src/components/shared/EmptyState.tsx
@src/lib/data/experiences.ts
@src/lib/firebase/admin.ts
@src/lib/firebase/client.ts
@src/lib/dal.ts
@src/lib/format.ts
@src/app/(public)/opplevelser/[slug]/page.tsx
@src/app/admin/opplevelser/page.tsx

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on -->

From src/types/index.ts (added in Plan 01):
```typescript
export type CartItemType = 'product' | 'experience'
export interface CartItem {
  id: string; type: CartItemType; name: string; price: number;
  quantity: number; image: ProductImage | null; slug: string;
  experienceDateId: string | null; experienceDate: string | null;
  experienceName: string | null;
}
export type OrderStatus = 'pending' | 'paid' | 'confirmed' | 'shipped' | 'delivered' | 'cancelled'
export interface Order { /* ... full order type from Plan 01 */ }
export type BookingStatus = 'pending' | 'confirmed' | 'cancelled'
export interface Booking {
  id: string; confirmationCode: string; stripeSessionId: string;
  customerId: string | null; customerEmail: string; customerName: string;
  experienceId: string; experienceName: string; dateId: string; date: Date;
  seats: number; pricePerSeat: number; total: number; whatToBring: string;
  status: BookingStatus; createdAt: Date; confirmedAt: Date | null;
}
```

From src/components/cart/CartProvider.tsx (Plan 01):
```typescript
export function useCart(): {
  items: CartItem[]; addItem: (item: CartItem) => void;
  removeItem: (id: string, experienceDateId?: string) => void;
  updateQuantity: (id: string, quantity: number) => void;
  clearCart: () => void; itemCount: number; subtotal: number;
}
```

From src/actions/orders.ts (Plan 01):
```typescript
export async function getOrders(): Promise<Order[]>
export async function getOrderById(orderId: string): Promise<Order | null>
export async function updateOrderStatus(orderId: string, status: OrderStatus): Promise<void>
```

From src/components/admin/DataTable.tsx:
```typescript
// Renders a data table with columns config; used for products, experiences, articles
// Expects columns array and data array
```

From src/components/admin/DeleteConfirmDialog.tsx:
```typescript
// Renders a confirmation dialog with heading, body, confirm/cancel buttons
// Used for destructive actions
```

From src/components/admin/StatusBadge.tsx:
```typescript
// Renders a colored badge (green/amber/red variants)
```

From src/components/experiences/SpotsRemaining.tsx:
```typescript
// Renders spots remaining text with urgency coloring when <= 3
```

From src/lib/data/experiences.ts:
```typescript
export async function getExperienceBySlug(slug: string): Promise<Experience | null>
export async function getExperienceDates(experienceId: string): Promise<ExperienceDate[]>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Booking date picker components and experience page integration</name>
  <files>
    src/components/experiences/DateCardPicker.tsx,
    src/components/experiences/DateCard.tsx,
    src/components/experiences/BookingInfoPanel.tsx,
    src/app/(public)/opplevelser/[slug]/page.tsx
  </files>
  <read_first>
    src/types/index.ts,
    src/components/cart/CartProvider.tsx,
    src/lib/cart.ts,
    src/components/experiences/SpotsRemaining.tsx,
    src/components/experiences/ExperienceCard.tsx,
    src/components/ui/Button.tsx,
    src/lib/format.ts,
    src/lib/data/experiences.ts,
    src/lib/firebase/client.ts,
    src/app/(public)/opplevelser/[slug]/page.tsx,
    .planning/phases/03-betaling-og-booking/03-UI-SPEC.md,
    .planning/phases/03-betaling-og-booking/03-CONTEXT.md
  </read_first>
  <action>
**1. Create `src/components/experiences/DateCard.tsx`** (per D-10, 03-UI-SPEC Date Card):
- `'use client'` directive
- Props: `experienceDate: ExperienceDate`, `isSelected: boolean`, `onSelect: (date: ExperienceDate) => void`
- Element is a `<button type="button">` with `aria-pressed` for selected state
- Dimensions: 80px wide, 68px height (structural size per UI-SPEC)
- Layout (internal): day-of-week (Inter 13px 400, bark, uppercase, center), day number (Inter 15px 400, forest, center), month (Inter 13px 400, bark, center)
- Format dates with `Intl.DateTimeFormat('nb-NO')` — extract weekday short, day, month short
- **Default state**: `bg-card` (`#F5F0E8`), `border: 1px solid rgba(27,67,50,0.15)`, `rounded-lg`
- **Selected state**: `bg-ember` (`#B84D00`), `border: 2px solid #B84D00`, all text changes to `text-cream` (`#FEFCF3`)
- **Sold-out state**: `opacity-50`, `cursor-not-allowed`, `disabled` attribute, `aria-disabled="true"`, red "Utsolgt" label (Inter 13px 400, `#C0392B`, center below date)
- `aria-label`: `"{weekday}, {date long}"` for available dates, `"{weekday}, {date long}, utsolgt"` for sold-out
- Hover (available): `box-shadow: 0 2px 8px rgba(0,0,0,0.08)` with `motion-safe:` guard
- Focus: 2px solid forest outline, 2px offset (inherits global focus ring)
- Sold-out condition: `experienceDate.availableSeats <= 0` or `!experienceDate.isActive`
- Only show future dates (filter dates where `experienceDate.date > new Date()`)

**2. Create `src/components/experiences/DateCardPicker.tsx`** (per D-10, 03-UI-SPEC):
- `'use client'` directive
- Props: `experienceId: string`, `dates: ExperienceDate[]`, `experience: Experience` (for name, price, whatToBring)
- State: `selectedDate: ExperienceDate | null`
- Section heading: `<h2>` "Velg dato" (Merriweather 20px 700, forest)
- Container: `role="group"`, `aria-labelledby` pointing to section heading
- Grid layout: mobile — horizontal scroll row (`overflow-x: auto`, `gap-8`, `pb-8` for scrollbar), desktop — wrapping flex row (`flex-wrap`, `gap-8`)
- Renders `<DateCard>` for each future, active date, ordered by date ascending
- When a date is selected, renders `<BookingInfoPanel>` below the grid
- Uses Firestore `onSnapshot` listener on the experience dates subcollection for real-time seat updates — import `doc, onSnapshot` from `firebase/firestore` and `db` from `src/lib/firebase/client.ts`. Listen to `/experiences/{experienceId}/dates/{dateId}` for the selected date to get live `availableSeats` updates.

**3. Create `src/components/experiences/BookingInfoPanel.tsx`** (per 03-UI-SPEC):
- `'use client'` directive
- Props: `selectedDate: ExperienceDate`, `experience: Experience`, `onAddToCart: () => void`
- Only renders when a date is selected (parent controls visibility)
- Background: `bg-card`, `rounded-lg`, `p-16`, `border border-forest/12`
- Selected date formatted: `Intl.DateTimeFormat('nb-NO', { dateStyle: 'long', weekday: 'long' })`
- Spots remaining: reuse `<SpotsRemaining>` component (Phase 2) or inline with same styling — "X av Y plasser igjen", when <= 3: `#C0392B` text + sr-only "(lavt antall)" per Phase 2 contract
- Price: formatted NOK (rust color)
- CTA: "Legg booking i handlekurv" — `Button variant="primary"`, full width
- **Capacity block (0 spots / BOOK-06)**: No CTA renders. Instead show red message block: `bg-[#FEE2E2]`, `border border-[#C0392B]/20`, `rounded-lg`, `p-16`, text "Alle plasser er fylt. Velg en annen dato eller sjekk tilbake senere." (Inter 15px 400, `#C0392B`), `role="alert"` on this block
- On CTA click: calls `useCart().addItem()` with a `CartItem` of type `'experience'`:
  ```typescript
  {
    id: experience.id,
    type: 'experience',
    name: experience.name,
    price: selectedDate.priceOverride ?? experience.basePrice,
    quantity: 1,  // Always 1 for experiences (D-11)
    image: experience.images[0] ?? null,
    slug: experience.slug,
    experienceDateId: selectedDate.id,
    experienceDate: selectedDate.date.toISOString(),
    experienceName: experience.name,
  }
  ```
- After adding: sonner toast "{name} lagt i handlekurven." with `role="status"`

**4. Update `src/app/(public)/opplevelser/[slug]/page.tsx`** — integrate booking flow:
- Read the existing page component. It already renders experience details from Phase 2.
- Add a "Velg dato" section below the existing content (or replace the Phase 2 disabled placeholder)
- Import `DateCardPicker` and render it with the experience data and dates
- The experience detail page is a Server Component. `DateCardPicker` is a Client Component — pass data as serialized props:
  - `experienceId={experience.id}`
  - `dates={dates}` (serialize dates to ISO strings if needed for client hydration)
  - `experience={experience}` (the experience object for name, price, whatToBring)
- Ensure dates are fetched server-side via `getExperienceDates(experience.id)` from `src/lib/data/experiences.ts`
- Only show the DateCardPicker section if there are active future dates. If no dates available, show a muted message "Ingen tilgjengelige datoer for oeblikket."

**IMPORTANT NOTES:**
- Experience items in cart have quantity fixed at 1 (D-11). Quantity controls are hidden for experiences in CartItem (already handled in Plan 01).
- Cart item uniqueness for experiences: `id + experienceDateId`. A customer can book the same experience on different dates (two separate cart items).
- Real-time availability via Firestore `onSnapshot` on the selected date — this is a client-side listener, NOT server-side. Only listen when a date is selected, clean up listener on unmount or date change.
- All dates must be formatted with `Intl.DateTimeFormat('nb-NO')` for Norwegian locale.
  </action>
  <verify>
    <automated>cd /home/william/Documents/Rootsnew && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "role=\"group\"" src/components/experiences/DateCardPicker.tsx confirms group role on date picker
    - grep -r "aria-pressed" src/components/experiences/DateCard.tsx confirms pressed state on date cards
    - grep -r "aria-disabled" src/components/experiences/DateCard.tsx confirms disabled state for sold-out
    - grep -r "Utsolgt" src/components/experiences/DateCard.tsx confirms Norwegian sold-out label
    - grep -r "role=\"alert\"" src/components/experiences/BookingInfoPanel.tsx confirms alert for capacity block (BOOK-06)
    - grep -r "Legg booking i handlekurv" src/components/experiences/BookingInfoPanel.tsx confirms Norwegian CTA
    - grep -r "Alle plasser er fylt" src/components/experiences/BookingInfoPanel.tsx confirms capacity message
    - grep -r "onSnapshot" src/components/experiences/DateCardPicker.tsx confirms real-time seat availability
    - grep -r "useCart" src/components/experiences/BookingInfoPanel.tsx confirms cart integration
    - grep -r "DateCardPicker" src/app/\(public\)/opplevelser/\[slug\]/page.tsx confirms integration in experience page
    - The build completes without errors
  </acceptance_criteria>
  <done>
    Booking date picker works on experience detail pages: date cards display future dates with availability, selecting a date shows booking info panel with spots remaining and price, clicking "Legg booking i handlekurv" adds experience to mixed cart, sold-out dates show "Utsolgt" and are disabled, fully-booked dates show capacity block with role="alert", real-time seat updates via Firestore onSnapshot. All Norwegian copy, all WCAG requirements met.
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin order management, admin booking management, and sidebar update</name>
  <files>
    src/actions/bookings.ts,
    src/lib/data/orders.ts,
    src/lib/data/bookings.ts,
    src/components/admin/AdminSidebar.tsx,
    src/components/admin/OrderStatusBadge.tsx,
    src/components/admin/BookingStatusBadge.tsx,
    src/components/admin/BookingFilterRow.tsx,
    src/app/admin/ordrer/page.tsx,
    src/app/admin/ordrer/[id]/page.tsx,
    src/app/admin/bookinger/page.tsx
  </files>
  <read_first>
    src/types/index.ts,
    src/actions/orders.ts,
    src/lib/data/experiences.ts,
    src/lib/firebase/admin.ts,
    src/lib/dal.ts,
    src/lib/format.ts,
    src/components/admin/AdminSidebar.tsx,
    src/components/admin/DataTable.tsx,
    src/components/admin/StatusBadge.tsx,
    src/components/admin/DeleteConfirmDialog.tsx,
    src/components/admin/AdminShell.tsx,
    src/components/admin/AdminBreadcrumb.tsx,
    src/components/shared/EmptyState.tsx,
    src/app/admin/opplevelser/page.tsx,
    src/app/admin/produkter/page.tsx,
    .planning/phases/03-betaling-og-booking/03-UI-SPEC.md,
    .planning/phases/03-betaling-og-booking/03-CONTEXT.md
  </read_first>
  <action>
**1. Create `src/lib/data/orders.ts`** — Order data functions:
```typescript
import 'server-only'
import { unstable_cache } from 'next/cache'
import { adminDb } from '@/lib/firebase/admin'
import type { Order } from '@/types'
```
- `getOrders()` — wrapped in `unstable_cache` with tag `'orders'`, reads from `orders` collection, ordered by `createdAt` descending. Maps Firestore timestamps to Date objects. Returns `Order[]`.
- `getOrderById(orderId: string)` — reads single order document. Returns `Order | null`.
- Note: If `adminDb` is null (build-time, no credentials), return empty array / null.

**2. Create `src/lib/data/bookings.ts`** — Booking data functions:
```typescript
import 'server-only'
import { unstable_cache } from 'next/cache'
import { adminDb } from '@/lib/firebase/admin'
import type { Booking } from '@/types'
```
- `getBookings()` — wrapped in `unstable_cache` with tag `'bookings'`, reads from `bookings` collection, ordered by `createdAt` descending. Returns `Booking[]`.
- `getBookingsByExperience(experienceId: string)` — filtered query for admin filtering. Not cached (dynamic filtering).
- `getBookingsByExperienceAndDate(experienceId: string, dateId: string)` — double-filtered for admin.
- `getBookingById(bookingId: string)` — reads single booking document. Returns `Booking | null`.

**3. Create `src/actions/bookings.ts`** — Booking server actions:
```typescript
'use server'
```
- `cancelBooking(bookingId: string)`:
  - Verify session is admin (via `verifySession()` from `src/lib/dal.ts`)
  - Read booking document from Firestore
  - Run Firestore transaction: update booking status to `'cancelled'`, re-increment `availableSeats` and decrement `bookedSeats` on the experience date document (reverse the atomic reservation from webhook)
  - `revalidateTag('bookings')` after success
  - Returns `{ success: true }` or `{ error: 'Norwegian error message' }`
- `getBookingsFiltered(experienceId?: string, dateId?: string)`:
  - Verify session is admin
  - Calls data functions from `src/lib/data/bookings.ts` with filters
  - Returns `Booking[]`

**4. Update `src/components/admin/AdminSidebar.tsx`** (per 03-UI-SPEC Admin Sidebar Update):
- Import `ShoppingCart` and `CalendarCheck` from `lucide-react`
- Add a new "ORDRE" section after the existing "INNHOLD" section
- Add two nav items under "ORDRE":
  ```typescript
  const orderNavItems = [
    { href: '/admin/ordrer', label: 'Ordrer', icon: ShoppingCart },
    { href: '/admin/bookinger', label: 'Bookinger', icon: CalendarCheck },
  ]
  ```
- Render with the same styling as the "INNHOLD" section: section label "ORDRE" in rust 13px uppercase, nav links with 44px height, active state with `border-l-3 border-ember`
- Keep existing nav items unchanged

**5. Create `src/components/admin/OrderStatusBadge.tsx`** (per 03-UI-SPEC):
- Props: `status: OrderStatus`
- Reuses the Phase 2 `StatusBadge` pattern or creates a new component with the same badge token styling:
  - Bekreftet (confirmed): `bg-[#DCFCE7] text-[#166534]` (success green / badge-easy)
  - Sendt (shipped): `bg-[#FEF3C7] text-[#92400E]` (amber / badge-moderate)
  - Levert (delivered): `bg-card text-forest border border-forest/20` (neutral)
  - Avbrutt (cancelled): `bg-[#FEE2E2] text-[#991B1B]` (red / badge-hard)
- Badge size: Inter 13px 400, `rounded-full`, `px-8 py-4`
- Norwegian status labels: "Bekreftet", "Sendt", "Levert", "Avbrutt"
- Status label mapping: `{ paid: 'Bekreftet', confirmed: 'Bekreftet', shipped: 'Sendt', delivered: 'Levert', cancelled: 'Avbrutt' }` (both `paid` and `confirmed` show as "Bekreftet")

**6. Create `src/components/admin/BookingStatusBadge.tsx`** (per 03-UI-SPEC):
- Props: `status: BookingStatus`
- Two states only:
  - Bekreftet (confirmed): `bg-[#DCFCE7] text-[#166534]` (success green)
  - Kansellert (cancelled): `bg-[#FEE2E2] text-[#991B1B]` (red)
- Same badge styling as OrderStatusBadge

**7. Create `src/app/admin/ordrer/page.tsx`** (per D-21, ADMN-05, 03-UI-SPEC):
- Follows Phase 2 admin page pattern: `AdminShell` > `AdminBreadcrumb` > `h1` "Ordrer" > `DataTable`
- Verify admin session server-side
- Fetch orders via `getOrders()` from `src/lib/data/orders.ts`
- DataTable columns (per 03-UI-SPEC):
  | Column | Key | Width | Formatting |
  |--------|-----|-------|------------|
  | Ordrenr. | id (truncated) | 120px | Inter 13px 400, bark |
  | Kunde | customerEmail | auto | Inter 15px 400, forest |
  | Varer | items.length | 80px | Center-aligned, Inter 13px 400, bark |
  | Total | total | 100px | `formatPrice()`, rust, right-aligned |
  | Status | status | 120px | `<OrderStatusBadge>` |
  | Dato | createdAt | 120px | `Intl.DateTimeFormat('nb-NO', { dateStyle: 'medium' })`, bark 13px |
  | Handlinger | — | 80px | Eye icon ghost button, `aria-label="Se ordre {id}"`, links to `/admin/ordrer/{id}` |
- No "Legg til" button (orders created by customers)
- Empty state: EmptyState with heading "Ingen ordrer enda", body "Ordrer vises her etter at kunder har gjennomfart kjop."

**8. Create `src/app/admin/ordrer/[id]/page.tsx`** (per D-21, ADMN-05, 03-UI-SPEC):
- Single column form layout, max-width 720px (matches Phase 2 edit form pattern)
- Verify admin session server-side
- Fetch order by ID via `getOrderById(params.id)`
- Page heading: "Ordre {short-id}" (h1, Merriweather 28px 700)
- Content sections:
  1. "Bestillingsoversikt" — itemized product list with names, quantities, prices
  2. "Kundeinfo" — email, shipping address (if present)
  3. "Betaling" — total paid (formatted NOK), Stripe payment intent ID (13px bark), payment date
  4. "Status" — `<select>` with `<label>` "Ordrestatus", options: "Bekreftet", "Sendt", "Levert", "Avbrutt"; "Oppdater status" `Button variant="primary"`
- Status update uses `updateOrderStatus` server action from `src/actions/orders.ts`
- On success: sonner toast "Ordrestatus oppdatert." with `role="status"`
- On error: sonner toast with error message and `role="alert"`

**9. Create `src/components/admin/BookingFilterRow.tsx`** (per 03-UI-SPEC):
- `'use client'` directive
- Props: `experiences: { id: string, name: string }[]`, `dates: { id: string, date: Date, experienceId: string }[]`, `onFilter: (experienceId?: string, dateId?: string) => void`
- Flex row, `gap-16`, `mb-24`
- "Filtrer etter opplevelse:" `<select>` with `<label>` and `htmlFor` — lists all experiences
- "Filtrer etter dato:" `<select>` with `<label>` — dates populated from selected experience only
- "Nullstill filter" — ghost `Button`, bark text, resets both selects
- When experience changes: reset date filter, update available dates for that experience
- Triggers `onFilter` callback on any filter change

**10. Create `src/app/admin/bookinger/page.tsx`** (per D-22, ADMN-06, 03-UI-SPEC):
- Follows Phase 2 admin page pattern: `AdminShell` > `AdminBreadcrumb` > `h1` "Bookinger" > filters > `DataTable`
- Verify admin session server-side
- Fetch bookings via `getBookings()`, experiences list for filter dropdown
- DataTable columns (per 03-UI-SPEC):
  | Column | Key | Width | Formatting |
  |--------|-----|-------|------------|
  | Kode | confirmationCode | 120px | Inter 13px 400, bark, `letter-spacing: 0.04em` |
  | Opplevelse | experienceName | auto | Inter 15px 400, forest |
  | Dato | date | 120px | `Intl.DateTimeFormat('nb-NO', { dateStyle: 'medium' })`, bark 13px |
  | Kunde | customerEmail | 160px | Inter 13px 400, bark |
  | Status | status | 100px | `<BookingStatusBadge>` |
  | Handlinger | — | 100px | Eye icon (ghost button), XCircle "Kanseller" icon (bark, hover `#C0392B`, `aria-label="Kanseller booking {kode}"`) |
- `<BookingFilterRow>` above the table for experience/date filtering
- Cancellation: XCircle button triggers `<DeleteConfirmDialog>` with heading "Kanseller booking?", body "Dette varsler ikke kunden automatisk. Vil du kansellere booking {kode}?", confirm "Ja, kanseller" (destructive red), cancel "Nei, behold"
- On confirm: calls `cancelBooking(bookingId)` server action (which runs Firestore transaction to reverse seat reservation)
- On success: sonner toast "Booking kansellert." with `role="status"`
- On error: sonner toast "Kunne ikke kansellere. Prov igjen." with `role="alert"`
- Empty state: EmptyState with heading "Ingen bookinger enda", body "Bookinger vises her etter at kunder har reservert plasser."
- Client-side filtering: when user selects experience/date filters, filter the bookings list client-side (data already loaded). Use `startTransition` if using server-side filtering instead.

**IMPORTANT NOTES:**
- Admin pages verify admin session via `verifySession()` — redirect non-admins per existing middleware pattern
- Follow Phase 2 admin layout patterns exactly: AdminShell for layout, AdminBreadcrumb for navigation, DataTable for list pages
- Booking cancellation reverses the atomic seat reservation: increment `availableSeats`, decrement `bookedSeats` in a Firestore transaction
- Use `revalidateTag('orders')` and `revalidateTag('bookings')` after mutations
- revalidateTag requires second arg `'max'` in Next.js 16 per Phase 02 decision
- Norwegian copy throughout per 03-UI-SPEC copywriting contract
  </action>
  <verify>
    <automated>cd /home/william/Documents/Rootsnew && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "ORDRE" src/components/admin/AdminSidebar.tsx confirms new section added
    - grep -r "ShoppingCart" src/components/admin/AdminSidebar.tsx confirms orders icon imported
    - grep -r "CalendarCheck" src/components/admin/AdminSidebar.tsx confirms bookings icon imported
    - grep -r "/admin/ordrer" src/components/admin/AdminSidebar.tsx confirms orders nav link
    - grep -r "/admin/bookinger" src/components/admin/AdminSidebar.tsx confirms bookings nav link
    - grep -r "unstable_cache" src/lib/data/orders.ts confirms cached data functions
    - grep -r "unstable_cache" src/lib/data/bookings.ts confirms cached data functions
    - grep -r "cancelBooking" src/actions/bookings.ts confirms booking cancellation action
    - grep -r "runTransaction" src/actions/bookings.ts confirms atomic seat reversal on cancellation
    - grep -r "revalidateTag" src/actions/bookings.ts confirms cache invalidation
    - grep -r "Ordrer" src/app/admin/ordrer/page.tsx confirms orders page heading
    - grep -r "Oppdater status" src/app/admin/ordrer/\[id\]/page.tsx confirms status update
    - grep -r "Bookinger" src/app/admin/bookinger/page.tsx confirms bookings page heading
    - grep -r "Kanseller booking" src/app/admin/bookinger/page.tsx confirms cancellation dialog
    - grep -r "Bekreftet" src/components/admin/OrderStatusBadge.tsx confirms Norwegian status labels
    - grep -r "Kansellert" src/components/admin/BookingStatusBadge.tsx confirms Norwegian status labels
    - The build completes without errors
  </acceptance_criteria>
  <done>
    Admin sidebar updated with "ORDRE" section containing Ordrer and Bookinger links. Admin orders page shows all orders in DataTable with status badges, links to detail page where status can be updated. Admin bookings page shows all bookings with experience/date filtering, booking cancellation with confirmation dialog that atomically reverses seat reservation. Order and booking data functions use unstable_cache with proper tags. All Norwegian copy, all WCAG requirements met. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds without errors
2. Date picker: experience detail page shows clickable date cards for future dates, selecting a date shows booking info panel with spots and CTA
3. Booking to cart: clicking "Legg booking i handlekurv" adds experience to mixed cart, toast confirms
4. Sold-out: dates with 0 available seats show "Utsolgt", are disabled, cannot be selected
5. Capacity block: when all spots filled, shows red alert message instead of CTA (BOOK-06)
6. Real-time: Firestore onSnapshot updates available seats on date card selection
7. Admin sidebar: "ORDRE" section visible with Ordrer and Bookinger links
8. Admin orders: list page shows orders, detail page allows status update with toast feedback
9. Admin bookings: list page with experience/date filters, cancellation reverses seats atomically
</verification>

<success_criteria>
- A customer can visit an experience page, see available dates as visual date cards, select a date, see remaining spots in real time, and add the booking to the mixed cart
- When all spots are filled, the customer sees "Alle plasser er fylt" with role="alert" and cannot book (BOOK-06)
- The booking flows through the same Stripe checkout as products (mixed cart per D-02, D-11)
- After payment, the webhook creates the booking with confirmation code and sends email with "hva du ma ta med" checklist (BOOK-05, BOOK-08)
- Admin can see all orders in a DataTable, click through to detail, and update status (ADMN-05)
- Admin can see all bookings filtered by experience/date, and cancel bookings with confirmation dialog that reverses the seat reservation (ADMN-06)
- Admin sidebar includes the new "ORDRE" section with correct icons and active states
- All Norwegian copy per UI-SPEC copywriting contract
- All WCAG accessibility requirements met
</success_criteria>

<output>
After completion, create `.planning/phases/03-betaling-og-booking/03-02-SUMMARY.md`
</output>
