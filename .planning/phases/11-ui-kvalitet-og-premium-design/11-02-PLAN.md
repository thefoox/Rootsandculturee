---
phase: 11-ui-kvalitet-og-premium-design
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Button.tsx
  - src/components/ui/Input.tsx
autonomous: true
requirements:
  - UI11-FORMS
must_haves:
  truths:
    - "Button default padding is px-5 py-2.5 (not px-4 py-2)"
    - "Button primary variant has shadow-sm hover:shadow-md"
    - "Button transition uses motion-safe:duration-150 (not duration-100)"
    - "Input padding is px-4 py-3 (not px-3 py-2)"
    - "Input border-radius is rounded-lg (not rounded-md)"
    - "Input focus state shows ring-2 ring-forest/15 in addition to border-forest"
    - "Input label uses font-medium (not font-normal)"
    - "Input error state has bg-destructive/5 background tint"
  artifacts:
    - path: "src/components/ui/Button.tsx"
      provides: "Updated padding, shadow, transition"
    - path: "src/components/ui/Input.tsx"
      provides: "Updated padding, radius, focus ring, label weight, error tint"
  key_links:
    - from: "src/components/ui/Button.tsx"
      to: "CheckoutForm, HeroSection"
      via: "className prop overrides hero CTA to px-8 py-4"
      pattern: "px-5 py-2.5"
    - from: "src/components/ui/Input.tsx"
      to: "CheckoutForm, auth forms"
      via: "direct import"
      pattern: "rounded-lg"
---

<objective>
Refine Button and Input components to premium standards: larger touch targets with breathing room, softer radius on inputs, focus ring glow on inputs, medium-weight labels, subtle error tint. These are self-contained component upgrades with no external dependencies.

Purpose: Forms are the primary purchase conversion path. Premium form feel increases perceived trust.
Output: Button.tsx and Input.tsx with UI-SPEC exact values applied.
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ui-kvalitet-og-premium-design/11-UI-SPEC.md

<interfaces>
<!-- Current Button.tsx base classes: -->
```typescript
'inline-flex items-center justify-center gap-2 rounded-md px-4 py-2 font-body text-body font-medium transition-all duration-100'
'min-h-[44px]'
// primary: 'bg-forest text-cream hover:bg-forest/90 active:scale-[0.98]'
// secondary: 'border-2 border-forest text-forest hover:opacity-85 active:scale-[0.98]'
// ghost: 'text-forest hover:opacity-85 active:scale-[0.98]'
```

<!-- Current Input.tsx classes: -->
```typescript
// label: 'text-label font-normal tracking-wide text-forest'
// input: 'min-h-[44px] rounded-md border bg-card px-3 py-2 font-body text-body text-forest placeholder:text-body/60'
// + 'transition-colors duration-100'
// error state: 'border-destructive'
// rest state: 'border-forest/20 focus:border-forest'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Button component — padding, shadow, transition</name>
  <files>src/components/ui/Button.tsx</files>
  <read_first>src/components/ui/Button.tsx</read_first>
  <action>
In `src/components/ui/Button.tsx`, make the following targeted changes:

**1. Base class string — change padding and transition:**
Replace:
```
'inline-flex items-center justify-center gap-2 rounded-md px-4 py-2 font-body text-body font-medium transition-all duration-100',
```
With:
```
'inline-flex items-center justify-center gap-2 rounded-md px-5 py-2.5 font-body text-body font-medium motion-safe:transition-all motion-safe:duration-150',
```

Note: `rounded-md` is intentionally kept (UI-SPEC confirms it correct for Button; hero CTA overrides to `rounded-full` via className prop).

**2. Primary variant — add shadow:**
Replace:
```
primary: 'bg-forest text-cream hover:bg-forest/90 active:scale-[0.98]',
```
With:
```
primary: 'bg-forest text-cream shadow-sm hover:bg-forest/90 hover:shadow-md active:scale-[0.98]',
```

**3. Do NOT change** `secondary` or `ghost` variants. Do NOT change `min-h-[44px]`. Do NOT change `forwardRef` structure.

The effect: default Button gets more breathing room (px-5 vs px-4), primary buttons get a subtle lift shadow, all buttons get a more intentional 150ms transition. Hero CTA in HeroSection passes `className="px-8 py-4 rounded-full"` which overrides padding — that override is applied in Plan 05.
  </action>
  <verify>
    <automated>grep -n "px-5 py-2.5\|shadow-sm\|motion-safe:duration-150" /home/william/Documents/Rootsnew/src/components/ui/Button.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `px-5 py-2.5` present in base classes
    - `shadow-sm hover:shadow-md` present in primary variant
    - `motion-safe:transition-all motion-safe:duration-150` present (not bare `transition-all duration-100`)
    - `min-h-[44px]` still present
    - TypeScript compiles: `cd /home/william/Documents/Rootsnew && npx tsc --noEmit 2>&1 | head -20`
  </acceptance_criteria>
  <done>Button uses px-5 py-2.5 default, primary has shadow-sm hover:shadow-md, transition is 150ms with motion-safe guard.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Input component — padding, radius, focus ring, label weight, error tint</name>
  <files>src/components/ui/Input.tsx</files>
  <read_first>src/components/ui/Input.tsx</read_first>
  <action>
In `src/components/ui/Input.tsx`, make the following targeted changes:

**1. Label — change font-normal to font-medium:**
Replace:
```
className="text-label font-normal tracking-wide text-forest"
```
With:
```
className="text-label font-medium tracking-wide text-forest"
```

**2. Input base classes — change padding and radius:**
Replace the cn() call's base string from:
```
'min-h-[44px] rounded-md border bg-card px-3 py-2 font-body text-body text-forest placeholder:text-body/60',
'transition-colors duration-100',
```
With:
```
'min-h-[44px] rounded-lg border bg-card px-4 py-3 font-body text-body text-forest placeholder:text-body/60',
'motion-safe:transition-colors motion-safe:duration-150',
```

**3. Focus ring — update the non-error border/focus classes:**
Replace:
```
error
  ? 'border-destructive'
  : 'border-forest/20 focus:border-forest',
```
With:
```
error
  ? 'border-destructive bg-destructive/5'
  : 'border-forest/20 focus:border-forest focus:outline-none focus:ring-2 focus:ring-forest/15',
```

Note: `focus:outline-none` on Input ONLY suppresses the global `*:focus-visible` outline in favour of the ring. The global rule stays in globals.css for all other elements. The ring provides equivalent accessibility with 3:1+ contrast ratio.

**4. Do NOT change** the error paragraph, the `aria-describedby`, `aria-invalid`, or `forwardRef` structure.
  </action>
  <verify>
    <automated>grep -n "rounded-lg\|px-4 py-3\|font-medium\|ring-2\|bg-destructive/5\|motion-safe:transition" /home/william/Documents/Rootsnew/src/components/ui/Input.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rounded-lg` present (not `rounded-md`)
    - `px-4 py-3` present (not `px-3 py-2`)
    - `font-medium` on label (not `font-normal`)
    - `focus:ring-2 focus:ring-forest/15 focus:outline-none` present in non-error state
    - `bg-destructive/5` present in error state
    - `motion-safe:transition-colors motion-safe:duration-150` present
    - `min-h-[44px]` still present
    - TypeScript compiles
  </acceptance_criteria>
  <done>Input has rounded-lg, px-4 py-3 padding, medium-weight label, soft focus ring, and destructive/5 error tint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form inputs → Server Actions | User-typed data enters through Input components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | Input.tsx focus:outline-none | mitigate | focus:ring-2 ring-forest/15 replaces the outline — equivalent accessibility, 3:1 contrast maintained. Global focus-visible rule unchanged for all other elements |
| T-11-04 | Spoofing | Input error state bg-destructive/5 | accept | Visual-only tint, no security boundary. Server-side validation remains unchanged |
</threat_model>

<verification>
1. `grep "px-5 py-2.5" /home/william/Documents/Rootsnew/src/components/ui/Button.tsx` — finds padding
2. `grep "rounded-lg" /home/william/Documents/Rootsnew/src/components/ui/Input.tsx` — finds radius
3. `grep "ring-2" /home/william/Documents/Rootsnew/src/components/ui/Input.tsx` — finds focus ring
4. `cd /home/william/Documents/Rootsnew && npx tsc --noEmit 2>&1 | head -20` — no TS errors
</verification>

<success_criteria>
- Button: px-5 py-2.5 default, shadow-sm on primary, motion-safe:duration-150
- Input: rounded-lg, px-4 py-3, font-medium label, focus ring, destructive/5 error tint
- All existing WCAG touch targets (min-h-[44px]) preserved
- TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-kvalitet-og-premium-design/11-02-SUMMARY.md`
</output>
