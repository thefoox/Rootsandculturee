---
phase: 11-ui-kvalitet-og-premium-design
plan: 06
type: execute
wave: 3
depends_on:
  - 11-01-PLAN.md
  - 11-02-PLAN.md
files_modified:
  - src/components/layout/Footer.tsx
  - src/components/checkout/CheckoutForm.tsx
  - src/hooks/useScrollReveal.ts
  - src/components/ui/ScrollReveal.tsx
autonomous: true
requirements:
  - UI11-FOOTER
  - UI11-SCROLLREVEAL
  - UI11-CHECKOUT
must_haves:
  truths:
    - "Footer background is bg-card (not bg-cream)"
    - "Footer brand description uses text-body (not text-label)"
    - "Footer newsletter section is left-aligned in its own full-width row with border-t, not centered max-w-md"
    - "Footer link hover removes underline, uses only color transition (motion-safe:duration-150)"
    - "Footer copyright uses text-forest/40"
    - "ScrollReveal component exists and wraps children, applying fade-up when intersecting"
    - "ScrollReveal immediately renders children visible if prefers-reduced-motion: reduce"
    - "CheckoutForm step indicator has step labels below circles: Kontaktinfo / Betaling"
    - "CheckoutForm step connector line fills forest green for completed steps"
  artifacts:
    - path: "src/hooks/useScrollReveal.ts"
      provides: "IntersectionObserver hook returning ref and isVisible"
    - path: "src/components/ui/ScrollReveal.tsx"
      provides: "Client component wrapping children with fade-up reveal"
    - path: "src/components/layout/Footer.tsx"
      provides: "bg-card, text-body brand desc, left-aligned newsletter, no underline hover, forest/40 copyright"
    - path: "src/components/checkout/CheckoutForm.tsx"
      provides: "Step labels Kontaktinfo/Betaling, filled connector line"
  key_links:
    - from: "src/hooks/useScrollReveal.ts"
      to: "src/components/ui/ScrollReveal.tsx"
      via: "hook used inside ScrollReveal component"
      pattern: "useScrollReveal"
    - from: "src/components/ui/ScrollReveal.tsx"
      to: "consumers (section headings, trust bar)"
      via: "exported wrapper component"
      pattern: "ScrollReveal"
---

<objective>
Footer visual upgrade to bg-card with editorial newsletter layout. Scroll reveal hook + component for section-level fade-up animations. CheckoutForm step indicator upgrade with labels and filled connector line.

Purpose: Footer grounds the page. Scroll reveal adds premium motion depth. Checkout step clarity reduces abandonment.
Output: Updated Footer.tsx, new useScrollReveal hook, new ScrollReveal component, updated CheckoutForm step indicator.
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ui-kvalitet-og-premium-design/11-UI-SPEC.md

<interfaces>
<!-- Current Footer structure summary:
  - <footer className="border-t border-forest/10 bg-cream px-6 py-14 lg:px-8">
  - 5-column grid: brand col (md:col-span-1) + 4 nav columns
  - Newsletter section: mt-10 border-t pt-8 → <div mx-auto max-w-md> (centered)
  - Link hover: text-body/70 hover:text-forest hover:underline
  - Copyright: text-label text-body/50
-->

<!-- Current CheckoutForm step indicator:
  <div className="mb-8 flex items-center gap-3">
    <div className={step === 1 ? 'bg-forest text-cream' : 'bg-forest/10 text-forest'} ... >1</div>
    <div className="h-px flex-1 bg-forest/12" />
    <div className={step === 2 ? 'bg-forest text-cream' : 'bg-forest/10 text-forest'} ... >2</div>
  </div>
-->

<!-- IntersectionObserver API: available in browser, not available server-side.
     ScrollReveal must be a 'use client' component. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Footer — bg-card, text-body brand, left-aligned newsletter, link hover, copyright</name>
  <files>src/components/layout/Footer.tsx</files>
  <read_first>src/components/layout/Footer.tsx</read_first>
  <action>
In `src/components/layout/Footer.tsx`, apply all five UI-SPEC footer upgrades:

**1. Footer background — bg-cream → bg-card:**
Replace:
```typescript
<footer className="border-t border-forest/10 bg-cream px-6 py-14 lg:px-8">
```
With:
```typescript
<footer className="border-t border-forest/10 bg-card px-6 py-14 lg:px-8">
```

**2. Brand description — text-label → text-body:**
Replace:
```typescript
<p className="mt-3 text-label leading-relaxed text-body/70">
```
With:
```typescript
<p className="mt-3 text-body leading-relaxed text-body/70">
```

**3. Link hover — remove hover:underline, add transition:**
There are two link patterns in the footer (external `<a>` and internal `<Link>`). Update both:

Replace ALL occurrences of:
```typescript
className="text-body/70 hover:text-forest hover:underline"
```
With:
```typescript
className="text-body/70 motion-safe:transition-colors motion-safe:duration-150 hover:text-forest"
```

**4. Newsletter section — left-aligned full-width (not centered max-w-md):**
Replace the entire newsletter `<div className="mt-10 border-t border-forest/8 pt-8">` block with:
```typescript
<div className="mt-10 border-t border-forest/8 pt-8">
  <div className="flex flex-col gap-3 md:flex-row md:items-end md:justify-between">
    <div>
      <h2 className="mb-2 text-label font-bold uppercase tracking-wider text-forest">
        Nyhetsbrev
      </h2>
      <p className="text-body/70 max-w-sm">
        Få nyheter om produkter, opplevelser og norsk natur rett i innboksen.
      </p>
    </div>
    <div className="md:min-w-[320px]">
      <NewsletterSignup />
    </div>
  </div>
</div>
```
This puts the heading+description on the left and the signup form on the right (desktop), stacking vertically on mobile.

**5. Copyright — text-body/50 → text-forest/40:**
Replace:
```typescript
<div className="mt-8 border-t border-forest/8 pt-6 text-center text-label text-body/50">
```
With:
```typescript
<div className="mt-8 border-t border-forest/8 pt-6 text-center text-label text-forest/40">
```
  </action>
  <verify>
    <automated>grep -n "bg-card\|text-body leading\|hover:text-forest\|text-forest/40\|md:flex-row md:items-end" /home/william/Documents/Rootsnew/src/components/layout/Footer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `bg-card` on footer (not `bg-cream`)
    - Brand description uses `text-body leading-relaxed`
    - All link hovers have only `hover:text-forest` (no `hover:underline`) with `motion-safe:transition-colors motion-safe:duration-150`
    - Newsletter is `md:flex-row md:items-end md:justify-between` (not centered max-w-md)
    - Copyright uses `text-forest/40`
    - TypeScript compiles: `cd /home/william/Documents/Rootsnew && npx tsc --noEmit 2>&1 | head -20`
  </acceptance_criteria>
  <done>Footer has bg-card, editorial newsletter layout, clean link hover, and forest/40 copyright.</done>
</task>

<task type="auto">
  <name>Task 2: Create useScrollReveal hook and ScrollReveal component; upgrade CheckoutForm step indicator</name>
  <files>src/hooks/useScrollReveal.ts, src/components/ui/ScrollReveal.tsx, src/components/checkout/CheckoutForm.tsx</files>
  <read_first>src/components/checkout/CheckoutForm.tsx</read_first>
  <action>
**Step 1: Create `src/hooks/useScrollReveal.ts`**

```typescript
'use client'

import { useEffect, useRef, useState } from 'react'

interface UseScrollRevealOptions {
  threshold?: number
  once?: boolean
}

/**
 * Returns a ref to attach to the element and a boolean indicating if it's visible.
 * Uses IntersectionObserver with 0.15 threshold (15% visible triggers).
 * Respects prefers-reduced-motion — if user prefers reduced motion, isVisible is always true.
 */
export function useScrollReveal(options: UseScrollRevealOptions = {}) {
  const { threshold = 0.15, once = true } = options
  const ref = useRef<HTMLDivElement>(null)
  const [isVisible, setIsVisible] = useState(false)

  useEffect(() => {
    // If user prefers reduced motion, skip animation and show immediately
    const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches
    if (prefersReduced) {
      setIsVisible(true)
      return
    }

    const element = ref.current
    if (!element) return

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            setIsVisible(true)
            if (once) observer.unobserve(element)
          } else if (!once) {
            setIsVisible(false)
          }
        })
      },
      { threshold }
    )

    observer.observe(element)
    return () => observer.disconnect()
  }, [threshold, once])

  return { ref, isVisible }
}
```

**Step 2: Create `src/components/ui/ScrollReveal.tsx`**

```typescript
'use client'

import { useScrollReveal } from '@/hooks/useScrollReveal'
import { cn } from '@/lib/utils'

interface ScrollRevealProps {
  children: React.ReactNode
  className?: string
  delay?: 0 | 100 | 200 | 300
}

/**
 * Wraps children in a fade-up reveal animation on scroll.
 * Children are immediately visible if prefers-reduced-motion: reduce.
 * delay is in milliseconds: 0, 100, 200, or 300.
 */
export function ScrollReveal({ children, className, delay = 0 }: ScrollRevealProps) {
  const { ref, isVisible } = useScrollReveal()

  const delayClass = {
    0: '',
    100: 'motion-safe:[transition-delay:100ms]',
    200: 'motion-safe:[transition-delay:200ms]',
    300: 'motion-safe:[transition-delay:300ms]',
  }[delay]

  return (
    <div
      ref={ref}
      className={cn(
        'motion-safe:transition-all motion-safe:duration-500',
        isVisible
          ? 'opacity-100 translate-y-0'
          : 'motion-safe:opacity-0 motion-safe:translate-y-4',
        delayClass,
        className
      )}
    >
      {children}
    </div>
  )
}
```

Note on the `motion-safe:opacity-0 motion-safe:translate-y-4` pattern: These classes only apply when motion is safe. If prefers-reduced-motion is set (or the observer returns true immediately), `isVisible` is true and the element renders at `opacity-100 translate-y-0` immediately — no flash.

**Step 3: Upgrade CheckoutForm step indicator**

In `src/components/checkout/CheckoutForm.tsx`, find the step indicator block:

```typescript
{/* Step indicator */}
<div className="mb-8 flex items-center gap-3">
  <div className={`flex h-8 w-8 items-center justify-center rounded-full text-sm font-bold ${step === 1 ? 'bg-forest text-cream' : 'bg-forest/10 text-forest'}`}>
    1
  </div>
  <div className="h-px flex-1 bg-forest/12" />
  <div className={`flex h-8 w-8 items-center justify-center rounded-full text-sm font-bold ${step === 2 ? 'bg-forest text-cream' : 'bg-forest/10 text-forest'}`}>
    2
  </div>
</div>
```

Replace with:
```typescript
{/* Step indicator */}
<div className="mb-8">
  <div className="flex items-center gap-3">
    <div className="flex flex-col items-center gap-1">
      <div className={`flex h-8 w-8 items-center justify-center rounded-full text-sm font-bold ${step === 1 ? 'bg-forest text-cream' : 'bg-forest/10 text-forest'}`}>
        1
      </div>
      <span className="text-label text-forest/60">Kontaktinfo</span>
    </div>
    {/* Connector: filled segment for completed, unfilled for upcoming */}
    <div className="relative h-px flex-1">
      <div className="absolute inset-0 bg-forest/12" />
      <div
        className="absolute inset-y-0 left-0 bg-forest motion-safe:transition-all motion-safe:duration-300"
        style={{ width: step >= 2 ? '100%' : '0%' }}
      />
    </div>
    <div className="flex flex-col items-center gap-1">
      <div className={`flex h-8 w-8 items-center justify-center rounded-full text-sm font-bold ${step === 2 ? 'bg-forest text-cream' : 'bg-forest/10 text-forest'}`}>
        2
      </div>
      <span className="text-label text-forest/60">Betaling</span>
    </div>
  </div>
</div>
```

The connector fills from 0% to 100% width when step advances to 2. The `motion-safe:transition-all motion-safe:duration-300` animates the fill.
  </action>
  <verify>
    <automated>grep -n "useScrollReveal\|isVisible\|translate-y-4\|prefers-reduced" /home/william/Documents/Rootsnew/src/hooks/useScrollReveal.ts && grep -n "ScrollReveal\|motion-safe:opacity-0" /home/william/Documents/Rootsnew/src/components/ui/ScrollReveal.tsx && grep -n "Kontaktinfo\|Betaling\|width: step" /home/william/Documents/Rootsnew/src/components/checkout/CheckoutForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useScrollReveal.ts` exists with IntersectionObserver, threshold 0.15, prefers-reduced-motion bypass
    - `src/components/ui/ScrollReveal.tsx` exists with `motion-safe:opacity-0 motion-safe:translate-y-4` initial state
    - ScrollReveal children are visible immediately when prefers-reduced-motion: reduce
    - CheckoutForm step indicator has "Kontaktinfo" and "Betaling" labels below circles
    - CheckoutForm connector line fills `bg-forest` when step >= 2
    - TypeScript compiles: `cd /home/william/Documents/Rootsnew && npx tsc --noEmit 2>&1 | head -20`
  </acceptance_criteria>
  <done>ScrollReveal hook and component created with motion guard. CheckoutForm has step labels and animated connector fill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IntersectionObserver | Browser API, reads element position only — no user data |
| CheckoutForm step display | Client-side state only — no server boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Denial of Service | IntersectionObserver in ScrollReveal | mitigate | `observer.disconnect()` on cleanup prevents observer accumulation. `once: true` default prevents repeated observations |
| T-11-11 | Tampering | ScrollReveal prefers-reduced-motion check | accept | `window.matchMedia` is read-only — no user-supplied values. Motion guard correctly bypasses animation |
</threat_model>

<verification>
1. `cat /home/william/Documents/Rootsnew/src/hooks/useScrollReveal.ts` — confirms hook with motion guard
2. `grep "bg-card" /home/william/Documents/Rootsnew/src/components/layout/Footer.tsx` — confirms footer background
3. `grep "Kontaktinfo" /home/william/Documents/Rootsnew/src/components/checkout/CheckoutForm.tsx` — confirms step label
4. `cd /home/william/Documents/Rootsnew && npx tsc --noEmit 2>&1 | head -20` — no TS errors
</verification>

<success_criteria>
- Footer: bg-card, text-body brand, flex newsletter layout, hover:text-forest only, text-forest/40 copyright
- useScrollReveal hook: IntersectionObserver, threshold 0.15, prefers-reduced-motion bypass, once:true cleanup
- ScrollReveal component: fade-up from translate-y-4 opacity-0, all classes motion-safe prefixed
- CheckoutForm: step labels Kontaktinfo/Betaling, animated connector fill
- TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-kvalitet-og-premium-design/11-06-SUMMARY.md`
</output>
