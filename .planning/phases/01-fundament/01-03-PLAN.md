---
phase: 01-fundament
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/layout/SkipLink.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/MegaMenuNav.tsx
  - src/components/layout/MobileNav.tsx
  - src/components/layout/Footer.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/navigation.ts
autonomous: true
requirements: [FOUND-06, FOUND-07, WCAG-03, WCAG-06]

must_haves:
  truths:
    - "Skip link is the first focusable element in the DOM and links to #main-content"
    - "Header is sticky at the top of the viewport with logo, nav links, and auth trigger"
    - "Mega-menu dropdown opens on hover (desktop) and keyboard Enter/Space, closes on Escape"
    - "Mobile nav hamburger opens full-screen forest-green overlay with cream text"
    - "Footer has 3-column grid on desktop, single column on mobile, forest background"
    - "All navigation text is in Norwegian (Produkter, Opplevelser, Blogg, Om oss, Kontakt)"
    - "Page is usable at 375px mobile width"
    - "Semantic HTML used throughout (nav, main, header, footer, button)"
  artifacts:
    - path: "src/components/layout/SkipLink.tsx"
      provides: "Skip-to-content link for WCAG-06"
      exports: ["SkipLink"]
    - path: "src/components/layout/Header.tsx"
      provides: "Sticky header with logo, nav, auth trigger, mobile hamburger"
      exports: ["Header"]
    - path: "src/components/layout/MegaMenuNav.tsx"
      provides: "Desktop mega-menu with category dropdowns"
      exports: ["MegaMenuNav"]
    - path: "src/components/layout/MobileNav.tsx"
      provides: "Full-screen mobile navigation overlay"
      exports: ["MobileNav"]
    - path: "src/components/layout/Footer.tsx"
      provides: "3-column footer with contact info"
      exports: ["Footer"]
    - path: "src/lib/navigation.ts"
      provides: "Navigation data structure (links, categories) in Norwegian"
      exports: ["mainNavItems", "footerLinks"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/SkipLink.tsx"
      via: "SkipLink rendered as first child of body"
      pattern: "<SkipLink"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Header.tsx"
      via: "Header rendered before main content"
      pattern: "<Header"
    - from: "src/components/layout/SkipLink.tsx"
      to: "src/app/layout.tsx"
      via: "href='#main-content' targets <main id='main-content'>"
      pattern: "main-content"
---

<objective>
Build the complete layout shell: skip link, sticky header with mega-menu navigation, mobile hamburger overlay, and informative footer. All in Norwegian. Responsive from 375px mobile to desktop.

Purpose: The layout shell wraps every page. Getting the header, navigation, footer, skip link, and responsive behavior right here means every future page automatically inherits correct structure, WCAG compliance, and Norwegian UI copy.
Output: Five layout components wired into the root layout, with a centralized navigation data structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundament/01-CONTEXT.md
@.planning/phases/01-fundament/01-UI-SPEC.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create navigation data, SkipLink, Header, and Footer</name>
  <files>
    src/lib/navigation.ts,
    src/components/layout/SkipLink.tsx,
    src/components/layout/Header.tsx,
    src/components/layout/Footer.tsx,
    src/app/layout.tsx
  </files>
  <read_first>
    src/app/layout.tsx (current state from Plan 01/02 -- font setup, lang="nb"),
    .planning/phases/01-fundament/01-UI-SPEC.md (Layout Specifications section, Header section, Footer section, Copywriting Contract section),
    .planning/phases/01-fundament/01-CONTEXT.md (D-14: sticky header, D-15: mega-menu, D-16: mobile hamburger, D-17: footer columns),
    .planning/research/ARCHITECTURE.md (Route Structure section for navigation URLs)
  </read_first>
  <action>
    1. Create `src/lib/navigation.ts` -- centralized navigation data in Norwegian. This is the single source of truth for all nav links:

       ```typescript
       export interface NavItem {
         label: string
         href: string
         children?: NavCategory[]
       }

       export interface NavCategory {
         label: string
         href: string
         description?: string
       }

       export const mainNavItems: NavItem[] = [
         {
           label: 'Produkter',
           href: '/produkter',
           children: [
             { label: 'Drikke', href: '/produkter?kategori=drikke', description: 'Tradisjonelle norske drikker' },
             { label: 'Kaffe og te', href: '/produkter?kategori=kaffe-te', description: 'Handplukket kaffe og urtete' },
             { label: 'Naturprodukter', href: '/produkter?kategori=naturprodukter', description: 'Ekte fra norsk natur' },
           ],
         },
         {
           label: 'Opplevelser',
           href: '/opplevelser',
           children: [
             { label: 'Naturretreater', href: '/opplevelser?kategori=retreat', description: 'Ro og fordypning i naturen' },
             { label: 'Kurs', href: '/opplevelser?kategori=kurs', description: 'Laer tradisjoner og handverk' },
             { label: 'Matopplevelser', href: '/opplevelser?kategori=matopplevelse', description: 'Smak norsk matkultur' },
           ],
         },
         { label: 'Blogg', href: '/blogg' },
         { label: 'Om oss', href: '/om-oss' },
         { label: 'Kontakt', href: '/kontakt' },
       ]

       export interface FooterColumn {
         title: string
         links: { label: string; href: string }[]
       }

       export const footerColumns: FooterColumn[] = [
         {
           title: 'Utforsk',
           links: [
             { label: 'Produkter', href: '/produkter' },
             { label: 'Opplevelser', href: '/opplevelser' },
             { label: 'Blogg', href: '/blogg' },
             { label: 'Om oss', href: '/om-oss' },
           ],
         },
         {
           title: 'Kundeservice',
           links: [
             { label: 'Kontakt oss', href: '/kontakt' },
             { label: 'Vanlige sporsmal', href: '/kontakt#faq' },
             { label: 'Frakt og retur', href: '/kontakt#frakt' },
           ],
         },
         {
           title: 'Kontakt',
           links: [
             { label: 'post@rootsculture.no', href: 'mailto:post@rootsculture.no' },
           ],
         },
       ]
       ```

    2. Create `src/components/layout/SkipLink.tsx` -- visually hidden until focused, links to #main-content (per WCAG-06 and UI-SPEC):

       ```tsx
       export function SkipLink() {
         return (
           <a
             href="#main-content"
             className="sr-only focus:not-sr-only focus:fixed focus:left-2 focus:top-2 focus:z-[200] focus:rounded-md focus:bg-forest focus:px-4 focus:py-2 focus:text-cream focus:outline-none"
           >
             Hopp til hovedinnhold
           </a>
         )
       }
       ```

       Copy is "Hopp til hovedinnhold" per UI-SPEC Copywriting Contract.

    3. Create `src/components/layout/Header.tsx` -- sticky header, 64px height, card background (#F5F0E8), forest bottom border at 12% opacity:

       ```tsx
       'use client'

       import { useState } from 'react'
       import Link from 'next/link'
       import { Menu } from 'lucide-react'
       import { MegaMenuNav } from './MegaMenuNav'
       import { MobileNav } from './MobileNav'

       export function Header() {
         const [mobileOpen, setMobileOpen] = useState(false)

         return (
           <header
             className="sticky top-0 z-50 flex h-16 items-center border-b border-forest/12 bg-card px-4 lg:px-8"
           >
             {/* Logo -- left */}
             <Link
               href="/"
               className="mr-auto font-heading text-xl font-bold text-forest"
             >
               Roots &amp; Culture
             </Link>

             {/* Desktop nav -- centered, hidden on mobile */}
             <nav className="hidden lg:flex" aria-label="Hovednavigasjon">
               <MegaMenuNav />
             </nav>

             {/* Auth trigger -- right (placeholder until Plan 05 wires AuthModal) */}
             <div className="ml-auto hidden lg:block">
               <button
                 type="button"
                 className="text-[15px] text-forest hover:opacity-85"
                 aria-label="Logg inn"
               >
                 Logg inn
               </button>
             </div>

             {/* Mobile hamburger -- right, visible below lg */}
             <button
               type="button"
               className="ml-auto flex h-11 w-11 items-center justify-center lg:hidden"
               onClick={() => setMobileOpen(true)}
               aria-label="Apne meny"
               aria-expanded={mobileOpen}
             >
               <Menu className="h-6 w-6 text-forest" aria-hidden="true" />
             </button>

             {/* Mobile nav overlay */}
             {mobileOpen && (
               <MobileNav onClose={() => setMobileOpen(false)} />
             )}
           </header>
         )
       }
       ```

       Key: 44px hamburger touch target (h-11 w-11 = 44px), aria-label "Apne meny" per UI-SPEC Copywriting, aria-expanded on hamburger.

    4. Create `src/components/layout/Footer.tsx` -- forest green background, cream text, 3-column grid on desktop, stacked on mobile:

       ```tsx
       import Link from 'next/link'
       import { footerColumns } from '@/lib/navigation'

       export function Footer() {
         return (
           <footer className="dark-surface bg-forest px-4 py-12 text-cream lg:px-8">
             <div className="mx-auto grid max-w-[1200px] gap-8 md:grid-cols-3">
               {footerColumns.map((column) => (
                 <div key={column.title}>
                   <h2 className="mb-4 font-heading text-[20px] font-bold">
                     {column.title}
                   </h2>
                   <ul className="space-y-2">
                     {column.links.map((link) => (
                       <li key={link.href}>
                         <Link
                           href={link.href}
                           className="text-[15px] text-cream/80 hover:text-cream hover:underline"
                         >
                           {link.label}
                         </Link>
                       </li>
                     ))}
                   </ul>
                 </div>
               ))}
             </div>
             <div className="mx-auto mt-8 max-w-[1200px] border-t border-cream/20 pt-8 text-center text-[13px] text-cream/60">
               &copy; {new Date().getFullYear()} Roots &amp; Culture. Alle rettigheter reservert.
             </div>
           </footer>
         )
       }
       ```

       Key: `dark-surface` class on footer for inverted focus rings (cream instead of forest). 48px top padding, 32px bottom padding per UI-SPEC. Footer uses semantic `<footer>` element (WCAG-03).

    5. Update `src/app/layout.tsx` to wire SkipLink, Header, Footer, and add `<main id="main-content">`:

       The layout structure should be:
       ```
       <body>
         <SkipLink />
         <Header />
         <main id="main-content" className="mx-auto max-w-[1200px] px-4 lg:px-8">
           {children}
         </main>
         <Footer />
         <Toaster />   (sonner toast container)
       </body>
       ```

       Import SkipLink, Header, Footer. Import `Toaster` from `sonner` with `role="status"` configuration for accessibility. Add the `main` element with `id="main-content"` as the skip-link target. Preserve the font variables from Plan 02.
  </action>
  <verify>
    <automated>cd /home/william/Documents/Rootsnew && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/navigation.ts contains "export const mainNavItems" and "export const footerColumns"
    - src/lib/navigation.ts contains "Produkter" and "Opplevelser" and "Blogg" and "Om oss" and "Kontakt" (Norwegian)
    - src/lib/navigation.ts contains "Drikke" and "Kaffe og te" and "Naturprodukter" (product categories)
    - src/components/layout/SkipLink.tsx contains 'href="#main-content"' and "Hopp til hovedinnhold"
    - src/components/layout/SkipLink.tsx contains "sr-only" and "focus:not-sr-only"
    - src/components/layout/Header.tsx contains "sticky top-0" and "z-50" and "h-16"
    - src/components/layout/Header.tsx contains 'aria-label="Apne meny"' and 'aria-expanded'
    - src/components/layout/Header.tsx contains "lg:hidden" (hamburger) and "hidden lg:flex" (desktop nav)
    - src/components/layout/Footer.tsx contains "dark-surface" and "bg-forest" and "text-cream"
    - src/components/layout/Footer.tsx contains "md:grid-cols-3"
    - src/app/layout.tsx contains '<SkipLink' and '<Header' and '<Footer'
    - src/app/layout.tsx contains 'id="main-content"'
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>SkipLink, Header, and Footer rendered in root layout. Navigation data centralized in Norwegian. Header is sticky 64px with logo, hamburger on mobile (44px target, aria-label). Footer has 3-column grid on desktop with dark-surface class for inverted focus rings. Skip link targets main-content.</done>
</task>

<task type="auto">
  <name>Task 2: Build MegaMenuNav and MobileNav with full keyboard interaction</name>
  <files>
    src/components/layout/MegaMenuNav.tsx,
    src/components/layout/MobileNav.tsx
  </files>
  <read_first>
    src/components/layout/Header.tsx (how MegaMenuNav and MobileNav are rendered, props expected),
    src/lib/navigation.ts (mainNavItems structure and data),
    .planning/phases/01-fundament/01-UI-SPEC.md (Mega-menu dropdown section, Mobile nav overlay section, Interaction States)
  </read_first>
  <action>
    1. Create `src/components/layout/MegaMenuNav.tsx` -- desktop mega-menu with hover and keyboard support:

       ```tsx
       'use client'

       import { useState, useRef, useEffect } from 'react'
       import Link from 'next/link'
       import { mainNavItems } from '@/lib/navigation'

       export function MegaMenuNav() {
         const [openIndex, setOpenIndex] = useState<number | null>(null)
         const navRef = useRef<HTMLUListElement>(null)

         // Close on Escape
         useEffect(() => {
           function handleKeyDown(e: KeyboardEvent) {
             if (e.key === 'Escape') setOpenIndex(null)
           }
           document.addEventListener('keydown', handleKeyDown)
           return () => document.removeEventListener('keydown', handleKeyDown)
         }, [])

         // Close when clicking outside
         useEffect(() => {
           function handleClickOutside(e: MouseEvent) {
             if (navRef.current && !navRef.current.contains(e.target as Node)) {
               setOpenIndex(null)
             }
           }
           document.addEventListener('mousedown', handleClickOutside)
           return () => document.removeEventListener('mousedown', handleClickOutside)
         }, [])

         return (
           <ul ref={navRef} className="flex items-center gap-1" role="menubar">
             {mainNavItems.map((item, index) => {
               const hasChildren = item.children && item.children.length > 0
               const isOpen = openIndex === index

               return (
                 <li
                   key={item.href}
                   className="relative"
                   role="none"
                   onMouseEnter={() => hasChildren && setOpenIndex(index)}
                   onMouseLeave={() => hasChildren && setOpenIndex(null)}
                 >
                   {hasChildren ? (
                     <button
                       type="button"
                       className="flex items-center gap-1 px-3 py-2 text-[15px] text-forest hover:underline"
                       role="menuitem"
                       aria-haspopup="true"
                       aria-expanded={isOpen}
                       onClick={() => setOpenIndex(isOpen ? null : index)}
                       onKeyDown={(e) => {
                         if (e.key === 'Enter' || e.key === ' ') {
                           e.preventDefault()
                           setOpenIndex(isOpen ? null : index)
                         }
                       }}
                     >
                       {item.label}
                       <svg
                         className={`h-4 w-4 transition-transform ${isOpen ? 'rotate-180' : ''}`}
                         fill="none"
                         viewBox="0 0 24 24"
                         stroke="currentColor"
                         aria-hidden="true"
                       >
                         <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />
                       </svg>
                     </button>
                   ) : (
                     <Link
                       href={item.href}
                       className="block px-3 py-2 text-[15px] text-forest hover:underline"
                       role="menuitem"
                     >
                       {item.label}
                     </Link>
                   )}

                   {/* Dropdown panel */}
                   {hasChildren && isOpen && (
                     <div
                       className="absolute left-0 top-full z-50 mt-0 w-[400px] rounded-b-lg border border-forest/15 bg-cream shadow-[0_8px_24px_rgba(0,0,0,0.08)]"
                       role="menu"
                     >
                       <ul className="grid grid-cols-1 gap-1 p-4">
                         {item.children!.map((child) => (
                           <li key={child.href} role="none">
                             <Link
                               href={child.href}
                               className="block rounded-md px-3 py-3 hover:bg-card"
                               role="menuitem"
                               onClick={() => setOpenIndex(null)}
                             >
                               <span className="block text-[15px] font-medium text-forest">
                                 {child.label}
                               </span>
                               {child.description && (
                                 <span className="block text-[13px] text-bark">
                                   {child.description}
                                 </span>
                               )}
                             </Link>
                           </li>
                         ))}
                       </ul>
                     </div>
                   )}
                 </li>
               )
             })}
           </ul>
         )
       }
       ```

       Key implementation details:
       - Hover opens dropdown (desktop), Enter/Space toggles (keyboard)
       - Escape closes dropdown (global keydown listener)
       - Click outside closes dropdown
       - `aria-haspopup="true"` and `aria-expanded` on trigger buttons
       - `role="menubar"` on top-level list, `role="menuitem"` on items
       - Dropdown: cream background (#FEFCF3), border forest/15, shadow per UI-SPEC
       - Chevron rotates when open

    2. Create `src/components/layout/MobileNav.tsx` -- full-viewport forest overlay with focus trap:

       ```tsx
       'use client'

       import { useEffect, useRef } from 'react'
       import Link from 'next/link'
       import { X } from 'lucide-react'
       import { mainNavItems } from '@/lib/navigation'

       interface MobileNavProps {
         onClose: () => void
       }

       export function MobileNav({ onClose }: MobileNavProps) {
         const closeButtonRef = useRef<HTMLButtonElement>(null)
         const navRef = useRef<HTMLDivElement>(null)

         // Focus close button on mount
         useEffect(() => {
           closeButtonRef.current?.focus()
         }, [])

         // Close on Escape
         useEffect(() => {
           function handleKeyDown(e: KeyboardEvent) {
             if (e.key === 'Escape') onClose()
           }
           document.addEventListener('keydown', handleKeyDown)
           return () => document.removeEventListener('keydown', handleKeyDown)
         }, [onClose])

         // Lock body scroll
         useEffect(() => {
           document.body.style.overflow = 'hidden'
           return () => {
             document.body.style.overflow = ''
           }
         }, [])

         // Focus trap
         useEffect(() => {
           function handleTab(e: KeyboardEvent) {
             if (e.key !== 'Tab' || !navRef.current) return
             const focusable = navRef.current.querySelectorAll<HTMLElement>(
               'a[href], button, [tabindex]:not([tabindex="-1"])'
             )
             if (focusable.length === 0) return
             const first = focusable[0]
             const last = focusable[focusable.length - 1]
             if (e.shiftKey && document.activeElement === first) {
               e.preventDefault()
               last.focus()
             } else if (!e.shiftKey && document.activeElement === last) {
               e.preventDefault()
               first.focus()
             }
           }
           document.addEventListener('keydown', handleTab)
           return () => document.removeEventListener('keydown', handleTab)
         }, [])

         return (
           <div
             ref={navRef}
             className="dark-surface fixed inset-0 z-[100] flex flex-col bg-forest"
             role="dialog"
             aria-modal="true"
             aria-label="Navigasjonsmeny"
           >
             {/* Close button -- top right */}
             <div className="flex h-16 items-center justify-end px-4">
               <button
                 ref={closeButtonRef}
                 type="button"
                 className="flex h-11 w-11 items-center justify-center"
                 onClick={onClose}
                 aria-label="Lukk meny"
               >
                 <X className="h-6 w-6 text-cream" aria-hidden="true" />
               </button>
             </div>

             {/* Navigation links */}
             <nav className="flex-1 px-8 pt-4" aria-label="Mobilnavigasjon">
               <ul className="space-y-2">
                 {mainNavItems.map((item) => (
                   <li key={item.href}>
                     <Link
                       href={item.href}
                       className="block py-3 font-heading text-2xl font-bold text-cream hover:text-cream/80"
                       onClick={onClose}
                     >
                       {item.label}
                     </Link>
                     {item.children && (
                       <ul className="ml-4 space-y-1">
                         {item.children.map((child) => (
                           <li key={child.href}>
                             <Link
                               href={child.href}
                               className="block py-2 text-[15px] text-cream/70 hover:text-cream"
                               onClick={onClose}
                             >
                               {child.label}
                             </Link>
                           </li>
                         ))}
                       </ul>
                     )}
                   </li>
                 ))}
               </ul>
             </nav>

             {/* Login button at bottom */}
             <div className="px-8 pb-8">
               <button
                 type="button"
                 className="w-full rounded-md bg-ember py-3 text-center font-body text-[15px] font-medium text-cream"
                 onClick={onClose}
               >
                 Logg inn
               </button>
             </div>
           </div>
         )
       }
       ```

       Key implementation details:
       - Full viewport: `fixed inset-0 z-[100]` per UI-SPEC
       - Forest background (#1B4332), cream text per UI-SPEC
       - `dark-surface` class for inverted focus rings
       - Links: 24px Merriweather bold (font-heading text-2xl) per UI-SPEC
       - Close button: 44px (h-11 w-11) touch target, `aria-label="Lukk meny"` per UI-SPEC
       - `role="dialog"`, `aria-modal="true"` per accessibility requirements
       - Focus trap: Tab wraps within overlay
       - Escape closes overlay
       - Body scroll locked while open
       - Focus moves to close button on open
  </action>
  <verify>
    <automated>cd /home/william/Documents/Rootsnew && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/MegaMenuNav.tsx contains "export function MegaMenuNav"
    - src/components/layout/MegaMenuNav.tsx contains 'role="menubar"' and 'role="menuitem"'
    - src/components/layout/MegaMenuNav.tsx contains 'aria-haspopup="true"' and "aria-expanded"
    - src/components/layout/MegaMenuNav.tsx contains "Escape" (keyboard close handler)
    - src/components/layout/MegaMenuNav.tsx contains "onMouseEnter" (hover open)
    - src/components/layout/MobileNav.tsx contains "export function MobileNav"
    - src/components/layout/MobileNav.tsx contains "fixed inset-0 z-[100]" and "bg-forest"
    - src/components/layout/MobileNav.tsx contains 'aria-label="Lukk meny"' and 'aria-modal="true"'
    - src/components/layout/MobileNav.tsx contains "dark-surface"
    - src/components/layout/MobileNav.tsx contains "overflow" (body scroll lock)
    - src/components/layout/MobileNav.tsx contains "Tab" or "handleTab" (focus trap)
    - src/components/layout/MobileNav.tsx contains "h-11 w-11" (44px close button target)
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>MegaMenuNav opens on hover/click with keyboard support (Enter/Space/Escape/Arrow), aria-haspopup and aria-expanded. MobileNav is a full-screen forest overlay with cream text, focus trap, body scroll lock, Escape close, and 44px touch targets. Both use centralized Norwegian navigation data.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` passes
- Layout.tsx renders SkipLink -> Header -> main#main-content -> Footer
- SkipLink contains "Hopp til hovedinnhold" and targets #main-content
- Header is sticky with hamburger visible only on mobile (lg:hidden)
- Footer has dark-surface class and 3-column grid
- MegaMenuNav has aria-haspopup, aria-expanded, Escape handler
- MobileNav has role="dialog", aria-modal, focus trap, body scroll lock
</verification>

<success_criteria>
- Skip link is first focusable element, hidden until focused, links to #main-content
- Header sticks to top, shows logo/nav/auth on desktop, hamburger on mobile
- Mega-menu opens on hover (desktop) and Enter/Space (keyboard), closes on Escape
- Mobile nav is full-screen forest overlay with cream text, closes on Escape/X button
- Footer is 3-column on desktop, stacked on mobile, forest background with cream text
- All text is in Norwegian
- All interactive elements have 44px minimum touch targets
- Semantic HTML throughout (header, nav, main, footer, button)
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundament/01-03-SUMMARY.md`
</output>
