---
phase: 19-cms-medium-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/produkter/page.tsx
  - src/app/admin/opplevelser/page.tsx
  - src/app/admin/artikler/page.tsx
  - src/app/admin/gavekort/page.tsx
  - src/app/admin/kunder/page.tsx
  - src/app/admin/ordrer/page.tsx
  - src/app/admin/ordrer/[id]/page.tsx
  - src/app/admin/bookinger/page.tsx
  - src/app/admin/innhold/page.tsx
  - src/app/admin/innhold/[pageId]/page.tsx
  - src/app/admin/kunder/[uid]/page.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - CMS-MED-ERRORHANDLING
  - CMS-MED-COLORS

must_haves:
  truths:
    - "Every useEffect fetch in admin pages has a .catch() handler that shows a Norwegian error toast"
    - "No hardcoded hex color classes (text-[#...] or bg-[#...]) exist in any admin page file"
    - "Admin list pages use Next.js Image instead of native img for thumbnails"
    - "Design tokens for refund/danger and status badge colors exist in globals.css"
  artifacts:
    - path: "src/app/globals.css"
      provides: "New design tokens for danger/refund colors"
      contains: "--color-refund"
    - path: "src/app/admin/ordrer/[id]/page.tsx"
      provides: "Order detail using design tokens instead of hex colors"
  key_links:
    - from: "src/app/admin/ordrer/[id]/page.tsx"
      to: "src/app/globals.css"
      via: "Tailwind CSS custom properties"
      pattern: "text-refund|bg-badge"
---

<objective>
Add error handling to all admin useEffect fetches and replace all hardcoded hex colors with design tokens.

Purpose: Five admin list pages silently swallow fetch failures (no .catch()). Users see a permanent loading spinner with no feedback. The order detail and booking pages use hardcoded hex colors (#C0392B, #DCFCE7, etc.) that violate the design system. Additionally, three admin list pages use native `<img>` instead of Next.js `<Image>`.

Output: All admin useEffect calls have .catch() with Norwegian toast error messages. All hex colors replaced with CSS custom properties. Admin list thumbnails use Next.js Image.
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v1.1-ROADMAP.md
@src/app/globals.css
@src/app/admin/produkter/page.tsx
@src/app/admin/opplevelser/page.tsx
@src/app/admin/artikler/page.tsx
@src/app/admin/ordrer/[id]/page.tsx
@src/app/admin/bookinger/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add design tokens and fix hardcoded hex colors in admin pages</name>
  <files>
    src/app/globals.css,
    src/app/admin/ordrer/[id]/page.tsx,
    src/app/admin/bookinger/page.tsx
  </files>
  <read_first>
    src/app/globals.css,
    src/app/admin/ordrer/[id]/page.tsx,
    src/app/admin/bookinger/page.tsx
  </read_first>
  <action>
1. Add new design tokens to `src/app/globals.css` inside the existing `:root` block, after the `/* Badge status colors */` section (around line 61, before the closing `}`):

```css
  /* Refund/danger status colors */
  --color-refund: #C0392B;
  --color-refund-bg: #FDECEA;
```

These map the currently hardcoded `#C0392B` to a named token. The existing `--color-success: #166534` and `--color-success-bg: #DCFCE7` already cover the green badge. The existing `--color-badge-warning: #92400E` and `--color-badge-warning-bg: #FEF3C7` already cover the amber badge.

2. In `src/app/admin/ordrer/[id]/page.tsx`, replace ALL hardcoded hex color classes:
  - `text-[#C0392B]` -> `text-refund` (3 occurrences)
  - `border-[#C0392B]` -> `border-refund`
  - `hover:bg-[#C0392B]/5` -> `hover:bg-refund/5`
  - `bg-[#DCFCE7] text-[#166534]` -> `bg-success-bg text-success` (refund status "succeeded" badge)
  - `bg-[#FEF3C7] text-[#92400E]` -> `bg-badge-warning-bg text-badge-warning` (refund status "pending" badge)

To make these Tailwind classes work with CSS custom properties, verify the `@theme` block in globals.css includes these colors. If Tailwind v4 auto-resolves `--color-*` variables as utility classes (it does -- any `--color-X` becomes `text-X`, `bg-X`, `border-X`), then the above replacements will work directly.

3. In `src/app/admin/bookinger/page.tsx`, replace:
  - `hover:text-[#C0392B]` -> `hover:text-refund`
  </action>
  <verify>
    <automated>cd /home/william/Documents/Rootsnew && grep -rn "text-\[#\|bg-\[#\|border-\[#\|hover:text-\[#\|hover:bg-\[#" src/app/admin/ordrer/[id]/page.tsx src/app/admin/bookinger/page.tsx && echo "FAIL: hex colors still present" || echo "PASS: no hardcoded hex colors"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "text-[#" or "bg-[#" or "border-[#" in src/app/admin/ordrer/[id]/page.tsx returns NO matches
    - grep for "text-[#" or "bg-[#" in src/app/admin/bookinger/page.tsx returns NO matches
    - grep "--color-refund" src/app/globals.css returns a match
    - grep "--color-refund-bg" src/app/globals.css returns a match
  </acceptance_criteria>
  <done>All hardcoded hex colors in admin order detail and bookings pages replaced with design tokens. New --color-refund and --color-refund-bg tokens defined in globals.css.</done>
</task>

<task type="auto">
  <name>Task 2: Add .catch() error handling to all admin useEffect fetches + replace img with Image</name>
  <files>
    src/app/admin/produkter/page.tsx,
    src/app/admin/opplevelser/page.tsx,
    src/app/admin/artikler/page.tsx,
    src/app/admin/gavekort/page.tsx,
    src/app/admin/kunder/page.tsx,
    src/app/admin/ordrer/page.tsx,
    src/app/admin/ordrer/[id]/page.tsx,
    src/app/admin/bookinger/page.tsx,
    src/app/admin/innhold/page.tsx,
    src/app/admin/innhold/[pageId]/page.tsx,
    src/app/admin/kunder/[uid]/page.tsx
  </files>
  <read_first>
    src/app/admin/produkter/page.tsx,
    src/app/admin/opplevelser/page.tsx,
    src/app/admin/artikler/page.tsx,
    src/app/admin/gavekort/page.tsx,
    src/app/admin/kunder/page.tsx,
    src/app/admin/ordrer/page.tsx,
    src/app/admin/ordrer/[id]/page.tsx,
    src/app/admin/bookinger/page.tsx,
    src/app/admin/innhold/page.tsx,
    src/app/admin/innhold/[pageId]/page.tsx,
    src/app/admin/kunder/[uid]/page.tsx
  </read_first>
  <action>
For EACH admin page with a useEffect fetch, add a `.catch()` handler with a Norwegian error toast. Import `toast` from `sonner` if not already imported. The pattern is:

```typescript
// BEFORE:
getAllProducts().then(setProducts)

// AFTER:
getAllProducts()
  .then(setProducts)
  .catch(() => toast.error('Kunne ikke laste produkter.'))
```

Here are ALL the specific changes needed:

**src/app/admin/produkter/page.tsx** (line ~27):
```
getAllProducts().then(setProducts).catch(() => toast.error('Kunne ikke laste produkter.'))
```
Also add `import { toast } from 'sonner'` if not present.
Also replace `<img` tag (line ~50) with `<Image` from `next/image` with width={48} height={48} and `className="h-12 w-12 rounded object-cover"`. Add `import Image from 'next/image'`.

**src/app/admin/opplevelser/page.tsx** (line ~20):
```
getAllExperiences().then(setExperiences).catch(() => toast.error('Kunne ikke laste opplevelser.'))
```
Also replace `<img` with `<Image` (same pattern: width={48} height={48}).

**src/app/admin/artikler/page.tsx** (line ~21):
```
getAllArticles().then(setArticles).catch(() => toast.error('Kunne ikke laste artikler.'))
```
Also replace `<img` with `<Image` (same pattern: width={48} height={48}).

**src/app/admin/gavekort/page.tsx** (line ~29):
```
getGiftCardsAdmin().then((cards) => {
  setGiftCards(cards)
  setLoading(false)
}).catch(() => {
  setLoading(false)
  toast.error('Kunne ikke laste gavekort.')
})
```

**src/app/admin/kunder/page.tsx** (line ~19):
```
getCustomerList().then(setCustomers).catch(() => toast.error('Kunne ikke laste kunder.'))
```

**src/app/admin/ordrer/page.tsx** (line ~21):
```
getOrders().then((data) => {
  setOrders(data)
  setLoading(false)
}).catch(() => {
  setLoading(false)
  toast.error('Kunne ikke laste ordrer.')
})
```

**src/app/admin/bookinger/page.tsx** (line ~24):
```
getBookingsFiltered().then(setBookings).catch(() => toast.error('Kunne ikke laste bookinger.'))
```

**src/app/admin/ordrer/[id]/page.tsx** -- multiple useEffect calls:
- line ~61: `getOrderById(params.id).then(...)` -- wrap the entire chain in `.catch(() => toast.error('Kunne ikke laste ordredetaljer.'))`
- line ~70: `getRefundsForOrder(...)` -- add `.catch(() => toast.error('Kunne ikke laste refusjoner.'))`
- line ~74: `getOrderNotes(params.id)` -- add `.catch(() => toast.error('Kunne ikke laste notater.'))`
- line ~115: `getOrderNotes(order.id)` (after adding note) -- add `.catch(() => {})`  (silent -- the note was already added, this is just a refresh)
- line ~125: `getRefundsForOrder(...)` (after refund) -- add `.catch(() => {})`
- line ~128: `getOrderById(order.id)` (refresh after refund) -- add `.catch(() => {})`

**src/app/admin/innhold/page.tsx** (line ~20):
The fetch('/api/page-content') chain. Add `.catch(() => { setLoading(false); toast.error('Kunne ikke laste sider.') })` after the existing `.then()`.

**src/app/admin/innhold/[pageId]/page.tsx** (line ~377):
The fetch(`/api/page-content/${pageId}`) chain. Add `.catch(() => toast.error('Kunne ikke laste sideinnhold.'))` after the existing `.then()`.

**src/app/admin/kunder/[uid]/page.tsx** (line ~22):
`getCustomerDetail(params.uid).then(...)` -- add `.catch(() => toast.error('Kunne ikke laste kundedetaljer.'))`

For the 3 `<img>` to `<Image>` replacements (produkter, opplevelser, artikler list pages):
- Add `import Image from 'next/image'` at the top.
- Replace `<img src={...} alt={...} className="h-12 w-12 rounded object-cover" />` with `<Image src={...} alt={...} width={48} height={48} className="h-12 w-12 rounded object-cover" />`.
- Keep the existing alt text and className.
  </action>
  <verify>
    <automated>cd /home/william/Documents/Rootsnew && echo "=== Checking .catch() presence ===" && for f in src/app/admin/produkter/page.tsx src/app/admin/opplevelser/page.tsx src/app/admin/artikler/page.tsx src/app/admin/gavekort/page.tsx src/app/admin/kunder/page.tsx src/app/admin/ordrer/page.tsx src/app/admin/bookinger/page.tsx src/app/admin/innhold/page.tsx; do echo -n "$f: "; grep -c "\.catch(" "$f"; done && echo "=== Checking no bare <img in lists ===" && grep -l "<img " src/app/admin/produkter/page.tsx src/app/admin/opplevelser/page.tsx src/app/admin/artikler/page.tsx 2>/dev/null && echo "FAIL: img tags still present" || echo "PASS: no img tags"</automated>
  </verify>
  <acceptance_criteria>
    - Every admin list page (produkter, opplevelser, artikler, gavekort, kunder, ordrer, bookinger, innhold) has at least one .catch( call
    - src/app/admin/ordrer/[id]/page.tsx has at least 3 .catch( calls (for the initial loads)
    - src/app/admin/innhold/[pageId]/page.tsx has at least 1 .catch( call
    - src/app/admin/kunder/[uid]/page.tsx has at least 1 .catch( call
    - grep "<img " in the three list pages (produkter, opplevelser, artikler) returns NO matches
    - Each .catch() callback uses toast.error with a Norwegian message (not English)
  </acceptance_criteria>
  <done>All 11 admin pages have error handling on useEffect fetches with Norwegian error toasts. Three list pages use Next.js Image instead of native img.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Changes are UI-only: error display and styling. No new data flows or trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | I (Info Disclosure) | toast.error messages | accept | Error messages are generic Norwegian text ("Kunne ikke laste..."), no stack traces or internal details exposed |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. No hardcoded hex colors in admin pages (grep confirms)
3. All admin useEffect fetches have .catch() handlers
4. `npm run build` succeeds
</verification>

<success_criteria>
- Every admin page useEffect fetch has .catch() with Norwegian toast
- Zero hardcoded hex colors in admin pages
- Three list pages use Next.js Image
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/19-cms-medium-fixes/19-02-SUMMARY.md`
</output>
