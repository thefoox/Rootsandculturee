---
phase: 15-redesign-hovedsider
plan: 03
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/app/(public)/kontakt/page.tsx
  - src/app/(public)/kontakt/ContactForm.tsx
  - src/app/(public)/om-oss/page.tsx
  - src/lib/data/mock-data.ts
autonomous: true
requirements: [RD-02, RD-03]

must_haves:
  truths:
    - "Kontakt page matches kontakt-v2.html: compact hero → overlapping contact cards → form section → FAQ grid → location"
    - "Om-oss page matches om-oss.html: centered hero → overlapping story → values → team photo-strip → location dark → CTA"
    - "Both pages use SectionRenderer for CMS sections"
    - "ContactForm component upgraded to match prototype styling"
    - "Mock data for kontakt and om-oss includes all sections with correct content"
  artifacts: []
  key_links: []
---

<objective>
Rebuild kontakt and om-oss pages to match their HTML prototypes. Uses upgraded section components from Plan 01.

Purpose: Apply the new visual designs from kontakt-v2.html and om-oss.html to the live pages.
Output: Two pages matching their respective prototypes with CMS sections + page-level components.
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-redesign-hovedsider/15-01-PLAN.md

Reference prototypes (READ for exact layout):
- public/playground/layout-alternatives/kontakt-v2.html
- public/playground/layout-alternatives/om-oss.html
</context>

<tasks>

<task id="1" type="code" effort="L">
**Rebuild kontakt page to match kontakt-v2.html**

Layout (top to bottom):

1. **Hero (CMS, compact variant)** — Forest bg, `padding: 160px 0 80px`, left-aligned text, max-width 640px. h1: "Ta kontakt", p: "Har du sporsmaal om opplevelser, produkter eller samarbeid? Vi svarer gjerne."
2. **Contact cards (CMS, contact-info)** — 3 cards overlapping hero (margin-top -48px, z-2). E-post (24h svar), Telefon (Man-Fre 09-17), Besok oss (Etter avtale). White cards with icon, title, subtitle, value link.
3. **Form section (page-level)** — 2-column grid: left intro (h2 + p + business note card) + right ContactForm. The form has: 2-column name/email row, subject select dropdown, message textarea, submit button.
4. **FAQ (CMS, faq)** — 2-column card grid. 6 cards with question + answer.
5. **Location (CMS, location)** — Cream bg. Grid: left (h2 + p + location details with icons) + right (map placeholder).

**Page structure:**
```tsx
const page = await getPageContent('kontakt')
const sorted = page.sections.sort(...)
// Render CMS sections via SectionRenderer
// Insert ContactForm section between contact-info and faq
```

The hero must be compact variant (no CTA buttons, forest bg, centered or left-aligned).
The contact-info section must use the new overlapping card style.

**Verification:** Kontakt page matches prototype layout. `npm run build` passes.
</task>

<task id="2" type="code" effort="M">
**Upgrade ContactForm to match prototype styling**

Current `src/app/(public)/kontakt/ContactForm.tsx` needs to match kontakt-v2.html form style:

- Form wrapper: card bg, rounded-2xl, padding 40px
- 2-column row for name + email inputs
- Subject: select dropdown (Velg emne, Opplevelser, Produkter, Bedrift, Samarbeid, Annet)
- Message: textarea, min-height 120px
- Submit button: full-width, forest bg, cream text, rounded-[10px], font-weight 600
- Input styling: cream bg, border forest/12, rounded-[10px], padding 14px 16px, focus: forest border + shadow ring
- Labels: 0.8125rem, font-weight 500, forest color

**Verification:** Form renders with prototype styling. `npm run build` passes.
</task>

<task id="3" type="code" effort="L">
**Rebuild om-oss page to match om-oss.html**

Layout (top to bottom):

1. **Hero (CMS, compact variant)** — 70vh, centered text, forest bg, optional bg image at 0.3 opacity. h1: "Om Roots & Culture", p: "Forankret i norsk natur og kulturarv. Vi bringer deg naermere det ekte."
2. **Story (CMS, text-image)** — Overlapping layout: left image (4/5 aspect, rounded right side, shadow) + right text box (card bg, padding 64px, rounded-3xl, z-2 overlaps image). h2: "Var historie", 3 paragraphs, signature line.
3. **Values (CMS, values)** — Section header "Hva vi star for" + subheading + 3 cards. Autentisitet (layers icon), Baerekraft (shield icon), Fellesskap (heart icon).
4. **Team (CMS, team)** — h2: "Menneskene bak" + 3 photo-strip cards (3/4 aspect photos, name, role).
5. **Location (CMS, location dark variant)** — Forest bg, cream text. Grid: left (h2 + p + details: map-pin, mail, phone) + right (map placeholder).
6. **CTA (CMS, cta)** — Card bg, centered. h2: "Klar for en opplevelse?", p, button.

**Page structure:**
```tsx
const page = await getPageContent('om-oss')
const sorted = page.sections.sort(...)
// Render all sections via SectionRenderer
// No page-level components needed — all sections are CMS types
```

**Verification:** Om-oss page matches prototype layout. `npm run build` passes.
</task>

<task id="4" type="code" effort="M">
**Update kontakt and om-oss mock data**

Update `src/lib/data/mock-data.ts` for both pages:

**Kontakt mock data sections:**
0. hero (compact variant, heading: "Ta kontakt", subheading from prototype)
1. contact-info (3 items: e-post with href mailto:, telefon with href tel:, besok with address)
2. faq (6 items from kontakt-v2.html)
3. location (light variant, Holtan Gard address, opening hours)

**Om-oss mock data sections:**
0. hero (compact variant, heading: "Om Roots & Culture", subheading, bg image)
1. text-image (story: heading "Var historie", 3 paragraphs + signature, image)
2. values (heading: "Hva vi star for", subheading: "Tre verdier som driver alt vi gjor", 3 items: Autentisitet/layers, Baerekraft/shield, Fellesskap/heart)
3. team (heading: "Menneskene bak", items from prototype)
4. location (dark variant, heading: "Hvor du finner oss", details)
5. cta (heading: "Klar for en opplevelse?", subheading, ctaText: "Se opplevelser", ctaLink: "/opplevelser")

Ensure the `PageSection` type supports the `variant` field if used for hero/location variants.

**Verification:** Mock data renders correctly on both pages. `npm run build` passes.
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contact form | Client-side form — no server action yet, just UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | N/A | Layout rebuild | accept | No new trust boundaries, using existing data layer |
</threat_model>

<verification>
After all tasks:
1. `npm run build` — must pass
2. Kontakt page section order matches prototype
3. Om-oss page section order matches prototype
4. ContactForm has prototype styling
5. Mock data for both pages has correct content
</verification>

<success_criteria>
- Kontakt page matches kontakt-v2.html layout
- Om-oss page matches om-oss.html layout
- ContactForm styled per prototype
- Mock data updated for both pages
- All sections render via SectionRenderer
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-redesign-hovedsider/15-03-SUMMARY.md`
</output>
