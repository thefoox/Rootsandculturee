---
phase: 15-redesign-hovedsider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/HeroSection.tsx
  - src/components/sections/TrustBarSection.tsx
  - src/components/sections/ExperiencesGridSection.tsx
  - src/components/sections/ProductsGridSection.tsx
  - src/components/sections/ArticlesGridSection.tsx
  - src/components/sections/FaqSection.tsx
  - src/components/sections/ContactInfoSection.tsx
  - src/components/sections/TeamSection.tsx
  - src/components/sections/ValuesSection.tsx
  - src/components/sections/TextImageSection.tsx
  - src/components/sections/LocationSection.tsx
  - src/components/sections/SectionRenderer.tsx
  - src/types/index.ts
autonomous: true
requirements: [RD-01, RD-02, RD-03]

must_haves:
  truths:
    - "HeroSection supports two variants: fullscreen (bottom-aligned text, dual CTA buttons, scroll indicator) and compact (centered text, dark bg, no image)"
    - "TrustBarSection renders as dark inline bar with icon+label items"
    - "ExperiencesGridSection shows detailed cards with category badge, date, location, price, and description"
    - "FaqSection renders as 2-column card grid (not accordion)"
    - "ContactInfoSection renders as overlapping cards that float above previous section"
    - "TeamSection renders as photo-strip with 3/4 aspect ratio photos"
    - "LocationSection exists with dark bg variant, location details, and map placeholder"
    - "SectionRenderer handles the new 'location' section type"
  artifacts: []
  key_links: []
---

<objective>
Upgrade all section components to match the visual designs from the HTML prototypes. Each component must support the new layout patterns while remaining CMS-driven (data from PageSection props).

Purpose: The existing section components have basic layouts that don't match the premium designs in forside-v4.html, kontakt-v2.html, and om-oss.html. These must be upgraded before the pages can be rebuilt.
Output: Upgraded section components that match the HTML prototypes pixel-for-pixel in Tailwind CSS.
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

Reference HTML prototypes (READ these for exact design specs):
- public/playground/layout-alternatives/forside-v4.html
- public/playground/layout-alternatives/kontakt-v2.html
- public/playground/layout-alternatives/om-oss.html
</context>

<tasks>

<task id="1" type="code" effort="M">
**Upgrade HeroSection to support two variants**

Current: Single fullscreen variant with bottom-left text.
Needed: Two variants based on prototype designs.

**Variant 1 — Fullscreen (forside-v4.html):**
- `min-height: 100vh`, content at bottom (`align-items: flex-end`)
- Gradient overlay: `linear-gradient(180deg, rgba(27,67,50,0.2) 0%, rgba(27,67,50,0.5) 60%, rgba(27,67,50,0.75) 100%)`
- h1 clamp font: `clamp(3rem, 2rem + 3.5vw, 5rem)`, max-width 700px
- Sub text: cream/85, max-width 520px
- Two CTA buttons: primary (forest bg, cream text, rounded-[10px]) + ghost (border cream/50, backdrop-blur)
- Scroll indicator arrow at bottom center, bouncing animation
- Use `section.ctaText` + `section.ctaLink` for primary CTA, add `ctaSecondaryText` + `ctaSecondaryLink` for ghost button (or use items array)

**Variant 2 — Compact (kontakt-v2.html, om-oss.html):**
- `min-height: 70vh` (om-oss) or `padding: 160px 0 80px` (kontakt)
- Centered text, forest background
- Optional background image at low opacity (0.3)
- h1 clamp: `clamp(2.25rem, 1.8rem + 1.5vw, 3rem)`, centered
- Subheading: Merriweather italic, cream/80

**Variant selection:** Use `section.items` or a field to determine variant. If section has CTA buttons → fullscreen. If no CTA → compact. OR use a `variant` field in the section data. The simplest approach: if `section.ctaText` exists → fullscreen variant, else → compact variant.

**Verification:** Both variants render correctly. `npm run build` passes.
</task>

<task id="2" type="code" effort="S">
**Upgrade TrustBarSection to dark inline style**

Current: Light or dark variant with 3 items in grid.
Needed (forside-v4.html): Dark forest background, centered row with icon+label, smaller text (0.8125rem), items separated by gaps not dividers.

Update to match: `background: var(--forest)`, `padding: 28px 0`, flex row centered, gap 40px, items are `flex items-center gap-2`, cream/65 text, 0.8125rem, icons 16px.

Remove the `variant` prop — always render dark style. The trust bar from the prototype is always dark.

**Verification:** Trust bar renders as dark inline row. `npm run build` passes.
</task>

<task id="3" type="code" effort="L">
**Upgrade ExperiencesGridSection with detailed cards**

Current: 3-column grid with overlay image cards (image fills entire card, text at bottom).
Needed (forside-v4.html): Detailed cards with separate image area and body:
- Card: rounded-2xl, border, hover translateY(-6px) + shadow
- Image: aspect-ratio 16/11, overflow hidden, zoom on hover
- Body: padding 24px
- Category badge: colored pill (retreat=green, kurs=yellow, mat=red)
- Title: Merriweather 1.1875rem bold, 2-line clamp
- Date: icon + text, 0.8125rem, bark color
- Location: 0.8125rem, body/50
- Description: 0.8125rem, 2-line clamp, opacity 0.65
- Footer: border-top, price left (1.1875rem bold), "Les mer →" right
- Section header: centered h2 + subtitle
- Filter tabs above grid (optional — depends on FilterableExperienceGrid reuse)

The component fetches experiences from `getExperiences()` + `getExperienceDates()`. Keep this data fetching. Update the card rendering.

For the category badge, map experience.category to badge colors:
- 'retreat' → green (bg-[#DCFCE7] text-[#166534])
- 'kurs' → yellow (bg-[#FEF3C7] text-[#92400E])
- 'matopplevelse' → red (bg-[#FEE2E2] text-[#991B1B])

**Verification:** Experience cards show badge, date, location, description, price. `npm run build` passes.
</task>

<task id="4" type="code" effort="S">
**Upgrade FaqSection to 2-column card grid**

Current: Accordion-style (details/summary) in single column, max-width 800px.
Needed (kontakt-v2.html): 2-column card grid, max-width 960px:
- Each FAQ is a card: cream bg, rounded-[14px], padding 24px, border forest/4
- Hover: shadow
- h4: 0.9375rem, line-height 1.3
- p: 0.8125rem, line-height 1.6, opacity 0.7
- Section bg: card color
- Header: centered h2

Replace the accordion with flat cards. No expand/collapse — both question and answer always visible.

**Verification:** FAQ renders as 2-column card grid. `npm run build` passes.
</task>

<task id="5" type="code" effort="M">
**Upgrade ContactInfoSection to overlapping cards**

Current: Vertical list of icon+label+description items.
Needed (kontakt-v2.html): 3-column card grid that overlaps the hero:
- Cards: white bg, rounded-2xl, padding 32px, text-center, shadow
- Hover: translateY(-4px) + bigger shadow
- Icon: 56x56 rounded-[14px] bg forest/6, centered, 24px icon
- h3: 1.0625rem
- p: 0.875rem, opacity 0.6
- Value link: font-weight 600, forest color, hover underline
- Container: `margin-top: -48px`, relative z-2 (overlaps hero)

The overlap effect is the key design element. Use negative margin-top and relative z-index.

**Verification:** Contact cards overlap the hero section. `npm run build` passes.
</task>

<task id="6" type="code" effort="M">
**Upgrade TeamSection to photo-strip layout**

Current: Grid with small 192px circle/rounded photos, centered text.
Needed (om-oss.html): Photo-strip with large photos:
- 3-column grid, gap 32px
- Photo: aspect-ratio 3/4, rounded-2xl, overflow hidden, card bg fallback
- Name: h4 1.125rem below photo
- Role: 0.8125rem, bark color
- Centered text below each photo

**Verification:** Team section shows large 3/4 photos with names below. `npm run build` passes.
</task>

<task id="7" type="code" effort="M">
**Upgrade TextImageSection to overlapping story layout**

Current: Side-by-side image + text.
Needed (om-oss.html): Overlapping layout:
- Grid: `1.2fr 1fr`, items centered
- Image: `border-radius: 0 24px 24px 0`, aspect-ratio 4/5, margin-right -48px, z-index 1, box-shadow
- Text box: card bg, padding 64px, rounded-3xl, z-index 2 (overlaps image)
- h2: clamp(1.75rem, 1.5rem + 0.8vw, 2rem)
- Paragraphs: 0.9375rem, line-height 1.65
- Optional signature line: Merriweather italic, bark color
- Mobile: stack vertically, no overlap

Keep reading `imagePosition` for which side the image is on. Default to left image / right text (matching prototype).

**Verification:** Text-image section shows overlapping layout on desktop, stacked on mobile. `npm run build` passes.
</task>

<task id="8" type="code" effort="M">
**Create LocationSection + register in SectionRenderer**

New component needed (both om-oss.html and kontakt-v2.html):

**Om-oss variant (dark):** forest bg, cream text
- Grid: 1fr 1fr, gap 64px
- Left: h2 cream, p cream/80, location details with icons (map-pin, mail, phone)
- Right: map placeholder (rounded-2xl, cream/10 bg)

**Kontakt variant (light):** cream bg
- Grid: 1fr 1.5fr, gap 48px
- Left: h2, p, location details with icons
- Right: map placeholder

Use the section's `items` array for location details (icon, label). Use a `variant` field or detect from background color.

Add `PageSection` type `'location'` to `src/types/index.ts` SectionType union.
Register in `SectionRenderer.tsx`.

**Verification:** LocationSection renders with map placeholder. SectionRenderer handles 'location' type. `npm run build` passes.
</task>

<task id="9" type="code" effort="S">
**Upgrade ValuesSection to match prototype**

Current: Card bg, rounded-xl, icon in circle.
Needed (om-oss.html): More spacious cards:
- Section bg: card
- Cards: cream bg, rounded-2xl, padding 48px 32px, text-center
- Hover: translateY(-4px)
- Icon: 48x48, stroke-width 1.5, forest color (no circle background)
- h3: 1.25rem
- p: 0.875rem, line-height 1.6

Also add section subheading support (values-header has h2 + p).

**Verification:** Values cards show large icons without circle bg, spacious padding. `npm run build` passes.
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS data → Section rendering | Section content from Firestore/mock data rendered via React JSX (auto-escaped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | XSS | Section body content | mitigate | React JSX auto-escapes. Rich text (if any) must use sanitized HTML |
</threat_model>

<verification>
After all tasks:
1. `npm run build` — must pass
2. All 14 section types render without errors
3. New LocationSection registered in SectionRenderer
4. No regressions on existing pages using these components
</verification>

<success_criteria>
- HeroSection supports fullscreen and compact variants
- TrustBarSection renders dark inline style
- ExperiencesGridSection shows detailed cards with badge/date/location/price
- FaqSection is 2-column card grid
- ContactInfoSection overlaps hero with floating cards
- TeamSection has 3/4 photo-strip
- TextImageSection has overlapping story layout
- LocationSection created and registered
- ValuesSection upgraded with spacious cards
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-redesign-hovedsider/15-01-SUMMARY.md`
</output>
