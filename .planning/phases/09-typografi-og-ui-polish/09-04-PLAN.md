---
phase: 09-typografi-og-ui-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/products/VariantSelector.tsx
  - src/app/(public)/produkter/[slug]/page.tsx
autonomous: false
requirements:
  - UIPOL-06
  - UIPOL-07

must_haves:
  truths:
    - "CartDrawer er verifisert fungerende — apner/lukker, viser riktig total, CTA navigerer til /checkout"
    - "Pa produktdetaljsiden oppdateres den viste prisen nar brukeren velger en annen variant"
    - "VariantSelector viser alltid gjeldende valgt pris tydelig"
  artifacts:
    - path: "src/components/products/VariantSelector.tsx"
      provides: "Dynamisk pris-visning basert pa valgt variant"
    - path: "src/app/(public)/produkter/[slug]/page.tsx"
      provides: "Korrekt bruk av VariantSelector med pris-prop"
  key_links:
    - from: "VariantSelector selectedVariantId state"
      to: "vist pris i UI"
      via: "activePrice = selectedVariant?.price ?? product.price"
      pattern: "activePrice"
---

<objective>
Verifiser og fiks CartDrawer (UIPOL-06), og legg til dynamisk pris-visning i VariantSelector slik at prisen oppdateres nar brukeren velger en variant (UIPOL-07). Avsluttes med en visuell human-verify checkpoint.

Purpose: Produktdetaljsiden viser alltid `product.price` via PriceBadge uavhengig av hvilken variant som er valgt — kunden ser aldri den variant-spesifikke prisen. Dette er forvirrende for produkter med prisforskjeller mellom varianter.
Output: VariantSelector viser live pris under variantknappene. CartDrawer-funksjonaliteten bekreftes korrekt.
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/products/VariantSelector.tsx
@src/app/(public)/produkter/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verifiser CartDrawer og legg til dynamisk pris i VariantSelector</name>
  <files>
    src/components/products/VariantSelector.tsx
    src/app/(public)/produkter/[slug]/page.tsx
  </files>
  <read_first>
    - src/components/products/VariantSelector.tsx — linje 22-83:
      - `selectedVariant` bestemmes fra `selectedVariantId` state (linje 26)
      - `activePrice = selectedVariant?.price ?? product.price` (linje 27) — riktig beregning
      - Men `activePrice` brukes kun i `handleAdd()` — aldri vist i UI
    - src/app/(public)/produkter/[slug]/page.tsx — linje 65: `PriceBadge priceInOre={product.price}` vises alltid, selv nar VariantSelector er aktiv
    - src/components/cart/CartDrawer.tsx — bekreft at OrderSummaryPanel med ctaText="Ga til betaling" og ctaHref="/checkout" er korrekt (ingen kodeendring nodvendig)
  </read_first>
  <action>
    **UIPOL-06 — CartDrawer audit:**
    CartDrawer.tsx er korrekt: apner/lukker via isOpen/onClose, viser subtotal via OrderSummaryPanel, CTA er "Ga til betaling" som navigerer til /checkout. Ingen endring nodvendig. Dokumenter i SUMMARY at CartDrawer passerer audit.

    **UIPOL-07 — Legg til pris-visning i VariantSelector:**

    I src/components/products/VariantSelector.tsx, etter `</div>` som lukker `flex flex-wrap gap-2` (variantknapp-listen), legg til:

    ```tsx
    <p className="mt-3 font-body text-h4 font-bold text-forest">
      {formatPrice(activePrice)}
    </p>
    ```

    `formatPrice` er allerede importert (linje 8). `activePrice` er allerede definert (linje 27).

    **Juster produktdetaljsiden:**

    I src/app/(public)/produkter/[slug]/page.tsx, endre linje 65 fra:
    ```tsx
    <PriceBadge priceInOre={product.price} className="mt-4 block text-h4" />
    ```
    til:
    ```tsx
    {product.variants.length === 0 && (
      <PriceBadge priceInOre={product.price} className="mt-4 block text-h4" />
    )}
    ```
    For produkter med varianter viser VariantSelector prisen dynamisk — ingen duplikat statisk pris.
  </action>
  <verify>
    `grep -n "formatPrice(activePrice)" src/components/products/VariantSelector.tsx` — skal gi treff
    `grep -n "variants.length === 0" src/app/(public)/produkter/[slug]/page.tsx` — skal gi treff
    `npm run build 2>&1 | tail -5` — ingen feil
  </verify>
  <done>
    VariantSelector viser `formatPrice(activePrice)` under variantknappene. slug/page.tsx viser PriceBadge kun for produkter uten varianter. `npm run build` passerer.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verifiser interaksjonstilstander visuelt</name>
  <action>
    Kjoer `npm run dev` og utfor den visuelle verifiseringen beskrevet under.
  </action>
  <verify>
    Kjoer `npm run dev` og test folgende:

    1. **CartDrawer (UIPOL-06):**
       - Klikk handlekurv-ikonet i headeren — skuffen skal slide inn fra hoyre
       - Legg til et produkt og apne skuffen igjen — subtotal og totalsum skal stemme
       - Klikk "Ga til betaling" — skal navigere til /checkout
       - Klikk kryss-knappen — hover skal vise lysegron bakgrunn, skuffen skal lukke

    2. **VariantSelector pris (UIPOL-07):**
       - Ga til et produkt med varianter (/produkter/[slug])
       - Klikk ulike varianter — prisen under knappene skal oppdateres umiddelbart
       - Ingen duplikat statisk PriceBadge over VariantSelector

    3. **DataTable mobil (UIPOL-01):**
       - Apne /admin/ordrer i DevTools pa 375px bredde
       - Tabellen skal vaere scrollbar horisontalt, ikke klippet

    4. **Status-badges (TYPO-05):**
       - Pa /admin/ordrer skal badges vise riktige farger: gul for venter, gronn for bekreftet, rod for avbrutt

    5. **Loading-states (UIPOL-03):**
       - Ga til /produkter med langsom nettforbindelse (DevTools throttle) — spinner skal vises
  </verify>
  <done>
    Alle visuelle interaksjonstilstander fungerer korrekt pa tvers av alle implementerte plans i Phase 09.
  </done>
  <what-built>
    Plan 09-01 til 09-04 implementerer designsystem-konsistens og UI-polish. Denne checkpoint verifiserer de mest kritiske interaksjonene visuelt.
  </what-built>
  <how-to-verify>
    Se verify-blokken over for fullstendige trinn.
  </how-to-verify>
  <resume-signal>Skriv "godkjent" hvis alt fungerer, eller beskriv problemene</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client state → cart | CartProvider haandterer state, ingen server-side eksponering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | VariantSelector price display | accept | Pris-visning er klientsideberegning basert pa lastede produktdata, ikke bruker-input |
</threat_model>

<verification>
`npm run build` uten feil. CartDrawer UIPOL-06 verifisert: apner, viser total, navigerer til checkout. VariantSelector UIPOL-07 verifisert: pris oppdateres ved variantvalg.
</verification>

<success_criteria>
- VariantSelector viser `formatPrice(activePrice)` under variantknappene
- PriceBadge pa produktdetaljsiden vises kun for produkter uten varianter
- CartDrawer apner/lukker, viser riktig total, CTA navigerer til /checkout
- Human checkpoint godkjent
- `npm run build` passerer
</success_criteria>

<output>
After completion, create `.planning/phases/09-typografi-og-ui-polish/09-04-SUMMARY.md`
</output>
