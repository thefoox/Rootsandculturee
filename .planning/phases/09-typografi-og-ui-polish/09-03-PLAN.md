---
phase: 09-typografi-og-ui-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/konto/EmptyState.tsx
  - src/app/konto/page.tsx
  - src/app/konto/ordrer/page.tsx
  - src/app/konto/bookinger/page.tsx
  - src/app/admin/ordrer/page.tsx
autonomous: true
requirements:
  - UIPOL-04
  - UIPOL-05

must_haves:
  truths:
    - "Tomme lister i kundekonto viser ikon + heading + hjelpetekst — ikke bare en ren tekst-streng"
    - "Admin-ordresiden viser meningsfull tomt-tilstand med ikon nar det ikke finnes ordrer"
    - "Konto-EmptyState bruker et ikonsystem som er konsistent med resten av appen"
  artifacts:
    - path: "src/components/konto/EmptyState.tsx"
      provides: "Oppdatert tomt-tilstand-komponent med ikon-prop og heading"
    - path: "src/app/konto/page.tsx"
      provides: "Korrekte icon-props pa EmptyState-kall"
    - path: "src/app/konto/ordrer/page.tsx"
      provides: "Korrekte icon-props pa EmptyState-kall"
    - path: "src/app/konto/bookinger/page.tsx"
      provides: "Korrekte icon-props pa EmptyState-kall"
    - path: "src/app/admin/ordrer/page.tsx"
      provides: "ShoppingCart-ikon i tomt-tilstand istedenfor tom streng"
  key_links:
    - from: "src/app/konto/page.tsx"
      to: "src/components/konto/EmptyState.tsx"
      via: "EmptyState-komponent med icon-prop"
      pattern: "<EmptyState"
---

<objective>
Oppgrader konto-EmptyState til a vise ikon + heading + hjelpetekst (konsistent med shared/EmptyState), og fiks admin-ordresiden sin tomt-tilstand.

Purpose: Tom liste med bare ren tekst gir en fattig brukeropplevelse. Med ikon og heading signaliserer vi tydelig hva som mangler og hva brukeren kan gjore.
Output: Oppdatert konto/EmptyState-komponent med icon-prop og heading-prop, oppdaterte kallers, fikset admin/ordrer tomt-tilstand.
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/shared/EmptyState.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Oppgrader konto/EmptyState og oppdater alle kall</name>
  <files>
    src/components/konto/EmptyState.tsx
    src/app/konto/page.tsx
    src/app/konto/ordrer/page.tsx
    src/app/konto/bookinger/page.tsx
  </files>
  <read_first>
    - src/components/konto/EmptyState.tsx — navarende implementasjon: Package-ikon hardkodet, kun `message`-prop, ingen `heading`-prop. Returnerer enkel p-tag.
    - src/components/shared/EmptyState.tsx — referanseimplementasjon med `icon`, `heading`, `body`, `ctaLabel`, `ctaHref` props.
    - src/app/konto/page.tsx — linje 60, 86: `<EmptyState message="..." ctaLabel="..." ctaHref="..." />`
    - src/app/konto/ordrer/page.tsx — linje 32: `<EmptyState message="..." ctaLabel="..." ctaHref="..." />`
    - src/app/konto/bookinger/page.tsx — linje 46, 62: `<EmptyState message="..." />` (uten CTA)
  </read_first>
  <action>
    **Steg 1 — Skriv om src/components/konto/EmptyState.tsx:**

    Endre grensesnittet til a matche shared/EmptyState-monsteret:
    ```tsx
    import Link from 'next/link'
    import type { ElementType } from 'react'

    interface EmptyStateProps {
      icon?: ElementType
      heading?: string
      message: string
      ctaLabel?: string
      ctaHref?: string
    }

    export function EmptyState({ icon: Icon, heading, message, ctaLabel, ctaHref }: EmptyStateProps) {
      return (
        <div className="flex flex-col items-center justify-center py-12 text-center">
          {Icon && (
            <Icon className="h-10 w-10 text-body/40 mb-3" aria-hidden="true" />
          )}
          {heading && (
            <h3 className="font-heading text-h4 font-bold text-forest mb-1">
              {heading}
            </h3>
          )}
          <p className="font-body text-body text-body/70">{message}</p>
          {ctaLabel && ctaHref && (
            <Link
              href={ctaHref}
              className="mt-4 inline-flex items-center rounded-full border border-forest px-4 py-2 font-body text-label font-medium text-forest motion-safe:transition-colors motion-safe:duration-150 hover:bg-forest hover:text-cream"
            >
              {ctaLabel}
            </Link>
          )}
        </div>
      )
    }
    ```

    `icon` og `heading` er valgfrie for bakoverkompatibilitet, men alle eksisterende kall skal oppdateres.

    **Steg 2 — Oppdater konto/page.tsx:**

    Legg til ShoppingBag og CalendarDays imports fra lucide-react. Oppdater EmptyState-kallene:
    - Ordrer-seksjonen (linje 60):
      ```tsx
      import { ShoppingBag, CalendarDays } from 'lucide-react'
      ...
      <EmptyState
        icon={ShoppingBag}
        heading="Ingen ordrer enda"
        message="Du har ingen ordrer enna."
        ctaLabel="Utforsk opplevelser"
        ctaHref="/opplevelser"
      />
      ```
    - Bookinger-seksjonen (linje 86):
      ```tsx
      <EmptyState
        icon={CalendarDays}
        heading="Ingen bookinger enda"
        message="Du har ingen bookinger enna."
        ctaLabel="Utforsk opplevelser"
        ctaHref="/opplevelser"
      />
      ```

    **Steg 3 — Oppdater konto/ordrer/page.tsx:**
    ```tsx
    import { ShoppingBag } from 'lucide-react'
    ...
    <EmptyState
      icon={ShoppingBag}
      heading="Ingen ordrer enda"
      message="Du har ingen ordrer enna."
      ctaLabel="Utforsk opplevelser"
      ctaHref="/opplevelser"
    />
    ```

    **Steg 4 — Oppdater konto/bookinger/page.tsx:**
    ```tsx
    import { CalendarDays } from 'lucide-react'
    ...
    // Kommende bookinger tom-tilstand:
    <EmptyState
      icon={CalendarDays}
      heading="Ingen kommende bookinger"
      message="Du har ingen kommende bookinger."
      ctaLabel="Utforsk opplevelser"
      ctaHref="/opplevelser"
    />
    // Tidligere bookinger tom-tilstand (uten CTA er OK):
    <EmptyState
      icon={CalendarDays}
      heading="Ingen tidligere bookinger"
      message="Du har ingen tidligere bookinger."
    />
    ```
  </action>
  <verify>
    `npm run build 2>&1 | tail -10` — ingen TypeScript-feil
    `grep -n "icon=" src/app/konto/page.tsx src/app/konto/ordrer/page.tsx src/app/konto/bookinger/page.tsx` — alle EmptyState-kall har icon-prop
  </verify>
  <done>
    konto/EmptyState.tsx godtar icon-, heading- og message-props. Alle 5 EmptyState-kall i konto-sidene har icon og heading. `npm run build` passerer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fiks admin/ordrer tomt-tilstand og verifiser norske feilmeldinger</name>
  <files>
    src/app/admin/ordrer/page.tsx
  </files>
  <read_first>
    - src/app/admin/ordrer/page.tsx — linje 111-130: to steder med EmptyState-kall som bruker `icon=""` (tom streng). DataTable-kallet pa linje 144-155 har ogsaa `icon: ''`. Admin-sidene bruker shared/EmptyState, ikke konto/EmptyState.
    - Sjekk linje 6-9: importerer `EmptyState` fra `@/components/shared/EmptyState` — denne krever en riktig `icon: ElementType`.
  </read_first>
  <action>
    **Steg 1 — Legg til ShoppingBag import og fiks EmptyState-kall i admin/ordrer/page.tsx:**

    Legg til ShoppingBag i importlisten oppe i filen (fra lucide-react).

    Fiks det standalone EmptyState-kallet (linje 123-128):
    ```tsx
    <EmptyState
      icon={ShoppingBag}
      heading="Ingen ordrer enda"
      body="Ordrer vises her etter at kunder har gjennomfort kjop."
    />
    ```

    Fiks DataTable emptyState-konfigurasjonen (linje 148-154):
    ```tsx
    emptyState={{
      icon: ShoppingBag,
      heading: 'Ingen ordrer enda',
      body: 'Ordrer vises her etter at kunder har gjennomfort kjop.',
      ctaLabel: '',
      ctaHref: '#',
    }}
    ```

    **Steg 2 — Verifiser norske feilmeldinger (UIPOL-05):**
    Sjekk om `admin/ordrer/page.tsx` viser en feilmelding ved catch. Den eksisterende `useEffect` catcher bare ved a kalle `setLoading(false)` — ingen toast ved feil. Legg til toast ved Firestore-feil:

    I `useEffect`-callbacken:
    ```typescript
    getOrders().then((data) => {
      setOrders(data)
      setLoading(false)
    }).catch(() => {
      setLoading(false)
      toast.error('Kunne ikke laste ordrer. Prover igjen.')
    })
    ```

    Legg til `import { toast } from 'sonner'` oppe i filen hvis det ikke allerede er der.
  </action>
  <verify>
    `grep -n "icon:" src/app/admin/ordrer/page.tsx` — skal ikke vise `icon: ''`
    `grep -n "toast" src/app/admin/ordrer/page.tsx` — skal vise toast-import og toast.error-kall
    `npm run build 2>&1 | tail -5` — ingen feil
  </verify>
  <done>
    admin/ordrer/page.tsx bruker ShoppingBag-ikon i EmptyState og DataTable-tomtilstand. Toast-feilmelding pa norsk vises ved lastingsfeil.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-komponenter | Ingen dataflyt-endringer, kun presentasjonslag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Information Disclosure | EmptyState | accept | Tom-tilstand avslorer ingen sensitiv informasjon |
</threat_model>

<verification>
`npm run build` uten TypeScript-feil. Alle konto EmptyState-kall har icon og heading. admin/ordrer bruker ShoppingBag-ikon.
</verification>

<success_criteria>
- konto/EmptyState.tsx godtar icon-, heading- og message-props
- Alle EmptyState-kall i konto/page.tsx, konto/ordrer/page.tsx og konto/bookinger/page.tsx har icon-prop
- admin/ordrer/page.tsx bruker ShoppingBag-ikon og har toast-feilmelding pa norsk
- `npm run build` passerer
</success_criteria>

<output>
After completion, create `.planning/phases/09-typografi-og-ui-polish/09-03-SUMMARY.md`
</output>
