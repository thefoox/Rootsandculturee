---
phase: 09-typografi-og-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/admin/OrderStatusBadge.tsx
  - src/components/admin/BookingStatusBadge.tsx
  - src/app/admin/page.tsx
  - src/components/experiences/ExperienceCard.tsx
  - src/components/cart/CartItem.tsx
  - src/components/products/VariantSelector.tsx
  - src/components/checkout/CheckoutForm.tsx
autonomous: true
requirements:
  - TYPO-01
  - TYPO-02
  - TYPO-03
  - TYPO-04
  - TYPO-05

must_haves:
  truths:
    - "Alle hardkodede tekststorrelser og hex-farger er erstattet med CSS-tokens fra globals.css"
    - "Status-badges bruker de definerte badge-fargene fra @theme i globals.css — ingen inline hex"
    - "article-prose i globals.css bruker CSS-variabler for fontstorrelser"
    - "Focus-ringen i globals.css bruker var(--color-forest) og var(--color-cream) — ikke hardkodede hex"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Design tokens og article-prose med CSS vars"
      contains: "var(--color-forest)"
    - path: "src/components/admin/OrderStatusBadge.tsx"
      provides: "Status-badge uten hardkodede hex-verdier"
    - path: "src/components/admin/BookingStatusBadge.tsx"
      provides: "Booking-badge uten hardkodede hex-verdier"
  key_links:
    - from: "src/app/globals.css @theme"
      to: "OrderStatusBadge.tsx"
      via: "CSS custom property tokens for badge farger"
      pattern: "badge-(pending|confirmed|cancelled|warning)"
    - from: "src/app/globals.css @theme"
      to: "globals.css article-prose"
      via: "var(--text-h2) var(--text-h3)"
      pattern: "var\\(--text-"
---

<objective>
Erstatt alle hardkodede tekststorrelser og hex-farger med CSS-tokens fra designsystemet. Fokuser pa de konkrete violasjonene som er identifisert i kildefilene.

Purpose: Konsistens i designsystemet — alle farger og tekststorrelser drives av CSS-variablene i globals.css. Ingen "mystery hex" i komponenter.
Output: Rene komponenter uten hardkodede verdier, nye badge-tokens i globals.css for status-farger.
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fiks hardkodede tekststorrelser i komponenter</name>
  <files>
    src/components/experiences/ExperienceCard.tsx
    src/components/cart/CartItem.tsx
    src/components/products/VariantSelector.tsx
    src/components/checkout/CheckoutForm.tsx
    src/app/globals.css
  </files>
  <read_first>
    - src/components/experiences/ExperienceCard.tsx — linje 54: `text-[12px]` for "earlybird"
    - src/components/cart/CartItem.tsx — linje 66: `text-[11px]` for "earlybird"
    - src/components/products/VariantSelector.tsx — linje 65: `bg-white` istedenfor `bg-cream`
    - src/components/checkout/CheckoutForm.tsx — linje 154, 158: `text-sm` i step-indicator div-er
    - src/app/globals.css — linje 116-130: article-prose bruker hardkodede px-verdier (20px for h2, 15px for h3)
  </read_first>
  <action>
    Gjor folgende eksakte endringer:

    **ExperienceCard.tsx linje 54:**
    - Endre `text-[12px]` → `text-label` (--text-label = 0.8125rem ≈ 13px, akseptabel avrunding)

    **CartItem.tsx linje 66:**
    - Endre `text-[11px]` → `text-label`

    **VariantSelector.tsx linje 65:**
    - Endre `bg-white` → `bg-cream` (hvit bakgrunn er ikke en designsystem-farge; cream er den rette ikke-valgte bakgrunnen)

    **CheckoutForm.tsx linje 154 og 158:**
    - Endre `text-sm font-bold` pa step-indicator-rundingene til `text-label font-bold`
    - Begge div-er med `flex h-8 w-8 items-center justify-center rounded-full text-sm font-bold` skal fa `text-label`

    **globals.css article-prose (linje 116-130):**
    - Endre `.article-prose h2 { font-size: 20px; }` → `font-size: var(--text-h3);`
    - Endre `.article-prose h3 { font-size: 15px; }` → `font-size: var(--text-h4);`
    - Behold line-height, margin, font-family og font-weight som de er
  </action>
  <verify>
    Grep for hardkodede tekststorrelser i de endrede filene:
    `grep -n "text-\[" src/components/experiences/ExperienceCard.tsx src/components/cart/CartItem.tsx` — skal returnere ingen treff
    `grep -n "bg-white" src/components/products/VariantSelector.tsx` — skal returnere ingen treff
    `grep -n "text-sm" src/components/checkout/CheckoutForm.tsx` — skal returnere ingen treff
    `grep -n "font-size: [0-9]" src/app/globals.css` — skal kun finne verdier i body-regelen (15px) og article-prose font-size skal ikke ha px-verdier
  </verify>
  <done>
    Ingen `text-[Npx]`, `text-[Nrem]` eller `bg-white` i de opplistede komponentene. article-prose bruker var(--text-h3) og var(--text-h4). CheckoutForm step-numre bruker text-label.
  </done>
</task>

<task type="auto">
  <name>Task 2: Legg til badge-tokens i globals.css og erstatt hardkodede hex i status-badges</name>
  <files>
    src/app/globals.css
    src/components/admin/OrderStatusBadge.tsx
    src/components/admin/BookingStatusBadge.tsx
    src/app/admin/page.tsx
  </files>
  <read_first>
    - src/app/globals.css — @theme-blokken (linje 3-55): eksisterende tokens for referanse. Legg til nye badge-tokens etter `--color-cart-badge`.
    - src/components/admin/OrderStatusBadge.tsx — alle hardkodede hex i statusConfig: `#FEF3C7`, `#92400E`, `#DCFCE7`, `#166534`, `#FEE2E2`, `#991B1B`
    - src/components/admin/BookingStatusBadge.tsx — samme hex-farger som OrderStatusBadge
    - src/app/admin/page.tsx — linje 153: inline `bg-[#DCFCE7] ... text-[#166534]` badge
  </read_first>
  <action>
    **Steg 1 — Legg til tokens i globals.css @theme (etter `--color-cart-badge: #B84D00;`):**

    ```css
    /* Badge status colors */
    --color-badge-warning: #92400E;
    --color-badge-warning-bg: #FEF3C7;
    --color-badge-error: #991B1B;
    --color-badge-error-bg: #FEE2E2;
    ```

    Merk: `--color-badge-easy` (`#166534` / `#DCFCE7`) og `--color-badge-moderate` finnes allerede fra Phase 2. Bruk disse for "betalt/bekreftet" og "moderat" statuser.

    **Steg 2 — Oppdater OrderStatusBadge.tsx statusConfig:**
    Erstatt alle `bg-[#...]` og `text-[#...]` med Tailwind-klasser som refererer tokens:
    ```typescript
    pending: { label: 'Venter', className: 'bg-badge-warning-bg text-badge-warning' },
    paid: { label: 'Bekreftet', className: 'bg-badge-easy-bg text-badge-easy' },
    confirmed: { label: 'Bekreftet', className: 'bg-badge-easy-bg text-badge-easy' },
    shipped: { label: 'Sendt', className: 'bg-badge-warning-bg text-badge-warning' },
    delivered: { label: 'Levert', className: 'border border-forest/20 bg-card text-forest' },
    cancelled: { label: 'Avbrutt', className: 'bg-badge-error-bg text-badge-error' },
    ```

    **Steg 3 — Oppdater BookingStatusBadge.tsx statusConfig:**
    ```typescript
    pending: { label: 'Venter', className: 'bg-badge-warning-bg text-badge-warning' },
    confirmed: { label: 'Bekreftet', className: 'bg-badge-easy-bg text-badge-easy' },
    cancelled: { label: 'Kansellert', className: 'bg-badge-error-bg text-badge-error' },
    ```

    **Steg 4 — Fiks admin/page.tsx linje 153:**
    Endre den inline "Betalt"-badge fra:
    `className: "rounded-full bg-[#DCFCE7] px-2 py-0.5 text-label text-[#166534]"`
    til:
    `className: "rounded-full bg-badge-easy-bg px-2 py-0.5 text-label text-badge-easy"`

    **Steg 5 — Fiks globals.css focus-ring (linje 67 og 73):**
    ```css
    *:focus-visible {
      outline: 2px solid var(--color-forest);  /* var i stedet for #1B4332 */
      outline-offset: 2px;
    }
    .dark-surface *:focus-visible {
      outline-color: var(--color-cream);  /* var i stedet for #FEFCF3 */
    }
    ```
  </action>
  <verify>
    `grep -n "#[0-9A-Fa-f]\{6\}" src/components/admin/OrderStatusBadge.tsx src/components/admin/BookingStatusBadge.tsx` — skal returnere ingen treff
    `grep -n "\[#" src/app/admin/page.tsx` — skal returnere ingen treff
    `grep -n "#1B4332\|#FEFCF3" src/app/globals.css` — skal returnere ingen treff (hex-verdiene som CSS-vars er OK inne i @theme-definisjoner)
    `npm run build 2>&1 | tail -5` — ingen TypeScript-feil
  </verify>
  <done>
    globals.css har `--color-badge-warning`, `--color-badge-warning-bg`, `--color-badge-error`, `--color-badge-error-bg` tokens. OrderStatusBadge, BookingStatusBadge og admin/page.tsx bruker bare Tailwind design-token klasser. Focus-ring bruker CSS vars.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS tokens → rendered HTML | Visual-only change, no data flow crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | globals.css badge tokens | accept | CSS-only change, no attack surface |
</threat_model>

<verification>
Kjoer `npm run build` og verifiser at ingen TypeScript-kompileringsfeil oppstar. Visuell inspeksjon: badges skal ha riktige farger (gul for venter, gronn for bekreftet, rod for avbrutt).
</verification>

<success_criteria>
- Ingen `text-[Npx]`, `text-[Nrem]`, `bg-white` eller inline hex (`bg-[#...]`, `text-[#...]`) i komponentene listet i files_modified
- article-prose i globals.css bruker var(--text-h3) og var(--text-h4)
- Focus-ringer bruker var(--color-forest) og var(--color-cream)
- OrderStatusBadge og BookingStatusBadge bruker kun designsystem-tokens
- `npm run build` passerer
</success_criteria>

<output>
After completion, create `.planning/phases/09-typografi-og-ui-polish/09-01-SUMMARY.md`
</output>
