---
phase: 07-cms-fikser-og-admin-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/page-content/[pageId]/route.ts
  - src/app/api/page-content/route.ts
  - src/components/sections/TrustBarSection.tsx
  - src/types/index.ts
autonomous: true
requirements: [CMS-01, CMS-02, CMS-03, CMS-05]

must_haves:
  truths:
    - "PUT /api/page-content/[pageId] kaller revalidateTag('page-content') etter lagring — siden oppdateres uten hard refresh"
    - "DELETE /api/page-content/[pageId] returnerer 200 og sletter dokumentet fra Firestore"
    - "TrustBarSection bruker section.items fra Firestore i stedet for hardkodede verdier"
    - "PageSection-typen har imagePosition-felt for text-image seksjoner"
  artifacts:
    - path: "src/app/api/page-content/[pageId]/route.ts"
      provides: "PUT med revalidateTag + DELETE endpoint"
      contains: "revalidateTag.*page-content"
    - path: "src/components/sections/TrustBarSection.tsx"
      provides: "Dynamisk rendering fra section.items med fallback"
      contains: "section.items"
    - path: "src/types/index.ts"
      provides: "imagePosition og text body i PageSection"
      contains: "imagePosition"
  key_links:
    - from: "PUT /api/page-content/[pageId]"
      to: "Next.js cache"
      via: "revalidateTag('page-content', 'max')"
      pattern: "revalidateTag.*page-content"
    - from: "TrustBarSection"
      to: "section.items"
      via: "conditional render med fallback til hardkodet"
      pattern: "section\\.items"
---

<objective>
Fiks CMS-cache og API-grunnlag: legg til revalidateTag i PUT-endepunktet, opprett DELETE-endepunkt for sider, og gjør TrustBarSection dynamisk (les fra section.items). Utvid PageSection-typen med imagePosition og body.

Purpose: Uten revalidateTag ser admin-brukere ikke endringer i opptil 1 time etter lagring. DELETE-endepunktet mangler. TrustBarSection er hardkodet og ignorerer CMS-data.
Output: Fungerende cache-invalidering, slett-funksjonalitet, dynamisk trust-bar, utvidet type.
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CMS API — legg til revalidateTag i PUT og opprett DELETE</name>
  <files>src/app/api/page-content/[pageId]/route.ts</files>
  <read_first>
    - src/app/api/page-content/[pageId]/route.ts (filen som endres)
    - src/actions/products.ts (linje 92: revalidateTag('products', 'max') som mønster)
  </read_first>
  <action>
Oppdater `src/app/api/page-content/[pageId]/route.ts`:

1. Legg til import øverst: `import { revalidateTag } from 'next/cache'`

2. I PUT-handleren, etter `adminDb.collection('pageContent').doc(pageId).set(...)` kallet og ETTER at adminDb-sjekken returnerer (for null-tilfellet), legg til:
   ```typescript
   revalidateTag('page-content', 'max')
   ```
   Plasser dette rett FØR `return NextResponse.json({ success: true })` slik at det kalles i begge veier (den mock-frie veien), men bare ETTER vellykket Firestore-skriving. For mock-tilfellet (adminDb is null), call revalidateTag der også rett før mock-return.

3. Legg til ny DELETE-handler på slutten av filen:
   ```typescript
   export async function DELETE(
     _request: Request,
     { params }: { params: Promise<{ pageId: string }> }
   ) {
     const { pageId } = await params

     if (!adminDb) {
       return NextResponse.json({ success: true, mock: true })
     }

     await adminDb.collection('pageContent').doc(pageId).delete()
     revalidateTag('page-content', 'max')
     return NextResponse.json({ success: true })
   }
   ```

VIKTIG: revalidateTag krever to argumenter i Next.js 16: tag-navn og 'max' (jf. STATE.md beslutning [Phase 02-02]: revalidateTag requires second arg 'max' in Next.js 16 for cache purge).
  </action>
  <verify>
    <automated>grep -n "revalidateTag\|DELETE" /home/william/Documents/Rootsnew/src/app/api/page-content/\[pageId\]/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - Filen inneholder `revalidateTag('page-content', 'max')` minst 2 ganger (en i PUT, en i DELETE)
    - Filen eksporterer `export async function DELETE(`
    - DELETE-handleren kaller `adminDb.collection('pageContent').doc(pageId).delete()`
    - PUT-handleren kaller revalidateTag etter Firestore-skriving og før return
  </acceptance_criteria>
  <done>PUT invaliderer cache etter lagring. DELETE sletter side og invaliderer cache.</done>
</task>

<task type="auto">
  <name>Task 2: Type-utvidelse og TrustBarSection dynamisk</name>
  <files>src/types/index.ts, src/components/sections/TrustBarSection.tsx</files>
  <read_first>
    - src/types/index.ts (PageSection interface, SectionItem interface)
    - src/components/sections/TrustBarSection.tsx (hardkodet trustItems array)
    - src/components/sections/ContactInfoSection.tsx (bruker section.items som mønster)
  </read_first>
  <action>
**Del A — `src/types/index.ts`:**

Legg til `imagePosition` og `body` på `PageSection`-interfacet. Finn `export interface PageSection {` og legg til disse to feltene:

```typescript
  imagePosition?: 'left' | 'right'  // CMS-05: text-image venstre/høyre
  body?: string                      // CMS-04: Allerede finnes kanskje, sjekk — body brukes til text-seksjon HTML
```

Sjekk om `body?: string` allerede finnes på PageSection. Hvis ja, legg bare til `imagePosition?: 'left' | 'right'`.

**Del B — `src/components/sections/TrustBarSection.tsx`:**

Reskriv komponenten slik at den bruker `section.items` hvis tilgjengelig, og faller tilbake til hardkodede verdier hvis items er tomme/fraværende. Lucide-ikoner må mappes fra ikonnavnstreng (som i ContactInfoSection). Eksempel:

```typescript
import * as icons from 'lucide-react'
import { Leaf, RotateCcw, Mountain } from 'lucide-react'
import type { PageSection } from '@/types'

const FALLBACK_ITEMS = [
  { icon: 'Leaf', label: 'Lokal produksjon', description: 'Alle produkter fra norsk natur' },
  { icon: 'RotateCcw', label: '14 dagers angrerett', description: 'Full returrett på alle produkter' },
  { icon: 'Mountain', label: 'Norsk natur', description: 'Autentiske naturopplevelser' },
]

function getIcon(name?: string): icons.LucideIcon | null {
  if (!name) return null
  const key = name.charAt(0).toUpperCase() + name.slice(1).replace(/-([a-z])/g, (_, c) => c.toUpperCase())
  const Icon = (icons as unknown as Record<string, icons.LucideIcon>)[key]
  return Icon ?? null
}

export function TrustBarSection({ section }: { section: PageSection }) {
  const items = (section.items && section.items.length > 0)
    ? section.items.map((item) => ({
        icon: item.icon || 'Leaf',
        label: item.title,
        description: item.description,
      }))
    : FALLBACK_ITEMS

  return (
    <section className="border-y border-forest/8 bg-cream py-10 md:py-14">
      <div className="mx-auto max-w-[1200px] px-6 md:px-8">
        <div className="grid gap-8 sm:grid-cols-3">
          {items.map(({ icon, label, description }) => {
            const Icon = getIcon(icon)
            return (
              <div key={label} className="flex flex-col items-center text-center">
                {Icon && <Icon className="h-8 w-8 text-forest" aria-hidden="true" />}
                <p className="mt-3 font-heading text-lg font-bold text-forest">{label}</p>
                <p className="mt-1 font-body text-label text-body/70">{description}</p>
              </div>
            )
          })}
        </div>
      </div>
    </section>
  )
}
```
  </action>
  <verify>
    <automated>grep -n "imagePosition\|section\.items\|FALLBACK_ITEMS" /home/william/Documents/Rootsnew/src/types/index.ts /home/william/Documents/Rootsnew/src/components/sections/TrustBarSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/index.ts` inneholder `imagePosition?: 'left' | 'right'` på PageSection-interfacet
    - `src/components/sections/TrustBarSection.tsx` inneholder `section.items`
    - `TrustBarSection.tsx` inneholder FALLBACK_ITEMS array for bakoverkompatibilitet
    - TypeScript kompilerer uten feil: `npx tsc --noEmit` returner 0
  </acceptance_criteria>
  <done>PageSection har imagePosition-felt. TrustBarSection leser fra CMS-data med fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → API | Admin-bruker sender PUT/DELETE via fetch i klientkomponent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | DELETE /api/page-content/[pageId] | mitigate | Legg til `verifySession()` sjekk i DELETE-handler som i products.ts — returner 401 hvis ikke admin |
| T-07-02 | Tampering | PUT-endepunkt uten autentisering | accept | Eksisterende PUT mangler auth-sjekk (pre-existing issue) — tas i sikkerhetsfase, ikke her |
| T-07-03 | Information Disclosure | Firestore pageId tilgjengelig i URL | accept | pageContent er ikke sensitiv data, lav risiko |
</threat_model>

<verification>
```bash
# 1. Sjekk revalidateTag i PUT-handler
grep -n "revalidateTag" /home/william/Documents/Rootsnew/src/app/api/page-content/\[pageId\]/route.ts

# 2. Sjekk DELETE eksporteres
grep -n "export async function DELETE" /home/william/Documents/Rootsnew/src/app/api/page-content/\[pageId\]/route.ts

# 3. Sjekk imagePosition i types
grep -n "imagePosition" /home/william/Documents/Rootsnew/src/types/index.ts

# 4. Sjekk TrustBarSection bruker section.items
grep -n "section\.items" /home/william/Documents/Rootsnew/src/components/sections/TrustBarSection.tsx

# 5. TypeScript-sjekk
cd /home/william/Documents/Rootsnew && npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
- PUT-endepunkt kaller revalidateTag('page-content', 'max') etter Firestore-skriving
- DELETE-endepunkt sletter pageContent-dokument og invaliderer cache
- TrustBarSection bruker section.items (med fallback til hardkodet)
- PageSection-type har imagePosition?: 'left' | 'right'
- TypeScript kompilerer uten nye feil
</success_criteria>

<output>
After completion, create `.planning/phases/07-cms-fikser-og-admin-crud/07-01-SUMMARY.md`
</output>
