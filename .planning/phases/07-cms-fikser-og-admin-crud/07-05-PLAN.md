---
phase: 07-cms-fikser-og-admin-crud
plan: 05
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/app/admin/innhold/[pageId]/page.tsx
  - src/components/sections/TextSection.tsx
  - src/components/sections/TextImageSection.tsx
  - src/components/admin/ContentBlockEditor.tsx
autonomous: true
requirements: [CMS-03, CMS-04, CMS-05, CMS-06, CMS-07, CMS-08, CMS-10]

must_haves:
  truths:
    - "Admin kan redigere trust-bar elementer (tittel, beskrivelse, ikon) i seksjons-editoren"
    - "text-seksjon viser Tiptap-editor for brødtekst og renders HTML på siden"
    - "text-image seksjon har imagePosition-toggle (venstre/høyre) som endrer rekkefølgen visuelt"
    - "contact-info elementer har href-felt for e-post/telefon/ekstern lenke"
    - "gallery-seksjonens overskrift er synlig i admin-editoren"
    - "Side-editoren har Vis side-lenke som åpner /{slug} i ny fane"
    - "ContentBlockEditor.tsx er slettet"
  artifacts:
    - path: "src/app/admin/innhold/[pageId]/page.tsx"
      provides: "Oppdatert seksjonEditor med alle fikser"
      contains: "imagePosition"
    - path: "src/components/sections/TextSection.tsx"
      provides: "Renderer for HTML body-innhold fra Tiptap"
      contains: "dangerouslySetInnerHTML"
    - path: "src/components/sections/TextImageSection.tsx"
      provides: "Støtte for imagePosition left/right"
      contains: "imagePosition"
  key_links:
    - from: "SortableSection (text type)"
      to: "TiptapEditor"
      via: "hasBody includes 'text'"
      pattern: "text.*hasBody\|hasBody.*text"
    - from: "TextImageSection"
      to: "imagePosition"
      via: "flex-direction basert på section.imagePosition"
      pattern: "imagePosition.*flex-row-reverse\|flex-row-reverse.*imagePosition"
---

<objective>
Fiks alle gjenværende seksjoneditor-problemer i CMS-admin: trust-bar items editor, text body/Tiptap, imagePosition-toggle, contact-info href, gallery heading, Vis side-lenke, og slett dead code.

Purpose: Disse er UI-fikser i page-editoren og tilhørende section-renderer-komponenter. Avhenger av Plan 01 (imagePosition i types/index.ts og TrustBarSection-mønsteret).
Output: Fullt fungerende CMS-seksjoneditor for alle seksjonstyper.
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-cms-fikser-og-admin-crud/07-01-SUMMARY.md
</context>

<interfaces>
Fra src/types/index.ts (oppdatert av Plan 01):
```typescript
export interface PageSection {
  id: string
  type: SectionType
  heading?: string
  subheading?: string
  body?: string
  image?: ProductImage
  items?: SectionItem[]
  ctaText?: string
  ctaLink?: string
  order: number
  imagePosition?: 'left' | 'right'  // lagt til av Plan 01
}

export interface SectionItem {
  title: string
  description: string
  image?: ProductImage
  icon?: string
  href?: string  // finnes allerede
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fiks SortableSection — trust-bar items, text body, gallery heading, imagePosition, contact-info href</name>
  <files>src/app/admin/innhold/[pageId]/page.tsx</files>
  <read_first>
    - src/app/admin/innhold/[pageId]/page.tsx (hele filen — SortableSection-komponenten fra linje ~71)
    - src/types/index.ts (PageSection og SectionItem interfaces — sjekk href på SectionItem)
  </read_first>
  <action>
Oppdater `SortableSection`-komponenten i `src/app/admin/innhold/[pageId]/page.tsx`:

**Fiks 1 — CMS-04: text-seksjon trenger Tiptap body:**

Endre `hasBody`-konstanten fra:
```typescript
const hasBody = section.type === 'text-image'
```
til:
```typescript
const hasBody = section.type === 'text-image' || section.type === 'text'
```

Legg også til `body: ''` i `createDefaultSection` for `text`-typen:
```typescript
// I createDefaultSection-funksjonen, legg til:
if (['text', 'text-image'].includes(type)) {
  base.body = ''
}
```
(Sjekk om `text` allerede er der — NB: eksisterende kode har `if (type === 'text-image')` for body)

**Fiks 2 — CMS-07: gallery heading synlig:**

Finn linjen:
```typescript
{section.type !== 'gallery' && (
  <Input label="Overskrift" ... />
)}
```
og fjern gallery-unntaket:
```typescript
<Input
  label="Overskrift"
  value={section.heading || ''}
  onChange={(e) => onUpdate({ heading: e.target.value })}
/>
```
(Fjern wrapping `{section.type !== 'gallery' && (...)}`-betingelsen helt.)

**Fiks 3 — CMS-05: imagePosition-toggle:**

Legg til `hasImagePosition` konstant:
```typescript
const hasImagePosition = section.type === 'text-image'
```

Legg til i JSX etter `hasImage`-blokken (eller etter CTA-blokken):
```tsx
{hasImagePosition && (
  <div>
    <label className="mb-2 block text-label font-medium text-forest">
      Bildeposisjon
    </label>
    <div className="flex gap-3">
      {(['left', 'right'] as const).map((pos) => (
        <label key={pos} className="flex items-center gap-2 text-body text-forest">
          <input
            type="radio"
            name={`imagePosition-${section.id}`}
            value={pos}
            checked={(section.imagePosition || 'left') === pos}
            onChange={() => onUpdate({ imagePosition: pos })}
            className="h-4 w-4 accent-forest"
          />
          {pos === 'left' ? 'Bilde til venstre' : 'Bilde til høyre'}
        </label>
      ))}
    </div>
  </div>
)}
```

**Fiks 4 — CMS-06: contact-info href-felt:**

I items-render-blokken, finn `{section.type === 'contact-info' && (` delen som viser ikonfeltet.
Legg til href-felt ETTER ikonfeltet for contact-info:
```tsx
{section.type === 'contact-info' && (
  <>
    <Input
      label="Ikon (lucide-navn)"
      value={item.icon || ''}
      onChange={(e) => onUpdateItem(i, { icon: e.target.value })}
    />
    <Input
      label="Lenke (mailto:, tel:, eller URL)"
      value={item.href || ''}
      onChange={(e) => onUpdateItem(i, { href: e.target.value })}
    />
  </>
)}
```
(Erstatt eksisterende enkelt-Input for ikon med dette `<>`-blokken.)

**Fiks 5 — CMS-03: trust-bar items editor:**

Legg til `'trust-bar'` i `hasItems`-betingelsen:
```typescript
const hasItems = ['faq', 'values', 'team', 'gallery', 'contact-info', 'trust-bar'].includes(section.type)
```

Og i `createDefaultSection`, legg til `trust-bar` i items-initialiseringen:
```typescript
if (['faq', 'values', 'team', 'gallery', 'contact-info', 'trust-bar'].includes(type)) {
  base.items = []
}
```

I items-render-blokken, legg til trust-bar spesifikke felt. Finn `{section.type === 'values' && (` og legg til tilsvarende for trust-bar:
```tsx
{section.type === 'trust-bar' && (
  <Input
    label="Ikon (lucide-navn, f.eks. Leaf)"
    value={item.icon || ''}
    onChange={(e) => onUpdateItem(i, { icon: e.target.value })}
  />
)}
```

**Fiks 6 — CMS-08: Vis side-lenke:**

Finn `<div className="mt-8 flex gap-3">` (eller lignende) som inneholder Lagre/Tilbake-knappene. Legg til lenke til slutt:
```tsx
{pageSlug && (
  <a
    href={`/${pageSlug === '/' ? '' : pageSlug}`}
    target="_blank"
    rel="noopener noreferrer"
    className="inline-flex items-center gap-1.5 rounded-lg border border-forest/20 px-4 py-2 text-sm font-medium text-forest hover:bg-card"
  >
    Vis side
    <span aria-hidden="true">↗</span>
  </a>
)}
```
  </action>
  <verify>
    <automated>grep -n "hasBody\|hasItems.*trust-bar\|imagePosition\|section\.type !== 'gallery'\|href.*lenke\|Vis side\|trust-bar" /home/william/Documents/Rootsnew/src/app/admin/innhold/\[pageId\]/page.tsx | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `hasBody` inneholder `section.type === 'text'` (Tiptap for text-seksjon)
    - `hasItems` inneholder `'trust-bar'`
    - `section.type !== 'gallery'`-guard er fjernet fra heading-input (gallery har nå synlig heading)
    - `hasImagePosition`-blokk finnes i JSX med radio-knapper for left/right
    - contact-info items har href-input-felt
    - trust-bar items har icon-input-felt
    - "Vis side"-lenke åpner `/{pageSlug}` i ny fane (`target="_blank"`)
  </acceptance_criteria>
  <done>Alle seksjoneditor-fikser er implementert i page-editor-komponenten.</done>
</task>

<task type="auto">
  <name>Task 2: Oppdater TextSection og TextImageSection + slett ContentBlockEditor</name>
  <files>src/components/sections/TextSection.tsx, src/components/sections/TextImageSection.tsx, src/components/admin/ContentBlockEditor.tsx</files>
  <read_first>
    - src/components/sections/TextSection.tsx (mangler body-rendering)
    - src/components/sections/TextImageSection.tsx (mangler imagePosition-støtte)
    - src/components/admin/ContentBlockEditor.tsx (bekreft at den aldri importeres)
  </read_first>
  <action>
**Del A — CMS-04: TextSection render body HTML:**

Oppdater `src/components/sections/TextSection.tsx` til å rendre `section.body` som HTML (fra Tiptap):

```typescript
export function TextSection({ section }: { section: PageSection }) {
  return (
    <section className="bg-cream py-20 md:py-28">
      <div className="mx-auto max-w-[1200px] px-6 md:px-8">
        {section.heading && (
          <h2 className="font-heading text-h1 font-bold text-forest">
            {section.heading}
          </h2>
        )}
        {section.subheading && (
          <p className="mt-2 max-w-md font-body text-body">
            {section.subheading}
          </p>
        )}
        {section.body && (
          <div
            className="mt-5 max-w-2xl font-body text-body leading-relaxed text-forest prose-p:mt-4 first:prose-p:mt-0"
            dangerouslySetInnerHTML={{ __html: section.body }}
          />
        )}
      </div>
    </section>
  )
}
```

**Del B — CMS-05: TextImageSection imagePosition:**

Oppdater `src/components/sections/TextImageSection.tsx` til å støtte `section.imagePosition`:

Finn `<div className="grid items-center gap-10 py-20 md:grid-cols-2 md:gap-16 md:py-28">` og erstatt med:
```tsx
<div className={`grid items-center gap-10 py-20 md:grid-cols-2 md:gap-16 md:py-28 ${
  section.imagePosition === 'right' ? 'md:[&>*:first-child]:order-last' : ''
}`}>
```

Alternativt, bruk `flex`-tilnærming som er enklere:
```tsx
<div className={`flex flex-col gap-10 py-20 md:gap-16 md:py-28 md:flex-row md:items-center ${
  section.imagePosition === 'right' ? 'md:flex-row-reverse' : ''
}`}>
```

Velg `flex`-tilnærmingen da den er mer eksplisitt. Erstatt grid-wrapper med flex-wrapper og juster image/text-div til å ikke bruke grid-spesifikke klasser.

Eksempel — slik tekstblokken og bildblokken struktureres med flex:
```tsx
<div className={`flex flex-col gap-10 py-20 md:gap-16 md:py-28 md:items-center ${
  section.imagePosition === 'right' ? 'md:flex-row-reverse' : 'md:flex-row'
}`}>
  {section.image && (
    <div className="relative aspect-[4/5] overflow-hidden rounded-2xl md:w-1/2">
      <Image ... />
    </div>
  )}
  <div className="md:w-1/2">
    {/* ... heading, body, CTA ... */}
  </div>
</div>
```

**Del C — CMS-10: Slett ContentBlockEditor.tsx:**

VERIFISER FØRST at ingen filer importerer ContentBlockEditor:
```bash
grep -r "ContentBlockEditor" /home/william/Documents/Rootsnew/src --include="*.ts" --include="*.tsx" -l
```

Hvis INGEN andre filer importerer den (kun filen selv), slett filen:
```bash
rm /home/william/Documents/Rootsnew/src/components/admin/ContentBlockEditor.tsx
```

Hvis noen filer importerer den, erstatt importene med tilsvarende `Input`/`textarea`-bruk, DERETTER slett.
  </action>
  <verify>
    <automated>grep -n "dangerouslySetInnerHTML\|section\.body" /home/william/Documents/Rootsnew/src/components/sections/TextSection.tsx && grep -n "imagePosition\|flex-row-reverse" /home/william/Documents/Rootsnew/src/components/sections/TextImageSection.tsx && ls /home/william/Documents/Rootsnew/src/components/admin/ContentBlockEditor.tsx 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - TextSection.tsx inneholder `dangerouslySetInnerHTML={{ __html: section.body }}`
    - TextImageSection.tsx inneholder `imagePosition` og `flex-row-reverse`
    - ContentBlockEditor.tsx eksisterer IKKE (ls returnerer "No such file or directory")
    - TypeScript kompilerer: npx tsc --noEmit returnerer 0
  </acceptance_criteria>
  <done>TextSection rendrer HTML-innhold. TextImageSection respekterer imagePosition. Dead code slettet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin CMS → TextSection render | HTML fra Tiptap (admin-generert) rendres med dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering | dangerouslySetInnerHTML i TextSection og TextImageSection | accept | Innholdet er admin-generert via Tiptap (betrodd kilde). Kun admin-rolle kan lagre innhold. XSS fra ekstern kilde er ikke relevant |
| T-07-11 | Information Disclosure | Vis side-lenke avslører slug | accept | Slugs er offentlige URL-er — ikke sensitiv informasjon |
</threat_model>

<verification>
```bash
# Sjekk TextSection har body-rendering
grep -n "dangerouslySetInnerHTML\|section\.body" /home/william/Documents/Rootsnew/src/components/sections/TextSection.tsx

# Sjekk TextImageSection har imagePosition
grep -n "imagePosition\|flex-row-reverse" /home/william/Documents/Rootsnew/src/components/sections/TextImageSection.tsx

# Sjekk ContentBlockEditor er slettet
ls /home/william/Documents/Rootsnew/src/components/admin/ContentBlockEditor.tsx 2>&1

# Sjekk ingen importerer ContentBlockEditor
grep -r "ContentBlockEditor" /home/william/Documents/Rootsnew/src --include="*.tsx" --include="*.ts"

# TypeScript
cd /home/william/Documents/Rootsnew && npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
- trust-bar items kan redigeres i seksjonseditoren (tittel, beskrivelse, ikon)
- text-seksjon har Tiptap-editor og viser HTML på siden
- text-image har imagePosition-toggle som endrer bildeplassering
- contact-info elementer har href-input
- gallery-overskrift er synlig i admin-editoren
- "Vis side"-lenke finnes i page-editoren
- ContentBlockEditor.tsx er slettet
- TypeScript kompilerer uten feil
</success_criteria>

<output>
After completion, create `.planning/phases/07-cms-fikser-og-admin-crud/07-05-SUMMARY.md`
</output>
