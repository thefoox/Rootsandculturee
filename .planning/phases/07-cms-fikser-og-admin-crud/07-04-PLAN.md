---
phase: 07-cms-fikser-og-admin-crud
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/opplevelser/[id]/page.tsx
  - src/app/admin/artikler/[id]/page.tsx
autonomous: true
requirements: [ADMN-11, ADMN-12, ADMN-13, ADMN-14]

must_haves:
  truths:
    - "Admin kan redigere en opplevelse med datoer/plasser via DateSlotsEditor"
    - "Admin kan redigere en artikkel med Tiptap-editor, forsidebilde og publiser/kladd"
    - "Ordrer og bookinger vises korrekt — eksisterende sider er verifisert fungerende"
  artifacts:
    - path: "src/app/admin/opplevelser/[id]/page.tsx"
      provides: "Edit-skjema for opplevelse med DateSlotsEditor"
      contains: "DateSlotsEditor"
    - path: "src/app/admin/artikler/[id]/page.tsx"
      provides: "Edit-skjema med TiptapEditor og PublishBar"
      contains: "TiptapEditor"
  key_links:
    - from: "opplevelser/[id]/page.tsx"
      to: "updateExperience(id, formData)"
      via: "submitForm med dates JSON"
      pattern: "updateExperience"
    - from: "artikler/[id]/page.tsx"
      to: "updateArticle(id, formData)"
      via: "submitForm med body HTML"
      pattern: "updateArticle"
---

<objective>
Verifiser og fiks CRUD-redigering for opplevelser og artikler. Kontroller at opplevelses-edit-siden har DateSlotsEditor og at artikkel-edit-siden har TiptapEditor med riktig datainitalisering.

Purpose: ADMN-11 og ADMN-12 krever komplett CRUD. Create-sidene er verifisert OK (har DateSlotsEditor og TiptapEditor). Edit-sidene må sjekkes og fikses.
Output: Fungerende edit-sider for opplevelser og artikler.
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<interfaces>
Fra src/actions/experiences.ts:
```typescript
export async function updateExperience(id: string, formData: FormData): Promise<{ success: boolean; errors?: Record<string, string> }>
export async function getExperienceDatesAdmin(experienceId: string): Promise<ExperienceDate[]>
```

Fra src/types/index.ts:
```typescript
export interface ExperienceDate {
  id: string
  date: Date
  maxSeats: number
  bookedSeats: number
  availableSeats: number
  isActive: boolean
  priceOverride: number | null
  earlyBirdPrice: number | null
  earlyBirdDeadline: Date | null
}
```

Fra src/app/admin/opplevelser/ny/page.tsx — DateSlotsEditor brukes med:
```typescript
<DateSlotsEditor dates={dates} onChange={setDates} />
// dates er: { date: string; maxSeats: number; earlyBirdPrice: string; earlyBirdDeadline: string }[]
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Sjekk og fiks opplevelse-edit-side med DateSlotsEditor</name>
  <files>src/app/admin/opplevelser/[id]/page.tsx</files>
  <read_first>
    - src/app/admin/opplevelser/[id]/page.tsx (hele filen)
    - src/app/admin/opplevelser/ny/page.tsx (som referanse for skjemastruktur)
    - src/actions/experiences.ts (getExperienceDatesAdmin og updateExperience)
  </read_first>
  <action>
Les `src/app/admin/opplevelser/[id]/page.tsx` og evaluer:

**Scenario A — DateSlotsEditor finnes ikke:**
Oppdater filen til å ligne på `ny/page.tsx`, men med:
1. `useEffect` som laster `getExperienceById(id)` OG `getExperienceDatesAdmin(id)`
2. `useState` for dates initialisert fra lastede ExperienceDate-data
3. DateSlotsEditor-seksjon i JSX
4. `submitForm` som sender dates som JSON via formData

Datodata fra Firestore er `ExperienceDate[]` — konverter til skjema-format:
```typescript
setDates(loadedDates.map((d) => ({
  date: d.date instanceof Date 
    ? d.date.toISOString().split('T')[0] 
    : new Date(d.date).toISOString().split('T')[0],
  maxSeats: d.maxSeats,
  earlyBirdPrice: d.earlyBirdPrice ? String(d.earlyBirdPrice / 100) : '',
  earlyBirdDeadline: d.earlyBirdDeadline 
    ? (d.earlyBirdDeadline instanceof Date ? d.earlyBirdDeadline : new Date(d.earlyBirdDeadline)).toISOString().split('T')[0] 
    : '',
})))
```

**Scenario B — DateSlotsEditor finnes allerede:**
Verifiser at datainitialiseringen er korrekt (se konverteringen ovenfor). Fiks eventuelle bugs i date-konvertering.

Uavhengig av scenario: sjekk at ALLE feltene fra `ny/page.tsx` finnes i `[id]/page.tsx`:
- name, slug, description, category
- location, durationText
- images
- basePrice
- whatIsIncluded, cancellationPolicy, whatToBring
- dates (DateSlotsEditor)
- isPublished state og PublishBar

Legg til manglende felt.
  </action>
  <verify>
    <automated>grep -n "DateSlotsEditor\|getExperienceDatesAdmin\|updateExperience" /home/william/Documents/Rootsnew/src/app/admin/opplevelser/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Filen importerer DateSlotsEditor
    - Filen kaller getExperienceDatesAdmin(id) i useEffect
    - Filen kaller updateExperience(id, formData) i submitForm
    - dates sendes som `formData.set('dates', JSON.stringify(dates))`
    - TypeScript kompilerer: npx tsc --noEmit returnerer 0
  </acceptance_criteria>
  <done>Admin kan redigere opplevelse med alle felt inkl. datoer og plasser.</done>
</task>

<task type="auto">
  <name>Task 2: Sjekk og fiks artikkel-edit-side + verifiser ordrer/bookinger</name>
  <files>src/app/admin/artikler/[id]/page.tsx</files>
  <read_first>
    - src/app/admin/artikler/[id]/page.tsx (hele filen)
    - src/app/admin/artikler/ny/page.tsx (referanse)
    - src/app/admin/ordrer/[id]/page.tsx (verifiser refund-funksjonalitet)
    - src/app/admin/bookinger/page.tsx (verifiser kansellering)
  </read_first>
  <action>
**Del A — artikkel-edit (`src/app/admin/artikler/[id]/page.tsx`):**

Les filen og sjekk:
1. Importeres TiptapEditor? Hvis nei, legg til.
2. Initialiseres `body` state fra `article.body` (HTML fra Tiptap) i useEffect?
3. Initialiseres `isPublished` state fra `article.status === 'published'` i useEffect?
4. Sendes `body` i formData (som HTML-streng)?
5. Har filen PublishBar med riktig `isPublished`-prop?

Fix all issues funnet. Kritisk: TiptapEditor trenger `content={body}` og `onChange={setBody}`. Body-feltet er HTML fra Tiptap — det MÅ initialises korrekt fra `article.body` for at redigering skal vise eksisterende innhold.

Sjekk at `formData.set('publish', ...)` bruker korrekt verdi fra `isPublished` state:
```typescript
formData.set('publish', String(isPublished))
```

**Del B — verifiser ordrer og bookinger (ADMN-13, ADMN-14):**

Les `src/app/admin/ordrer/[id]/page.tsx` og `src/app/admin/bookinger/page.tsx`:
- ordrer: verifiser at `updateOrderStatus`, `RefundDialog` og `getRefundsForOrder` kalles korrekt
- bookinger: verifiser at `cancelBooking` kalles og at status oppdateres i UI

Gjør INGEN endringer i ordrer/bookinger-sidene med mindre det finnes konkrete bugs.

Dokumenter funn i SUMMARY.
  </action>
  <verify>
    <automated>grep -n "TiptapEditor\|article\.body\|article\.status\|isPublished\|PublishBar" /home/william/Documents/Rootsnew/src/app/admin/artikler/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `artikler/[id]/page.tsx` importerer og bruker TiptapEditor
    - body initialiseres fra `article.body` i useEffect
    - isPublished initialiseres fra `article.status === 'published'`
    - formData inneholder body og publish
    - TypeScript kompilerer: npx tsc --noEmit returnerer 0
  </acceptance_criteria>
  <done>Admin kan redigere artikler med Tiptap og publiser/kladd-status. Ordrer og bookinger er verifisert fungerende.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin form → updateExperience/updateArticle | FormData med HTML-innhold (body) fra Tiptap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | HTML body-innhold fra Tiptap i artikler | accept | Tiptap sanitiserer output på klienten — serveren lagrer det som-er. Admin-only, lav ekstern risiko |
| T-07-09 | Tampering | Dates JSON-injeksjon i experiences | mitigate | Zod-validering i experienceSchema avviser ugyldig dato-format |
</threat_model>

<verification>
```bash
# Sjekk artikkel-edit
grep -n "TiptapEditor\|article\.body" /home/william/Documents/Rootsnew/src/app/admin/artikler/\[id\]/page.tsx

# Sjekk opplevelse-edit
grep -n "DateSlotsEditor\|updateExperience" /home/william/Documents/Rootsnew/src/app/admin/opplevelser/\[id\]/page.tsx

# TypeScript
cd /home/william/Documents/Rootsnew && npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
- Artikkel-edit-side har TiptapEditor med korrekt datainitalisering fra article.body
- Opplevelses-edit-side har DateSlotsEditor med korrekt datainitalisering
- Ordrer og bookinger er verifisert fungerende (status-oppdatering, refund, kansellering)
- TypeScript kompilerer uten feil
</success_criteria>

<output>
After completion, create `.planning/phases/07-cms-fikser-og-admin-crud/07-04-SUMMARY.md`
</output>
