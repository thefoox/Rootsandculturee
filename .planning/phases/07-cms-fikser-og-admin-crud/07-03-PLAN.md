---
phase: 07-cms-fikser-og-admin-crud
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/produkter/ny/page.tsx
  - src/app/admin/produkter/[id]/page.tsx
autonomous: true
requirements: [ADMN-10]

must_haves:
  truths:
    - "Admin kan opprette produkt med varianter (f.eks. 250ml / 500ml med egne priser og lagertall)"
    - "Admin kan redigere et eksisterende produkts varianter"
    - "Varianter sendes korrekt til createProduct/updateProduct via formData som JSON"
  artifacts:
    - path: "src/app/admin/produkter/ny/page.tsx"
      provides: "Inline VariantsEditor-seksjon for oppretting"
      contains: "varianter"
    - path: "src/app/admin/produkter/[id]/page.tsx"
      provides: "Inline VariantsEditor-seksjon for redigering"
      contains: "varianter"
  key_links:
    - from: "ny/page.tsx variants state"
      to: "createProduct(formData)"
      via: "formData.set('variants', JSON.stringify(variants))"
      pattern: "variants.*JSON\.stringify"
    - from: "productSchema"
      to: "variants array"
      via: "Zod-validering"
      pattern: "variants"
---

<objective>
Legg til variant-redigering i produktadmin: opprett- og redigeringsskjemaene mangler VariantsEditor-seksjon. Produkter kan ha varianter (f.eks. størrelser/volum) med egne priser og lagertall.

Purpose: ADMN-10 krever at produkter CRUD fungerer komplett inkl. varianter. Uten dette kan admin ikke opprette produkter med ulike størrelser/varianter.
Output: Produktopprettelse og -redigering med inline variantredigering.
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<interfaces>
Fra src/types/index.ts:
```typescript
export interface ProductVariant {
  id: string
  label: string          // e.g. "250 ml flaske"
  price: number          // NOK in ore (integer)
  inStock: boolean
  stockCount: number
}
```

Fra src/lib/validations.ts — productSchema har IKKE variants ennå. Sjekk dette ved lesing.

Fra src/actions/products.ts — createProduct og updateProduct aksepterer FormData. Variant-feltet sendes som `formData.get('variants')` — sjekk om dette finnes i server action.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Legg til variants-støtte i productSchema og server actions</name>
  <files>src/lib/validations.ts, src/actions/products.ts</files>
  <read_first>
    - src/lib/validations.ts (productSchema — sjekk om variants finnes)
    - src/actions/products.ts (createProduct og updateProduct — sjekk om variants håndteres)
    - src/types/index.ts (ProductVariant interface)
  </read_first>
  <action>
**Steg 1 — sjekk eksisterende kode:**

Les `src/lib/validations.ts` nøye. Hvis `productSchema` allerede har `variants`-felt, hopp over schema-endringen. Hvis ikke, legg til:

```typescript
// I productSchema, legg til variants som optional:
variants: z.array(z.object({
  id: z.string(),
  label: z.string().min(1, 'Variantnavn er påkrevd.'),
  price: z.number().int().positive('Variantpris må være positiv.'),
  inStock: z.boolean().optional().default(true),
  stockCount: z.number().int().min(0).default(0),
})).optional().default([]),
```

**Steg 2 — sjekk createProduct:**

Les `src/actions/products.ts`. Hvis `createProduct` allerede leser `formData.get('variants')`, verifiser at det parses korrekt og at varianter lagres i Firestore. Hvis IKKE, legg til:

```typescript
// I createProduct, etter rawImages-linjen:
const rawVariants = formData.get('variants') as string

// I productSchema.safeParse-kallet:
variants: rawVariants ? JSON.parse(rawVariants) : [],

// I adminDb.collection('products').add({...}) kallet:
variants: (parsed.data.variants || []).map((v) => ({
  id: v.id || `variant-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
  label: v.label,
  price: Math.round(Number(v.price) * 100), // Prisen er allerede i ore fra skjema
  inStock: v.stockCount > 0,
  stockCount: v.stockCount,
})),
```

VIKTIG: Prisen i formData for varianter sendes som NOK (desimaltall fra brukeren), konverteres til ore i front-end state ELLER i server action. Bestem et konsistent mønster: skjemaet sender `price` som NOK-verdi (float) og server action konverterer til ore med `Math.round(Number(v.price) * 100)`. ELLER: skjemaet sender allerede ore.

Velg mønster: **Brukeren taster pris i NOK, server action konverterer til ore.** Dette er konsistent med mainPrice-feltet i samme action.

Gjør tilsvarende endring i `updateProduct`.
  </action>
  <verify>
    <automated>grep -n "variants\|rawVariants" /home/william/Documents/Rootsnew/src/lib/validations.ts /home/william/Documents/Rootsnew/src/actions/products.ts</automated>
  </verify>
  <acceptance_criteria>
    - productSchema i validations.ts har variants-felt (enten det allerede finnes eller legges til)
    - createProduct i products.ts leser formData.get('variants') og lagrer til Firestore
    - updateProduct i products.ts oppdaterer variants-feltet
    - TypeScript kompilerer: npx tsc --noEmit returnerer 0
  </acceptance_criteria>
  <done>Server-siden håndterer produktvarianter fra formData.</done>
</task>

<task type="auto">
  <name>Task 2: Inline VariantsEditor i produkt opprett- og redigeringsskjema</name>
  <files>src/app/admin/produkter/ny/page.tsx, src/app/admin/produkter/[id]/page.tsx</files>
  <read_first>
    - src/app/admin/produkter/ny/page.tsx (hele filen — legg merke til form-strukturen)
    - src/app/admin/produkter/[id]/page.tsx (hele filen — spesielt useEffect som laster data)
    - src/types/index.ts (ProductVariant interface)
  </read_first>
  <action>
Legg til inline variantredigering i BEGGE skjemaene. Implementer det IDENTISK i begge filer.

**State å legge til:**
```typescript
const [variants, setVariants] = useState<Array<{
  id: string
  label: string
  price: string  // NOK som string for input
  stockCount: string
}>>([])
```

**I `[id]/page.tsx` — initialiser variants fra loadet produkt:**
I `useEffect` der produktet lastes, legg til:
```typescript
setVariants((product.variants || []).map((v) => ({
  id: v.id,
  label: v.label,
  price: String(v.price / 100),  // ore til NOK
  stockCount: String(v.stockCount),
})))
```

**I `submitForm`-funksjonen, legg til variants i formData ETTER de andre feltene:**
```typescript
formData.set('variants', JSON.stringify(
  variants.map((v) => ({
    id: v.id || `v-${Date.now()}-${Math.random().toString(36).slice(2, 5)}`,
    label: v.label,
    price: Number(v.price),    // NOK — server action konverterer til ore
    stockCount: Number(v.stockCount),
  }))
))
```

**JSX-seksjon å legge til etter kategori-seksjonen og FØR PublishBar:**
```tsx
<hr className="border-forest/12" />

{/* Varianter */}
<section>
  <h2 className="mb-4 font-heading text-h4 font-bold text-forest">
    Varianter <span className="text-label font-normal text-body/60">(valgfritt)</span>
  </h2>
  <p className="mb-4 text-label text-body/70">
    Legg til varianter hvis produktet finnes i ulike størrelser, volum eller typer.
  </p>
  <div className="space-y-3">
    {variants.map((v, i) => (
      <div key={v.id || i} className="flex flex-wrap items-end gap-3 rounded-lg border border-forest/10 bg-card p-4">
        <div className="flex-1 min-w-[140px]">
          <label className="mb-1 block text-label font-medium text-forest" htmlFor={`v-label-${i}`}>
            Navn
          </label>
          <input
            id={`v-label-${i}`}
            type="text"
            value={v.label}
            onChange={(e) => {
              const updated = [...variants]
              updated[i] = { ...updated[i], label: e.target.value }
              setVariants(updated)
            }}
            placeholder="F.eks. 250 ml"
            className="w-full rounded-md border border-forest/20 bg-cream px-3 py-2 text-body text-forest focus:border-forest focus:outline-none"
          />
        </div>
        <div className="w-28">
          <label className="mb-1 block text-label font-medium text-forest" htmlFor={`v-price-${i}`}>
            Pris (NOK)
          </label>
          <input
            id={`v-price-${i}`}
            type="number"
            min={0}
            value={v.price}
            onChange={(e) => {
              const updated = [...variants]
              updated[i] = { ...updated[i], price: e.target.value }
              setVariants(updated)
            }}
            className="w-full rounded-md border border-forest/20 bg-cream px-3 py-2 text-body text-forest focus:border-forest focus:outline-none"
          />
        </div>
        <div className="w-24">
          <label className="mb-1 block text-label font-medium text-forest" htmlFor={`v-stock-${i}`}>
            Lager
          </label>
          <input
            id={`v-stock-${i}`}
            type="number"
            min={0}
            value={v.stockCount}
            onChange={(e) => {
              const updated = [...variants]
              updated[i] = { ...updated[i], stockCount: e.target.value }
              setVariants(updated)
            }}
            className="w-full rounded-md border border-forest/20 bg-cream px-3 py-2 text-body text-forest focus:border-forest focus:outline-none"
          />
        </div>
        <button
          type="button"
          onClick={() => setVariants(variants.filter((_, idx) => idx !== i))}
          className="shrink-0 rounded-md px-3 py-2 text-label text-body/60 hover:text-destructive"
          aria-label={`Fjern variant ${v.label || i + 1}`}
        >
          Fjern
        </button>
      </div>
    ))}
  </div>
  <button
    type="button"
    onClick={() => setVariants([...variants, { id: '', label: '', price: '', stockCount: '0' }])}
    className="mt-3 inline-flex items-center gap-1 rounded-lg border border-dashed border-forest/20 px-3 py-2 text-sm text-forest hover:bg-card"
  >
    <span aria-hidden="true">+</span>
    Legg til variant
  </button>
</section>
```

Implementer IDENTISK i `ny/page.tsx` og `[id]/page.tsx`. Eneste forskjell er at `[id]/page.tsx` initialiserer `variants` fra det lastede produktet i useEffect.
  </action>
  <verify>
    <automated>grep -n "Varianter\|setVariants\|v-label\|variants.*JSON" /home/william/Documents/Rootsnew/src/app/admin/produkter/ny/page.tsx /home/william/Documents/Rootsnew/src/app/admin/produkter/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Begge filer inneholder `useState` for variants
    - Begge filer inneholder `formData.set('variants', JSON.stringify(...))`
    - `[id]/page.tsx` initialiserer variants fra `product.variants` i useEffect
    - JSX inneholder variantrader med felt for label, pris (NOK), lagertall
    - "Legg til variant"-knapp finnes i begge filer
    - TypeScript kompilerer: npx tsc --noEmit returnerer 0
  </acceptance_criteria>
  <done>Admin kan opprette og redigere produkter med varianter inkl. pris og lagertall per variant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin form → createProduct/updateProduct | FormData med variants JSON sendes til server action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | variants JSON-injeksjon via FormData | mitigate | Zod-validering av variants i productSchema avviser ugyldig data |
| T-07-07 | Tampering | Negativ pris på variant | mitigate | Zod schema: price: z.number().int().positive() |
</threat_model>

<verification>
```bash
# Sjekk variants i ny-skjema
grep -n "variants\|Varianter" /home/william/Documents/Rootsnew/src/app/admin/produkter/ny/page.tsx | head -10

# Sjekk variants i edit-skjema
grep -n "variants\|Varianter" /home/william/Documents/Rootsnew/src/app/admin/produkter/\[id\]/page.tsx | head -10

# Sjekk server action håndterer variants
grep -n "rawVariants\|variants" /home/william/Documents/Rootsnew/src/actions/products.ts

# TypeScript
cd /home/william/Documents/Rootsnew && npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
- Admin kan legge til og fjerne varianter i produktskjemaet
- Varianter lagres i Firestore med label, price (ore), inStock, stockCount
- Eksisterende varianter lastes inn i redigeringsskjemaet
- TypeScript kompilerer uten feil
</success_criteria>

<output>
After completion, create `.planning/phases/07-cms-fikser-og-admin-crud/07-03-SUMMARY.md`
</output>
