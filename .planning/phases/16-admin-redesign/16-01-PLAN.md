---
phase: 16-admin-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/AdminSidebar.tsx
  - src/components/admin/AdminTopBar.tsx
autonomous: true
requirements:
  - ADR-01
  - ADR-02
  - ADR-04
  - ADR-05

must_haves:
  truths:
    - "Desktop sidebar has white background instead of dark green"
    - "Every nav item shows a lucide-react icon next to its label"
    - "Active nav item has card-colored background with forest left border"
    - "Mobile top bar has white background with forest text"
    - "Mobile hamburger uses lucide Menu icon, mobile close uses lucide X icon"
    - "Footer actions (Tilbake, Logg ut) show lucide ArrowLeft and LogOut icons"
    - "Sidebar has a right border separating it from the cream content area"
    - "Focus rings on sidebar items use forest green (not cream)"
  artifacts:
    - path: "src/components/admin/AdminSidebar.tsx"
      provides: "White sidebar with lucide icons in expanded mode"
      contains: "from 'lucide-react'"
    - path: "src/components/admin/AdminTopBar.tsx"
      provides: "White mobile top bar with lucide Menu icon"
      contains: "Menu"
  key_links:
    - from: "src/components/admin/AdminSidebar.tsx"
      to: "lucide-react"
      via: "named icon imports"
      pattern: "Package|TreePine|FileText|LayoutTemplate|ShoppingBag|CalendarDays|Gift|Users|ArrowLeft|LogOut|X"
---

<objective>
Retheme the admin sidebar and mobile top bar from dark green to white, and add lucide-react icons to all navigation items.

Purpose: Replace the dark admin-sidebar color scheme with a white panel that uses forest-green text, card-colored hover/active states, and a right-side border separator. Add lucide-react icons to every nav item and footer action for better visual navigation.

Output: Updated AdminSidebar.tsx (white bg, icons, new hover/active states) and AdminTopBar.tsx (white bg, lucide Menu icon).
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-admin-redesign/16-UI-SPEC.md

<interfaces>
<!-- Current AdminSidebar nav item arrays that must be extended with icon references -->
From src/components/admin/AdminSidebar.tsx:
```typescript
const contentNavItems = [
  { href: '/admin/produkter', label: 'Produkter' },
  { href: '/admin/opplevelser', label: 'Opplevelser' },
  { href: '/admin/artikler', label: 'Artikler' },
  { href: '/admin/innhold', label: 'Sideinnhold' },
]

const orderNavItems = [
  { href: '/admin/ordrer', label: 'Ordrer' },
  { href: '/admin/bookinger', label: 'Bookinger' },
  { href: '/admin/gavekort', label: 'Gavekort' },
  { href: '/admin/kunder', label: 'Kunder' },
]

interface AdminSidebarProps {
  mobile?: boolean
  onClose?: () => void
}
```

From src/components/admin/AdminTopBar.tsx:
```typescript
interface AdminTopBarProps {
  onMenuClick: () => void
}
```

From src/app/globals.css (relevant tokens):
```css
--color-forest: #1B4332;
--color-cream: #FEFCF3;
--color-card: #F5F0E8;
--color-rust: #A0440B;
--color-admin-sidebar: #1A2E23;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retheme AdminSidebar to white with lucide icons</name>
  <files>src/components/admin/AdminSidebar.tsx</files>
  <read_first>
    - src/components/admin/AdminSidebar.tsx (current implementation — 134 lines)
    - .planning/phases/16-admin-redesign/16-UI-SPEC.md (design contract)
    - src/app/globals.css (color tokens — lines 1-62)
  </read_first>
  <action>
Modify `src/components/admin/AdminSidebar.tsx` with these exact changes:

**1. Add lucide-react imports at the top of the file:**
```typescript
import { Package, TreePine, FileText, LayoutTemplate, ShoppingBag, CalendarDays, Gift, Users, ArrowLeft, LogOut, X } from 'lucide-react'
```

**2. Add `icon` field to nav item arrays using `LucideIcon` type:**
```typescript
import type { LucideIcon } from 'lucide-react'

const contentNavItems: { href: string; label: string; icon: LucideIcon }[] = [
  { href: '/admin/produkter', label: 'Produkter', icon: Package },
  { href: '/admin/opplevelser', label: 'Opplevelser', icon: TreePine },
  { href: '/admin/artikler', label: 'Artikler', icon: FileText },
  { href: '/admin/innhold', label: 'Sideinnhold', icon: LayoutTemplate },
]

const orderNavItems: { href: string; label: string; icon: LucideIcon }[] = [
  { href: '/admin/ordrer', label: 'Ordrer', icon: ShoppingBag },
  { href: '/admin/bookinger', label: 'Bookinger', icon: CalendarDays },
  { href: '/admin/gavekort', label: 'Gavekort', icon: Gift },
  { href: '/admin/kunder', label: 'Kunder', icon: Users },
]
```

**3. Replace `<aside>` className — remove `dark-surface` and `bg-admin-sidebar`, add white bg + border:**
Change from:
```
'dark-surface flex h-full w-[240px] flex-col bg-admin-sidebar',
```
To:
```
'flex h-full w-[240px] flex-col bg-white border-r border-forest/12',
```

**4. Update brand label text color:**
Change `text-cream` to `text-forest` in the brand label span.

**5. Update mobile close button:**
Replace the close button content from `<span aria-hidden="true">✕</span>` to `<X className="h-5 w-5" aria-hidden="true" />`. Change button `text-cream` to `text-forest`.

**6. Update section header colors (keep as-is):**
- "Innhold" header already uses `text-forest` — no change needed.
- "Ordre & Kunder" header already uses `text-rust` — no change needed.

**7. Update nav link classes for BOTH contentNavItems and orderNavItems map blocks:**
Change nav link `<Link>` className from:
```
'flex h-[44px] items-center px-4 text-body text-cream',
'hover:bg-[rgba(254,252,243,0.08)]',
isActive && 'border-l-[3px] border-forest bg-[rgba(254,252,243,0.08)]'
```
To:
```
'flex h-[44px] items-center gap-3 px-4 text-body text-forest',
'hover:bg-card/60',
isActive && 'border-l-[3px] border-forest bg-card'
```
Note: Added `gap-3` for icon-to-label spacing.

**8. Add icon rendering inside each nav Link (both contentNavItems and orderNavItems):**
Inside the `<Link>`, before `{item.label}`, add:
```tsx
<item.icon className="h-5 w-5 shrink-0" aria-hidden="true" />
```
The component pattern `<item.icon />` works because `icon` is a React component reference (LucideIcon type).

**9. Update footer section border:**
Change `border-t border-cream/10` to `border-t border-forest/12`.

**10. Update "Tilbake til nettbutikk" link:**
Change `text-cream` to `text-forest`. Change `hover:bg-[rgba(254,252,243,0.08)]` to `hover:bg-card/60`. Replace `<span aria-hidden="true">←</span>` with `<ArrowLeft className="h-5 w-5 shrink-0" aria-hidden="true" />`.

**11. Update "Logg ut" button:**
Change `text-cream` to `text-forest`. Change `hover:bg-[rgba(254,252,243,0.08)]` to `hover:text-destructive hover:bg-card/60`. Add icon before the text: `<LogOut className="h-5 w-5 shrink-0" aria-hidden="true" />`. Add `gap-3` to the button's className for icon spacing.
  </action>
  <acceptance_criteria>
    - grep "from 'lucide-react'" src/components/admin/AdminSidebar.tsx returns a match containing Package, TreePine, FileText, LayoutTemplate, ShoppingBag, CalendarDays, Gift, Users, ArrowLeft, LogOut, X
    - grep "dark-surface" src/components/admin/AdminSidebar.tsx returns NO matches
    - grep "bg-admin-sidebar" src/components/admin/AdminSidebar.tsx returns NO matches
    - grep "bg-white" src/components/admin/AdminSidebar.tsx returns at least 1 match
    - grep "border-r border-forest/12" src/components/admin/AdminSidebar.tsx returns at least 1 match
    - grep "text-cream" src/components/admin/AdminSidebar.tsx returns NO matches
    - grep "text-forest" src/components/admin/AdminSidebar.tsx returns at least 4 matches
    - grep "item.icon" src/components/admin/AdminSidebar.tsx returns at least 2 matches (both nav lists)
    - grep "hover:bg-card" src/components/admin/AdminSidebar.tsx returns at least 2 matches
    - grep "rgba(254,252,243" src/components/admin/AdminSidebar.tsx returns NO matches
    - grep "ArrowLeft" src/components/admin/AdminSidebar.tsx returns at least 1 match
    - grep "LogOut" src/components/admin/AdminSidebar.tsx returns at least 1 match
    - grep "gap-3" src/components/admin/AdminSidebar.tsx returns at least 2 matches
    - npm run build completes without errors in src/components/admin/AdminSidebar.tsx
  </acceptance_criteria>
  <verify>
    <automated>cd /home/william/Documents/Rootsnew && npx tsc --noEmit src/components/admin/AdminSidebar.tsx 2>&1 | head -20</automated>
  </verify>
  <done>AdminSidebar has white background, forest text, border-r separator, lucide icons on all 8 nav items + 2 footer actions, card-colored hover/active states, no dark-surface class, no cream text, no rgba hover backgrounds.</done>
</task>

<task type="auto">
  <name>Task 2: Retheme AdminTopBar to white with lucide Menu icon</name>
  <files>src/components/admin/AdminTopBar.tsx</files>
  <read_first>
    - src/components/admin/AdminTopBar.tsx (current implementation — 23 lines)
    - .planning/phases/16-admin-redesign/16-UI-SPEC.md (Mobile Behavior Contract section)
  </read_first>
  <action>
Modify `src/components/admin/AdminTopBar.tsx` with these exact changes:

**1. Add lucide-react import:**
```typescript
import { Menu } from 'lucide-react'
```

**2. Replace outer div className:**
Change from:
```
"dark-surface flex h-[56px] items-center bg-admin-sidebar px-4 md:hidden"
```
To:
```
"flex h-[56px] items-center bg-white border-b border-forest/12 px-4 md:hidden"
```

**3. Update hamburger button:**
Change `text-cream` to `text-forest` in the button className.
Replace the inner content from:
```tsx
<span className="text-xl" aria-hidden="true">☰</span>
```
To:
```tsx
<Menu className="h-6 w-6" aria-hidden="true" />
```

**4. Update center text:**
Change `text-cream` to `text-forest` in the center span className.
  </action>
  <acceptance_criteria>
    - grep "dark-surface" src/components/admin/AdminTopBar.tsx returns NO matches
    - grep "bg-admin-sidebar" src/components/admin/AdminTopBar.tsx returns NO matches
    - grep "bg-white" src/components/admin/AdminTopBar.tsx returns at least 1 match
    - grep "border-b border-forest/12" src/components/admin/AdminTopBar.tsx returns at least 1 match
    - grep "text-cream" src/components/admin/AdminTopBar.tsx returns NO matches
    - grep "text-forest" src/components/admin/AdminTopBar.tsx returns at least 2 matches
    - grep "Menu" src/components/admin/AdminTopBar.tsx returns at least 1 match (lucide icon)
    - grep "from 'lucide-react'" src/components/admin/AdminTopBar.tsx returns a match
    - The string "☰" does NOT appear in src/components/admin/AdminTopBar.tsx
    - npx tsc --noEmit src/components/admin/AdminTopBar.tsx completes without errors
  </acceptance_criteria>
  <verify>
    <automated>cd /home/william/Documents/Rootsnew && npx tsc --noEmit src/components/admin/AdminTopBar.tsx 2>&1 | head -20</automated>
  </verify>
  <done>AdminTopBar has white background with bottom border, forest text, lucide Menu icon replacing text hamburger, no dark-surface class, no cream text, no admin-sidebar bg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin auth | All admin pages already protected by middleware — unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Spoofing | AdminSidebar | accept | Pure visual change — auth unchanged, signOut logic unchanged |
| T-16-02 | Information Disclosure | AdminTopBar | accept | No data displayed — purely structural/styling change |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes for both modified files
2. No `dark-surface` class remains on admin sidebar or top bar
3. No `bg-admin-sidebar` class remains on either component
4. No `text-cream` class remains on either component
5. All 10 lucide icons imported and rendered
6. `npm run build` succeeds
</verification>

<success_criteria>
- AdminSidebar renders with white (#FFFFFF) background, forest (#1B4332) text
- All 8 nav items display a lucide icon + label
- Footer "Tilbake" shows ArrowLeft icon, "Logg ut" shows LogOut icon
- Mobile close button uses lucide X instead of text ✕
- Active state: bg-card + border-l-[3px] border-forest
- Hover state: bg-card/60
- AdminTopBar renders white bg + forest text + lucide Menu icon
- Sidebar has border-r border-forest/12 separator
- No dark-surface class on any admin component
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/16-admin-redesign/16-01-SUMMARY.md`
</output>
