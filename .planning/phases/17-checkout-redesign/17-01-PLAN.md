---
phase: 17-checkout-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/cart/OrderSummaryPanel.tsx
  - src/components/checkout/GiftCardInput.tsx
autonomous: true
requirements:
  - UI-SPEC-summary-panel
  - UI-SPEC-gift-card

must_haves:
  truths:
    - "OrderSummaryPanel uses warm bark borders instead of cold forest borders"
    - "Totalt price displays at text-h4 size with font-bold weight"
    - "Subtotal and Frakt labels use text-body color not text-forest"
    - "Divider uses bark/20 color"
    - "GiftCardInput collapses by default with a disclosure toggle"
    - "GiftCardInput applied state uses green success background"
    - "All font-medium in these files replaced with font-normal or font-bold per spec"
  artifacts:
    - path: "src/components/cart/OrderSummaryPanel.tsx"
      provides: "Premium summary panel with warm borders and Totalt hierarchy"
      contains: "border-bark/20"
    - path: "src/components/checkout/GiftCardInput.tsx"
      provides: "Collapsible gift card input with disclosure toggle and success applied state"
      contains: "aria-expanded"
  key_links:
    - from: "src/components/cart/OrderSummaryPanel.tsx"
      to: "src/app/(public)/checkout/page.tsx"
      via: "imported and rendered in checkout page"
      pattern: "OrderSummaryPanel"
    - from: "src/components/checkout/GiftCardInput.tsx"
      to: "src/app/(public)/checkout/page.tsx"
      via: "imported and rendered in checkout page"
      pattern: "GiftCardInput"
---

<objective>
Upgrade OrderSummaryPanel and GiftCardInput to premium visual quality per the UI-SPEC design contract.

Purpose: These two self-contained components can be redesigned independently before the main CheckoutPage layout changes. OrderSummaryPanel gets warm bark borders and improved Totalt price hierarchy. GiftCardInput gets a disclosure toggle pattern (collapsed by default) and a green success applied state.

Output: Two visually upgraded components ready for integration in the CheckoutPage layout (Plan 03).
</objective>

<execution_context>
@/home/william/.claude/get-shit-done/workflows/execute-plan.md
@/home/william/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-checkout-redesign/17-UI-SPEC.md

@src/components/cart/OrderSummaryPanel.tsx
@src/components/checkout/GiftCardInput.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade OrderSummaryPanel card warmth and Totalt hierarchy</name>
  <files>src/components/cart/OrderSummaryPanel.tsx</files>
  <read_first>
    - src/components/cart/OrderSummaryPanel.tsx
    - .planning/phases/17-checkout-redesign/17-UI-SPEC.md (section: OrderSummaryPanel)
    - src/app/globals.css (for token references)
  </read_first>
  <action>
Modify OrderSummaryPanel.tsx with these exact changes:

1. **Card wrapper border warmth**: Change `rounded-lg border border-forest/12 bg-card p-6` to `rounded-xl border border-bark/20 bg-card p-6`. The rounded-lg becomes rounded-xl, border-forest/12 becomes border-bark/20.

2. **Sammendrag heading letter-spacing**: The h2 currently has `mb-4 font-heading text-h4 font-bold text-forest`. Change `mb-4` to `mb-5` and add `tracking-[-0.015em]` for the premium letter-spacing.

3. **Subtotal row label color**: Change the Subtotal label `<span className="text-body">Subtotal</span>` — it currently uses implicit text-forest from body. Add explicit `text-body` color class: `<span className="text-body text-body-color">Subtotal</span>`. Since text-body is a font-size class and the color token is --color-body, use the Tailwind color class `text-[var(--color-body)]` or use `text-body` for size and add a separate `text-[#374740]` — actually, the simplest correct approach: the span already has `className="text-body"` which sets font-size. The body text color in globals.css is applied to the whole body element. The current code has `text-forest` on the price span but not on the label span. Per UI-SPEC, the label should use `--color-body` (#374740) not `--color-forest`. Since the body already inherits text-forest from `body { @apply text-forest }`, the label needs to override to body color. Add the class: `className="text-body text-body-color"`. Wait — Tailwind v4 with the @theme block creates `text-body` as a color utility from `--color-body`. But `text-body` is ALSO mapped as a font-size from `--font-size-body`. This is a naming conflict. Check the codebase pattern: in globals.css `--color-body: #374740` creates `text-body` as color, and `--font-size-body` creates `text-body` as font-size. Tailwind v4 resolves this by having both: `text-body` applies the font-size (from --font-size-*), and for the color you need `text-[color:var(--color-body)]` or the color class. Looking at existing codebase usage: `text-body` is used as BOTH font-size and color in different contexts. The GiftCardInput uses `text-body` in `text-label text-body` where text-body appears to be a color. Let me be explicit:
    - For the Subtotal label: use `className="text-body text-[color:--color-body]"` — ACTUALLY, looking at the codebase, `text-body` when used with `text-label` or `text-forest` suggests `text-body` IS the color utility (from --color-body). The font-size utility from --font-size-body would be the same name. In practice the codebase uses `text-body` as the font-size AND as color interchangeably — this works because both the size and color are named "body". Let me follow the existing pattern: use `text-body` for font-size and override color with an explicit class. The safest approach matching existing patterns in CheckoutForm.tsx: label text uses pattern like `font-body text-body text-forest` or `font-body text-label text-body` where `text-body` after `text-label` is the COLOR. Following this: change the Subtotal span to: `<span className="font-body text-body">Subtotal</span>` — this sets both the body font family and body font size. The color will inherit from the parent body which is text-forest. To override to --color-body, use: `<span className="font-body text-body" style={{ color: 'var(--color-body)' }}>Subtotal</span>` — NO, avoid inline styles. Instead: the Tailwind class for the --color-body color is `text-body` since it comes from --color-body. The font-size is also text-body from --font-size-body. These clash in Tailwind v4 — but the codebase already uses this pattern. When `text-body` is used alone, it sets the color (as seen in CheckoutForm where `text-body` appears after text-label for color). The font-size text-body is the same value. So `text-body` sets both — which is fine.

    SIMPLIFIED APPROACH: Change the Subtotal label from `<span className="text-body">Subtotal</span>` to `<span className="text-body text-body">Subtotal</span>` — no, that's redundant. The issue is that the current code inherits text-forest for color. The fix: add an explicit non-forest color. Use `<span className="text-body" style={{}}>` — actually the simplest: Subtotal label already has `text-body` which in Tailwind v4 @theme resolves to BOTH font-size: var(--font-size-body) AND color: var(--color-body) since both --font-size-body and --color-body exist. So `text-body` ALREADY applies the body color (#374740). The parent div has no explicit text-forest on it. The body element has text-forest via globals.css, which would cascade. So `text-body` on the span should override the inherited text-forest with --color-body (#374740). This is already correct. But the price span has explicit `text-forest` which overrides back to forest. So the current label inherits forest from body, and `text-body` should give it body color. CONFIRM: just ensure `text-body` class is on both label spans (Subtotal and Frakt).

    - Frakt label: same pattern, ensure `text-body` class is present (it already is).
    - Frakt price: keep `text-body text-forest`.

4. **Divider warmth**: Change `border-t border-forest/12 my-4` to `border-t border-bark/20 my-4`.

5. **Totalt row hierarchy**: 
   - Change Totalt label from `<span className="text-body font-normal text-forest">Totalt</span>` to `<span className="text-body font-bold text-forest">Totalt</span>`.
   - Change Totalt price from `<span className="text-body text-forest">{formatPrice(total)}</span>` to `<span className="text-h4 font-bold text-forest">{formatPrice(total)}</span>`. This bumps the price to text-h4 size (18-20px) per UI-SPEC.
   - Add `pt-3` to the Totalt row div: `<div className="flex items-center justify-between pt-3">`.
  </action>
  <verify>
    <automated>cd /home/william/Documents/Rootsnew && grep -c "border-bark/20" src/components/cart/OrderSummaryPanel.tsx | grep -q "2" && grep -c "rounded-xl" src/components/cart/OrderSummaryPanel.tsx | grep -q "1" && grep "text-h4 font-bold text-forest" src/components/cart/OrderSummaryPanel.tsx | grep -q "formatPrice" && grep "tracking-\[-0.015em\]" src/components/cart/OrderSummaryPanel.tsx | grep -q "Sammendrag" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "rounded-xl border border-bark/20 bg-card p-6" finds the card wrapper
    - grep "border-bark/20" finds exactly 2 matches (card border + divider)
    - grep "text-h4 font-bold text-forest" on Totalt price line finds formatPrice
    - grep "font-bold text-forest" on Totalt label line matches
    - grep "tracking-\[-0.015em\]" on Sammendrag heading matches
    - grep "mb-5" on Sammendrag heading matches
    - grep "pt-3" on Totalt row matches
  </acceptance_criteria>
  <done>OrderSummaryPanel has warm bark/20 borders (card + divider), rounded-xl card, Totalt price at text-h4 font-bold, Totalt label at font-bold, Sammendrag heading with tracking and mb-5, pt-3 on Totalt row.</done>
</task>

<task type="auto">
  <name>Task 2: Add disclosure toggle to GiftCardInput and success applied state</name>
  <files>src/components/checkout/GiftCardInput.tsx</files>
  <read_first>
    - src/components/checkout/GiftCardInput.tsx
    - .planning/phases/17-checkout-redesign/17-UI-SPEC.md (sections: GiftCardInput, Interaction Contracts > Gift card disclosure toggle)
  </read_first>
  <action>
Modify GiftCardInput.tsx with these changes:

1. **Add disclosure toggle state**: Add `const [expanded, setExpanded] = useState(false)` at the top of the component, alongside the existing state. Import `ChevronDown` from lucide-react (add to existing import).

2. **Font weight mapping**: Replace ALL `font-medium` occurrences with `font-normal`. There are 3 occurrences:
   - Line 70: `font-body text-body font-medium text-forest` -> `font-body text-body font-normal text-forest`
   - Line 86: `font-body text-body font-medium text-forest` -> `font-body text-body font-bold text-forest` (this is the applied deduction amount — per UI-SPEC: "applied deduction amount: text-body font-bold (700)")
   - Line 98: `font-body text-body font-medium text-forest` -> `font-body text-body font-normal text-forest`

3. **Applied state visual upgrade**: In the applied state return block (when `appliedCode && appliedBalance !== null`):
   - Change the wrapper div from `rounded-lg border border-forest/12 bg-card p-4` to `rounded-xl border border-success/20 bg-success-bg p-4`
   - Change the Check icon color from `text-badge-easy` to `text-success` (both are #166534, but text-success is semantically correct per UI-SPEC)
   - Change the "Gavekort brukt" label from `font-body text-body font-normal text-forest` to `font-body text-body font-normal text-forest` (stays same after font-medium -> font-normal mapping)

4. **Collapsed state — card wrapper**: Change the non-applied state wrapper from `rounded-lg border border-forest/12 bg-card p-4` to `rounded-xl border border-forest/10 bg-card` (remove p-4 from outer div — padding will be on inner elements).

5. **Disclosure trigger button**: Replace the static header div (`<div className="flex items-center gap-2 mb-3">` containing Ticket icon and "Har du et gavekort?" text) with a clickable disclosure trigger:
   ```jsx
   <button
     type="button"
     onClick={() => setExpanded(!expanded)}
     className="flex w-full items-center justify-between p-4 text-left"
     aria-expanded={expanded}
     aria-controls="gift-card-panel"
   >
     <span className="flex items-center gap-2">
       <Ticket className="h-4 w-4 text-forest" aria-hidden="true" />
       <span className="font-body text-body font-normal text-forest">
         Har du et gavekort?
       </span>
     </span>
     <ChevronDown
       className={cn(
         "h-4 w-4 text-forest/60 motion-safe:transition-transform motion-safe:duration-200",
         expanded && "rotate-180"
       )}
       aria-hidden="true"
     />
   </button>
   ```

6. **Expandable panel**: Wrap the input area (the `<div className="flex gap-2">` and the validBalance block) in a collapsible div:
   ```jsx
   {expanded && (
     <div id="gift-card-panel" className="px-4 pb-4">
       {/* existing input flex gap-2 and validBalance blocks */}
     </div>
   )}
   ```

7. **Import cn utility**: Add `import { cn } from '@/lib/utils'` at the top of the file (needed for the conditional ChevronDown rotation class).

8. **Applied state layout**: The applied state wrapper already has p-4 and should keep it. Only the non-applied collapsed state loses outer padding (the trigger button and panel manage their own padding).
  </action>
  <verify>
    <automated>cd /home/william/Documents/Rootsnew && grep "aria-expanded" src/components/checkout/GiftCardInput.tsx | grep -q "expanded" && grep "aria-controls" src/components/checkout/GiftCardInput.tsx | grep -q "gift-card-panel" && grep "bg-success-bg" src/components/checkout/GiftCardInput.tsx | grep -q "border-success" && grep "ChevronDown" src/components/checkout/GiftCardInput.tsx | grep -q "rotate-180" && grep -c "font-medium" src/components/checkout/GiftCardInput.tsx | grep -q "0" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep "aria-expanded" finds the disclosure trigger button
    - grep "aria-controls.*gift-card-panel" finds the trigger pointing to the panel
    - grep "id=\"gift-card-panel\"" finds the expandable panel
    - grep "bg-success-bg" finds the applied state background
    - grep "border-success/20" finds the applied state border
    - grep "ChevronDown" finds the chevron icon import and usage
    - grep "rotate-180" finds the chevron rotation class
    - grep -c "font-medium" returns 0 (all replaced with font-normal or font-bold)
    - grep "font-bold" on the deduction amount line matches
    - grep "rounded-xl" finds both card states (applied and collapsed)
    - grep "cn(" confirms cn utility is imported and used
  </acceptance_criteria>
  <done>GiftCardInput collapses by default with a disclosure toggle (aria-expanded, aria-controls, ChevronDown rotation). Applied state shows bg-success-bg with border-success/20 and text-success check icon. All font-medium replaced per the 2-weight rule (400/700 only). Cards use rounded-xl.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed in this plan. All changes are presentational — no data flow, API calls, or user input handling is modified.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | I (Info Disclosure) | GiftCardInput applied state | accept | Gift card code display is existing behavior — already shows masked/partial code. No change to what data is shown. |
</threat_model>

<verification>
- Both components render without TypeScript errors: `npx tsc --noEmit`
- OrderSummaryPanel has warm bark borders and Totalt hierarchy
- GiftCardInput has disclosure toggle with correct ARIA attributes
- GiftCardInput applied state uses success colors
- No font-medium remains in either file
</verification>

<success_criteria>
- OrderSummaryPanel card uses rounded-xl border-bark/20
- OrderSummaryPanel Totalt price is text-h4 font-bold
- OrderSummaryPanel divider is border-bark/20
- GiftCardInput starts collapsed (input hidden by default)
- GiftCardInput toggle uses aria-expanded and aria-controls
- GiftCardInput applied state uses bg-success-bg and border-success/20
- Zero occurrences of font-medium in both files
- npx tsc --noEmit passes
</success_criteria>

<output>
After completion, create `.planning/phases/17-checkout-redesign/17-01-SUMMARY.md`
</output>
